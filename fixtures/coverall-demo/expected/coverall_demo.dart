// Generated by uniffi-bindgen-dart. DO NOT EDIT.
// ignore_for_file: unused_element, unused_import, unused_field, unnecessary_non_null_assertion
library coverall_demo;

import 'dart:convert';
import 'dart:ffi' as ffi;
import 'package:ffi/ffi.dart';
import 'dart:typed_data';

final class _RustBuffer extends ffi.Struct {
  external ffi.Pointer<ffi.Uint8> data;

  @ffi.Uint64()
  external int len;
}

final class _RustCallStatus extends ffi.Struct {
  @ffi.Int8()
  external int code;

  external ffi.Pointer<Utf8> errorBuf;
}

const int _rustCallStatusSuccess = 0;
const int _rustCallStatusError = 1;
const int _rustCallStatusUnexpectedError = 2;
const int _rustCallStatusCancelled = 3;
const _sentinel = Object();

/// Record with default values — tests default field generation.
class DictWithDefaults {
  const DictWithDefaults({
    this.name = 'default-value',
    this.category = null,
    this.integer = 31,
    this.itemList = const [],
    this.itemMap = const {},
  });

  final String name;
  final String? category;
  final int integer;
  final List<String> itemList;
  final Map<String, String> itemMap;

  Map<String, dynamic> toJson() {
    return {
      'name': this.name,
      'category': this.category,
      'integer': this.integer,
      'itemList': this.itemList,
      'itemMap': this.itemMap,
    };
  }

  factory DictWithDefaults.fromJson(Map<String, dynamic> json) {
    return DictWithDefaults(
      name: json.containsKey('name') ? json['name'] as String : 'default-value',
      category: json.containsKey('category') ? json['category'] == null ? null : json['category'] as String : null,
      integer: json.containsKey('integer') ? (json['integer'] as num).toInt() : 31,
      itemList: json.containsKey('itemList') ? (json['itemList'] as List).map((item) => item as String).toList() : const [],
      itemMap: json.containsKey('itemMap') ? (json['itemMap'] as Map<String, dynamic>).map((key, value) => MapEntry(key, value as String)) : const {},
    );
  }

  DictWithDefaults copyWith({
    String? name,
    Object? category = _sentinel,
    int? integer,
    List<String>? itemList,
    Map<String, String>? itemMap,
  }) {
    return DictWithDefaults(
      name: name ?? this.name,
      category: category == _sentinel ? this.category : category as String?,
      integer: integer ?? this.integer,
      itemList: itemList ?? this.itemList,
      itemMap: itemMap ?? this.itemMap,
    );
  }

  @override
  String toString() {
    return 'DictWithDefaults(name: $name, category: $category, integer: $integer, itemList: $itemList, itemMap: $itemMap)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DictWithDefaults && name == other.name && category == other.category && integer == other.integer && itemList == other.itemList && itemMap == other.itemMap;

  @override
  int get hashCode => Object.hash(name, category, integer, itemList, itemMap);
}

/// Empty record — tests zero-field record generation.
class EmptyStruct {
  const EmptyStruct();


  Map<String, dynamic> toJson() {
    return {
    };
  }

  factory EmptyStruct.fromJson(Map<String, dynamic> json) {
    return EmptyStruct(
    );
  }

  @override
  String toString() {
    return 'EmptyStruct()';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is EmptyStruct;

  @override
  int get hashCode => runtimeType.hashCode;
}

/// Record with an object field — tests object-in-record via JSON codec.
class Repair {
  const Repair({
    required this.when_,
    required this.patch,
  });

  final DateTime when_;
  final Patch patch;

  Map<String, dynamic> toJson() {
    return {
      'when_': this.when_.toUtc().microsecondsSinceEpoch,
      'patch': PatchFfiCodec.lower(this.patch),
    };
  }

  factory Repair.fromJson(Map<String, dynamic> json) {
    return Repair(
      when_: DateTime.fromMicrosecondsSinceEpoch((json['when_'] as num).toInt(), isUtc: true),
      patch: PatchFfiCodec.lift((json['patch'] as num).toInt()),
    );
  }

  Repair copyWith({
    DateTime? when_,
    Patch? patch,
  }) {
    return Repair(
      when_: when_ ?? this.when_,
      patch: patch ?? this.patch,
    );
  }

  @override
  String toString() {
    return 'Repair(when_: $when_, patch: $patch)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Repair && when_ == other.when_ && patch == other.patch;

  @override
  int get hashCode => Object.hash(when_, patch);
}

/// Return-only record type with an error enum as a field.
class ReturnOnlyDict {
  const ReturnOnlyDict({
    required this.e,
  });

  final CoverallFlatError e;

  Map<String, dynamic> toJson() {
    return {
      'e': CoverallFlatErrorFfiCodec.encode(this.e),
    };
  }

  factory ReturnOnlyDict.fromJson(Map<String, dynamic> json) {
    return ReturnOnlyDict(
      e: CoverallFlatErrorFfiCodec.decode(json['e'] as String),
    );
  }

  ReturnOnlyDict copyWith({
    CoverallFlatError? e,
  }) {
    return ReturnOnlyDict(
      e: e ?? this.e,
    );
  }

  @override
  String toString() {
    return 'ReturnOnlyDict(e: $e)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ReturnOnlyDict && e == other.e;

  @override
  int get hashCode => e.hashCode;
}

/// Primary test record with a rich set of field types.
class SimpleDict {
  const SimpleDict({
    required this.text,
    required this.maybeCount,
    required this.flag,
    required this.color,
    required this.tags,
    required this.counts,
    required this.maybeText,
    required this.maybePatch,
    required this.maybeU8,
    required this.maybeU16,
    required this.maybeU64,
    required this.maybeI8,
    required this.maybeI64,
    required this.maybeF32,
    required this.maybeF64,
    required this.maybeBool,
    required this.maybeBytes,
    required this.someBytes,
    required this.coveralls,
  });

  final String text;
  final int maybeCount;
  final bool flag;
  final Color color;
  final List<String> tags;
  final Map<String, int> counts;
  final String? maybeText;
  final Patch? maybePatch;
  final int? maybeU8;
  final int? maybeU16;
  final int? maybeU64;
  final int? maybeI8;
  final int? maybeI64;
  final double? maybeF32;
  final double? maybeF64;
  final bool? maybeBool;
  final Uint8List? maybeBytes;
  final Uint8List someBytes;
  final Coveralls? coveralls;

  Map<String, dynamic> toJson() {
    return {
      'text': this.text,
      'maybeCount': this.maybeCount,
      'flag': this.flag,
      'color': ColorFfiCodec.encode(this.color),
      'tags': this.tags,
      'counts': this.counts,
      'maybeText': this.maybeText,
      'maybePatch': this.maybePatch == null ? null : (() { final __tmp = this.maybePatch!; return PatchFfiCodec.lower(__tmp); })(),
      'maybeU8': this.maybeU8,
      'maybeU16': this.maybeU16,
      'maybeU64': this.maybeU64,
      'maybeI8': this.maybeI8,
      'maybeI64': this.maybeI64,
      'maybeF32': this.maybeF32,
      'maybeF64': this.maybeF64,
      'maybeBool': this.maybeBool,
      'maybeBytes': this.maybeBytes == null ? null : (() { final __tmp = this.maybeBytes!; return base64Encode(__tmp); })(),
      'someBytes': base64Encode(this.someBytes),
      'coveralls': this.coveralls == null ? null : (() { final __tmp = this.coveralls!; return CoverallsFfiCodec.lower(__tmp); })(),
    };
  }

  factory SimpleDict.fromJson(Map<String, dynamic> json) {
    return SimpleDict(
      text: json['text'] as String,
      maybeCount: (json['maybeCount'] as num).toInt(),
      flag: json['flag'] as bool,
      color: ColorFfiCodec.decode(json['color'] as String),
      tags: (json['tags'] as List).map((item) => item as String).toList(),
      counts: (json['counts'] as Map<String, dynamic>).map((key, value) => MapEntry(key, (value as num).toInt())),
      maybeText: json['maybeText'] == null ? null : json['maybeText'] as String,
      maybePatch: json['maybePatch'] == null ? null : (() { final __tmp = json['maybePatch']; return PatchFfiCodec.lift((__tmp as num).toInt()); })(),
      maybeU8: json['maybeU8'] == null ? null : (json['maybeU8'] as num).toInt(),
      maybeU16: json['maybeU16'] == null ? null : (json['maybeU16'] as num).toInt(),
      maybeU64: json['maybeU64'] == null ? null : (json['maybeU64'] as num).toInt(),
      maybeI8: json['maybeI8'] == null ? null : (json['maybeI8'] as num).toInt(),
      maybeI64: json['maybeI64'] == null ? null : (json['maybeI64'] as num).toInt(),
      maybeF32: json['maybeF32'] == null ? null : (json['maybeF32'] as num).toDouble(),
      maybeF64: json['maybeF64'] == null ? null : (json['maybeF64'] as num).toDouble(),
      maybeBool: json['maybeBool'] == null ? null : json['maybeBool'] as bool,
      maybeBytes: json['maybeBytes'] == null ? null : (() { final __tmp = json['maybeBytes']; return base64Decode(__tmp as String); })(),
      someBytes: base64Decode(json['someBytes'] as String),
      coveralls: json['coveralls'] == null ? null : (() { final __tmp = json['coveralls']; return CoverallsFfiCodec.lift((__tmp as num).toInt()); })(),
    );
  }

  SimpleDict copyWith({
    String? text,
    int? maybeCount,
    bool? flag,
    Color? color,
    List<String>? tags,
    Map<String, int>? counts,
    Object? maybeText = _sentinel,
    Object? maybePatch = _sentinel,
    Object? maybeU8 = _sentinel,
    Object? maybeU16 = _sentinel,
    Object? maybeU64 = _sentinel,
    Object? maybeI8 = _sentinel,
    Object? maybeI64 = _sentinel,
    Object? maybeF32 = _sentinel,
    Object? maybeF64 = _sentinel,
    Object? maybeBool = _sentinel,
    Object? maybeBytes = _sentinel,
    Uint8List? someBytes,
    Object? coveralls = _sentinel,
  }) {
    return SimpleDict(
      text: text ?? this.text,
      maybeCount: maybeCount ?? this.maybeCount,
      flag: flag ?? this.flag,
      color: color ?? this.color,
      tags: tags ?? this.tags,
      counts: counts ?? this.counts,
      maybeText: maybeText == _sentinel ? this.maybeText : maybeText as String?,
      maybePatch: maybePatch == _sentinel ? this.maybePatch : maybePatch as Patch?,
      maybeU8: maybeU8 == _sentinel ? this.maybeU8 : maybeU8 as int?,
      maybeU16: maybeU16 == _sentinel ? this.maybeU16 : maybeU16 as int?,
      maybeU64: maybeU64 == _sentinel ? this.maybeU64 : maybeU64 as int?,
      maybeI8: maybeI8 == _sentinel ? this.maybeI8 : maybeI8 as int?,
      maybeI64: maybeI64 == _sentinel ? this.maybeI64 : maybeI64 as int?,
      maybeF32: maybeF32 == _sentinel ? this.maybeF32 : maybeF32 as double?,
      maybeF64: maybeF64 == _sentinel ? this.maybeF64 : maybeF64 as double?,
      maybeBool: maybeBool == _sentinel ? this.maybeBool : maybeBool as bool?,
      maybeBytes: maybeBytes == _sentinel ? this.maybeBytes : maybeBytes as Uint8List?,
      someBytes: someBytes ?? this.someBytes,
      coveralls: coveralls == _sentinel ? this.coveralls : coveralls as Coveralls?,
    );
  }

  @override
  String toString() {
    return 'SimpleDict(text: $text, maybeCount: $maybeCount, flag: $flag, color: $color, tags: $tags, counts: $counts, maybeText: $maybeText, maybePatch: $maybePatch, maybeU8: $maybeU8, maybeU16: $maybeU16, maybeU64: $maybeU64, maybeI8: $maybeI8, maybeI64: $maybeI64, maybeF32: $maybeF32, maybeF64: $maybeF64, maybeBool: $maybeBool, maybeBytes: $maybeBytes, someBytes: $someBytes, coveralls: $coveralls)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SimpleDict && text == other.text && maybeCount == other.maybeCount && flag == other.flag && color == other.color && tags == other.tags && counts == other.counts && maybeText == other.maybeText && maybePatch == other.maybePatch && maybeU8 == other.maybeU8 && maybeU16 == other.maybeU16 && maybeU64 == other.maybeU64 && maybeI8 == other.maybeI8 && maybeI64 == other.maybeI64 && maybeF32 == other.maybeF32 && maybeF64 == other.maybeF64 && maybeBool == other.maybeBool && maybeBytes == other.maybeBytes && someBytes == other.someBytes && coveralls == other.coveralls;

  @override
  int get hashCode => Object.hash(text, maybeCount, flag, color, tags, counts, maybeText, maybePatch, maybeU8, maybeU16, maybeU64, maybeI8, maybeI64, maybeF32, maybeF64, maybeBool, maybeBytes, someBytes, coveralls);
}

/// Simple three-variant flat enum.
enum Color {
  red,
  blue,
  green,
}

/// Rich error with structured variant data.
sealed class ComplexError {
  const ComplexError();
}

final class ComplexErrorOsError extends ComplexError {
  const ComplexErrorOsError({
    required this.code,
    required this.extendedCode,
  });
  final int code;
  final int extendedCode;

  @override
  String toString() {
    return 'ComplexErrorOsError(code: $code, extendedCode: $extendedCode)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ComplexErrorOsError && code == other.code && extendedCode == other.extendedCode;

  @override
  int get hashCode => Object.hash(code, extendedCode);
}

final class ComplexErrorPermissionDenied extends ComplexError {
  const ComplexErrorPermissionDenied({
    required this.reason,
  });
  final String reason;

  @override
  String toString() {
    return 'ComplexErrorPermissionDenied(reason: $reason)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ComplexErrorPermissionDenied && reason == other.reason;

  @override
  int get hashCode => reason.hashCode;
}

final class ComplexErrorUnknownError extends ComplexError {
  const ComplexErrorUnknownError();

  @override
  String toString() {
    return 'ComplexErrorUnknownError()';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ComplexErrorUnknownError;

  @override
  int get hashCode => runtimeType.hashCode;
}

/// Simple flat error (single tag discriminator).
sealed class CoverallError {
  const CoverallError();
}

final class CoverallErrorTooManyHoles extends CoverallError {
  const CoverallErrorTooManyHoles();

  @override
  String toString() {
    return 'CoverallErrorTooManyHoles()';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CoverallErrorTooManyHoles;

  @override
  int get hashCode => runtimeType.hashCode;
}

/// Flat error with case-testing variant names.
sealed class CoverallFlatError {
  const CoverallFlatError();
}

final class CoverallFlatErrorTooManyVariants extends CoverallFlatError {
  const CoverallFlatErrorTooManyVariants();

  @override
  String toString() {
    return 'CoverallFlatErrorTooManyVariants()';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CoverallFlatErrorTooManyVariants;

  @override
  int get hashCode => runtimeType.hashCode;
}

/// Flat error with non-standard casing.
sealed class HTMLError {
  const HTMLError();
}

final class HTMLErrorInvalidHTML extends HTMLError {
  const HTMLErrorInvalidHTML();

  @override
  String toString() {
    return 'HTMLErrorInvalidHTML()';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is HTMLErrorInvalidHTML;

  @override
  int get hashCode => runtimeType.hashCode;
}

/// Enum wrapping an object — tests object in data enum variant.
sealed class MaybeObject {
  const MaybeObject();
}

final class MaybeObjectObj extends MaybeObject {
  const MaybeObjectObj({
    required this.p,
  });
  final Patch p;

  @override
  String toString() {
    return 'MaybeObjectObj(p: $p)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MaybeObjectObj && p == other.p;

  @override
  int get hashCode => p.hashCode;
}

final class MaybeObjectNah extends MaybeObject {
  const MaybeObjectNah();

  @override
  String toString() {
    return 'MaybeObjectNah()';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MaybeObjectNah;

  @override
  int get hashCode => runtimeType.hashCode;
}

/// Enum wrapping a record — tests enum-with-associated-data.
sealed class MaybeSimpleDict {
  const MaybeSimpleDict();
}

final class MaybeSimpleDictYeah extends MaybeSimpleDict {
  const MaybeSimpleDictYeah({
    required this.value,
  });
  final SimpleDict value;

  @override
  String toString() {
    return 'MaybeSimpleDictYeah(value: $value)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MaybeSimpleDictYeah && value == other.value;

  @override
  int get hashCode => value.hashCode;
}

final class MaybeSimpleDictNah extends MaybeSimpleDict {
  const MaybeSimpleDictNah();

  @override
  String toString() {
    return 'MaybeSimpleDictNah()';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MaybeSimpleDictNah;

  @override
  int get hashCode => runtimeType.hashCode;
}

/// Return-only enum with nested error and record types.
sealed class ReturnOnlyEnum {
  const ReturnOnlyEnum();
}

final class ReturnOnlyEnumOne extends ReturnOnlyEnum {
  const ReturnOnlyEnumOne({
    required this.e,
  });
  final CoverallFlatError e;

  @override
  String toString() {
    return 'ReturnOnlyEnumOne(e: $e)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ReturnOnlyEnumOne && e == other.e;

  @override
  int get hashCode => e.hashCode;
}

final class ReturnOnlyEnumTwo extends ReturnOnlyEnum {
  const ReturnOnlyEnumTwo({
    required this.d,
  });
  final ReturnOnlyDict d;

  @override
  String toString() {
    return 'ReturnOnlyEnumTwo(d: $d)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ReturnOnlyEnumTwo && d == other.d;

  @override
  int get hashCode => d.hashCode;
}

final class ReturnOnlyEnumThree extends ReturnOnlyEnum {
  const ReturnOnlyEnumThree({
    required this.l,
  });
  final List<CoverallFlatError> l;

  @override
  String toString() {
    return 'ReturnOnlyEnumThree(l: $l)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ReturnOnlyEnumThree && l == other.l;

  @override
  int get hashCode => l.hashCode;
}

final class ReturnOnlyEnumFour extends ReturnOnlyEnum {
  const ReturnOnlyEnumFour({
    required this.m,
  });
  final Map<String, CoverallFlatError> m;

  @override
  String toString() {
    return 'ReturnOnlyEnumFour(m: $m)';
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ReturnOnlyEnumFour && m == other.m;

  @override
  int get hashCode => m.hashCode;
}

/// Rich error with structured variant data.
sealed class ComplexErrorException implements Exception {
  const ComplexErrorException();
}

final class ComplexErrorExceptionOsError extends ComplexErrorException {
  const ComplexErrorExceptionOsError({
    required this.code,
    required this.extendedCode,
  });
  final int code;
  final int extendedCode;

  @override
  String toString() {
    return 'ComplexErrorExceptionOsError(code: $code, extendedCode: $extendedCode)';
  }
}

final class ComplexErrorExceptionPermissionDenied extends ComplexErrorException {
  const ComplexErrorExceptionPermissionDenied({
    required this.reason,
  });
  final String reason;

  @override
  String toString() {
    return 'ComplexErrorExceptionPermissionDenied(reason: $reason)';
  }
}

final class ComplexErrorExceptionUnknownError extends ComplexErrorException {
  const ComplexErrorExceptionUnknownError();

  @override
  String toString() {
    return 'ComplexErrorExceptionUnknownError()';
  }
}

/// Simple flat error (single tag discriminator).
sealed class CoverallErrorException implements Exception {
  const CoverallErrorException();
}

final class CoverallErrorExceptionTooManyHoles extends CoverallErrorException {
  const CoverallErrorExceptionTooManyHoles();

  @override
  String toString() {
    return 'CoverallErrorExceptionTooManyHoles()';
  }
}

/// Flat error with case-testing variant names.
sealed class CoverallFlatErrorException implements Exception {
  const CoverallFlatErrorException();
}

final class CoverallFlatErrorExceptionTooManyVariants extends CoverallFlatErrorException {
  const CoverallFlatErrorExceptionTooManyVariants();

  @override
  String toString() {
    return 'CoverallFlatErrorExceptionTooManyVariants()';
  }
}

/// Flat error with non-standard casing.
sealed class HTMLErrorException implements Exception {
  const HTMLErrorException();
}

final class HTMLErrorExceptionInvalidHTML extends HTMLErrorException {
  const HTMLErrorExceptionInvalidHTML();

  @override
  String toString() {
    return 'HTMLErrorExceptionInvalidHTML()';
  }
}

String _encodeColor(Color value) {
  return switch (value) {
    Color.red => 'red',
    Color.blue => 'blue',
    Color.green => 'green',
  };
}

Color _decodeColor(String raw) {
  return switch (raw) {
    'red' => Color.red,
    'blue' => Color.blue,
    'green' => Color.green,
    _ => throw StateError('Unknown Color variant: $raw'),
  };
}

String _encodeComplexError(ComplexError value) {
  if (value is ComplexErrorOsError) {
    return jsonEncode({
      'tag': 'osError',
      'code': value.code,
      'extendedCode': value.extendedCode,
    });
  }
  if (value is ComplexErrorPermissionDenied) {
    return jsonEncode({
      'tag': 'permissionDenied',
      'reason': value.reason,
    });
  }
  if (value is ComplexErrorUnknownError) {
    return jsonEncode({
      'tag': 'unknownError',
    });
  }
  throw StateError('Unknown ComplexError variant instance: $value');
}

ComplexError _decodeComplexError(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'osError':
      return ComplexErrorOsError(
        code: (map['code'] as num).toInt(),
        extendedCode: (map['extendedCode'] as num).toInt(),
      );
    case 'permissionDenied':
      return ComplexErrorPermissionDenied(
        reason: map['reason'] as String,
      );
    case 'unknownError':
      return ComplexErrorUnknownError(
      );
    default:
      throw StateError('Unknown ComplexError variant tag: $tag');
  }
}

String _encodeCoverallError(CoverallError value) {
  if (value is CoverallErrorTooManyHoles) {
    return jsonEncode({
      'tag': 'tooManyHoles',
    });
  }
  throw StateError('Unknown CoverallError variant instance: $value');
}

CoverallError _decodeCoverallError(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'tooManyHoles':
      return CoverallErrorTooManyHoles(
      );
    default:
      throw StateError('Unknown CoverallError variant tag: $tag');
  }
}

String _encodeCoverallFlatError(CoverallFlatError value) {
  if (value is CoverallFlatErrorTooManyVariants) {
    return jsonEncode({
      'tag': 'tooManyVariants',
    });
  }
  throw StateError('Unknown CoverallFlatError variant instance: $value');
}

CoverallFlatError _decodeCoverallFlatError(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'tooManyVariants':
      return CoverallFlatErrorTooManyVariants(
      );
    default:
      throw StateError('Unknown CoverallFlatError variant tag: $tag');
  }
}

String _encodeHTMLError(HTMLError value) {
  if (value is HTMLErrorInvalidHTML) {
    return jsonEncode({
      'tag': 'invalidHTML',
    });
  }
  throw StateError('Unknown HTMLError variant instance: $value');
}

HTMLError _decodeHTMLError(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'invalidHTML':
      return HTMLErrorInvalidHTML(
      );
    default:
      throw StateError('Unknown HTMLError variant tag: $tag');
  }
}

String _encodeMaybeObject(MaybeObject value) {
  if (value is MaybeObjectObj) {
    return jsonEncode({
      'tag': 'obj',
      'p': PatchFfiCodec.lower(value.p),
    });
  }
  if (value is MaybeObjectNah) {
    return jsonEncode({
      'tag': 'nah',
    });
  }
  throw StateError('Unknown MaybeObject variant instance: $value');
}

MaybeObject _decodeMaybeObject(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'obj':
      return MaybeObjectObj(
        p: PatchFfiCodec.lift((map['p'] as num).toInt()),
      );
    case 'nah':
      return MaybeObjectNah(
      );
    default:
      throw StateError('Unknown MaybeObject variant tag: $tag');
  }
}

String _encodeMaybeSimpleDict(MaybeSimpleDict value) {
  if (value is MaybeSimpleDictYeah) {
    return jsonEncode({
      'tag': 'yeah',
      'value': value.value.toJson(),
    });
  }
  if (value is MaybeSimpleDictNah) {
    return jsonEncode({
      'tag': 'nah',
    });
  }
  throw StateError('Unknown MaybeSimpleDict variant instance: $value');
}

MaybeSimpleDict _decodeMaybeSimpleDict(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'yeah':
      return MaybeSimpleDictYeah(
        value: SimpleDict.fromJson(map['value'] as Map<String, dynamic>),
      );
    case 'nah':
      return MaybeSimpleDictNah(
      );
    default:
      throw StateError('Unknown MaybeSimpleDict variant tag: $tag');
  }
}

String _encodeReturnOnlyEnum(ReturnOnlyEnum value) {
  if (value is ReturnOnlyEnumOne) {
    return jsonEncode({
      'tag': 'one',
      'e': CoverallFlatErrorFfiCodec.encode(value.e),
    });
  }
  if (value is ReturnOnlyEnumTwo) {
    return jsonEncode({
      'tag': 'two',
      'd': value.d.toJson(),
    });
  }
  if (value is ReturnOnlyEnumThree) {
    return jsonEncode({
      'tag': 'three',
      'l': value.l.map((item) => CoverallFlatErrorFfiCodec.encode(item)).toList(),
    });
  }
  if (value is ReturnOnlyEnumFour) {
    return jsonEncode({
      'tag': 'four',
      'm': value.m.map((key, value) => MapEntry(key, CoverallFlatErrorFfiCodec.encode(value))),
    });
  }
  throw StateError('Unknown ReturnOnlyEnum variant instance: $value');
}

ReturnOnlyEnum _decodeReturnOnlyEnum(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'one':
      return ReturnOnlyEnumOne(
        e: CoverallFlatErrorFfiCodec.decode(map['e'] as String),
      );
    case 'two':
      return ReturnOnlyEnumTwo(
        d: ReturnOnlyDict.fromJson(map['d'] as Map<String, dynamic>),
      );
    case 'three':
      return ReturnOnlyEnumThree(
        l: (map['l'] as List).map((item) => CoverallFlatErrorFfiCodec.decode(item as String)).toList(),
      );
    case 'four':
      return ReturnOnlyEnumFour(
        m: (map['m'] as Map<String, dynamic>).map((key, value) => MapEntry(key, CoverallFlatErrorFfiCodec.decode(value as String))),
      );
    default:
      throw StateError('Unknown ReturnOnlyEnum variant tag: $tag');
  }
}

String _encodeComplexErrorException(ComplexErrorException value) {
  if (value is ComplexErrorExceptionOsError) {
    return jsonEncode({
      'tag': 'osError',
      'code': value.code,
      'extendedCode': value.extendedCode,
    });
  }
  if (value is ComplexErrorExceptionPermissionDenied) {
    return jsonEncode({
      'tag': 'permissionDenied',
      'reason': value.reason,
    });
  }
  if (value is ComplexErrorExceptionUnknownError) {
    return jsonEncode({
      'tag': 'unknownError',
    });
  }
  throw StateError('Unknown ComplexErrorException exception instance: $value');
}

ComplexErrorException _decodeComplexErrorException(Object? raw) {
  final Map<String, dynamic> map = raw is String ? (jsonDecode(raw) as Map<String, dynamic>) : (raw as Map<String, dynamic>);
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'osError':
      return ComplexErrorExceptionOsError(
        code: (map['code'] as num).toInt(),
        extendedCode: (map['extendedCode'] as num).toInt(),
      );
    case 'permissionDenied':
      return ComplexErrorExceptionPermissionDenied(
        reason: map['reason'] as String,
      );
    case 'unknownError':
      return const ComplexErrorExceptionUnknownError();
    default:
      throw StateError('Unknown ComplexErrorException exception tag: $tag');
  }
}

String _encodeCoverallErrorException(CoverallErrorException value) {
  if (value is CoverallErrorExceptionTooManyHoles) {
    return jsonEncode({
      'tag': 'tooManyHoles',
    });
  }
  throw StateError('Unknown CoverallErrorException exception instance: $value');
}

CoverallErrorException _decodeCoverallErrorException(Object? raw) {
  final Map<String, dynamic> map = raw is String ? (jsonDecode(raw) as Map<String, dynamic>) : (raw as Map<String, dynamic>);
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'tooManyHoles':
      return const CoverallErrorExceptionTooManyHoles();
    default:
      throw StateError('Unknown CoverallErrorException exception tag: $tag');
  }
}

String _encodeCoverallFlatErrorException(CoverallFlatErrorException value) {
  if (value is CoverallFlatErrorExceptionTooManyVariants) {
    return jsonEncode({
      'tag': 'tooManyVariants',
    });
  }
  throw StateError('Unknown CoverallFlatErrorException exception instance: $value');
}

CoverallFlatErrorException _decodeCoverallFlatErrorException(Object? raw) {
  final Map<String, dynamic> map = raw is String ? (jsonDecode(raw) as Map<String, dynamic>) : (raw as Map<String, dynamic>);
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'tooManyVariants':
      return const CoverallFlatErrorExceptionTooManyVariants();
    default:
      throw StateError('Unknown CoverallFlatErrorException exception tag: $tag');
  }
}

String _encodeHTMLErrorException(HTMLErrorException value) {
  if (value is HTMLErrorExceptionInvalidHTML) {
    return jsonEncode({
      'tag': 'invalidHTML',
    });
  }
  throw StateError('Unknown HTMLErrorException exception instance: $value');
}

HTMLErrorException _decodeHTMLErrorException(Object? raw) {
  final Map<String, dynamic> map = raw is String ? (jsonDecode(raw) as Map<String, dynamic>) : (raw as Map<String, dynamic>);
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'invalidHTML':
      return const HTMLErrorExceptionInvalidHTML();
    default:
      throw StateError('Unknown HTMLErrorException exception tag: $tag');
  }
}

final class ColorFfiCodec {
  const ColorFfiCodec._();

  static String encode(Color value) => _encodeColor(value);

  static Color decode(String raw) => _decodeColor(raw);
}

final class ComplexErrorFfiCodec {
  const ComplexErrorFfiCodec._();

  static String encode(ComplexError value) => _encodeComplexError(value);

  static ComplexError decode(String raw) => _decodeComplexError(raw);
}

final class CoverallErrorFfiCodec {
  const CoverallErrorFfiCodec._();

  static String encode(CoverallError value) => _encodeCoverallError(value);

  static CoverallError decode(String raw) => _decodeCoverallError(raw);
}

final class CoverallFlatErrorFfiCodec {
  const CoverallFlatErrorFfiCodec._();

  static String encode(CoverallFlatError value) => _encodeCoverallFlatError(value);

  static CoverallFlatError decode(String raw) => _decodeCoverallFlatError(raw);
}

final class HTMLErrorFfiCodec {
  const HTMLErrorFfiCodec._();

  static String encode(HTMLError value) => _encodeHTMLError(value);

  static HTMLError decode(String raw) => _decodeHTMLError(raw);
}

final class MaybeObjectFfiCodec {
  const MaybeObjectFfiCodec._();

  static String encode(MaybeObject value) => _encodeMaybeObject(value);

  static MaybeObject decode(String raw) => _decodeMaybeObject(raw);
}

final class MaybeSimpleDictFfiCodec {
  const MaybeSimpleDictFfiCodec._();

  static String encode(MaybeSimpleDict value) => _encodeMaybeSimpleDict(value);

  static MaybeSimpleDict decode(String raw) => _decodeMaybeSimpleDict(raw);
}

final class ReturnOnlyEnumFfiCodec {
  const ReturnOnlyEnumFfiCodec._();

  static String encode(ReturnOnlyEnum value) => _encodeReturnOnlyEnum(value);

  static ReturnOnlyEnum decode(String raw) => _decodeReturnOnlyEnum(raw);
}

final class ComplexErrorExceptionFfiCodec {
  const ComplexErrorExceptionFfiCodec._();

  static String encode(ComplexErrorException value) => _encodeComplexErrorException(value);

  static ComplexErrorException decode(Object? raw) => _decodeComplexErrorException(raw);
}

final class CoverallErrorExceptionFfiCodec {
  const CoverallErrorExceptionFfiCodec._();

  static String encode(CoverallErrorException value) => _encodeCoverallErrorException(value);

  static CoverallErrorException decode(Object? raw) => _decodeCoverallErrorException(raw);
}

final class CoverallFlatErrorExceptionFfiCodec {
  const CoverallFlatErrorExceptionFfiCodec._();

  static String encode(CoverallFlatErrorException value) => _encodeCoverallFlatErrorException(value);

  static CoverallFlatErrorException decode(Object? raw) => _decodeCoverallFlatErrorException(raw);
}

final class HTMLErrorExceptionFfiCodec {
  const HTMLErrorExceptionFfiCodec._();

  static String encode(HTMLErrorException value) => _encodeHTMLErrorException(value);

  static HTMLErrorException decode(Object? raw) => _decodeHTMLErrorException(raw);
}

final class _UniFfiBinaryWriter {
  final BytesBuilder _builder = BytesBuilder(copy: false);

  void writeU8(int value) => _builder.add([value & 0xFF]);
  void writeI8(int value) => _builder.add([(value) & 0xFF]);
  void writeU16(int value) {
    final data = ByteData(2)..setUint16(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeI16(int value) {
    final data = ByteData(2)..setInt16(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeU32(int value) {
    final data = ByteData(4)..setUint32(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeI32(int value) {
    final data = ByteData(4)..setInt32(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeU64(int value) {
    final data = ByteData(8)..setUint64(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeI64(int value) {
    final data = ByteData(8)..setInt64(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeF32(double value) {
    final data = ByteData(4)..setFloat32(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeF64(double value) {
    final data = ByteData(8)..setFloat64(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeBool(bool value) => writeI8(value ? 1 : 0);
  void writeBytes(Uint8List bytes) => _builder.add(bytes);
  void writeString(String value) {
    final bytes = Uint8List.fromList(utf8.encode(value));
    writeI32(bytes.length);
    writeBytes(bytes);
  }

  Uint8List toBytes() => _builder.takeBytes();
}

final class _UniFfiBinaryReader {
  _UniFfiBinaryReader(this._bytes);
  final Uint8List _bytes;
  int _offset = 0;

  bool get isDone => _offset == _bytes.length;

  ByteData _readData(int len) {
    if (_offset + len > _bytes.length) {
      throw StateError('buffer underflow while decoding UniFFI payload');
    }
    final data = ByteData.sublistView(_bytes, _offset, _offset + len);
    _offset += len;
    return data;
  }

  int readU8() => _readData(1).getUint8(0);
  int readI8() => _readData(1).getInt8(0);
  int readU16() => _readData(2).getUint16(0, Endian.big);
  int readI16() => _readData(2).getInt16(0, Endian.big);
  int readU32() => _readData(4).getUint32(0, Endian.big);
  int readI32() => _readData(4).getInt32(0, Endian.big);
  int readU64() => _readData(8).getUint64(0, Endian.big);
  int readI64() => _readData(8).getInt64(0, Endian.big);
  double readF32() => _readData(4).getFloat32(0, Endian.big);
  double readF64() => _readData(8).getFloat64(0, Endian.big);
  bool readBool() {
    final value = readI8();
    if (value == 0) return false;
    if (value == 1) return true;
    throw StateError('invalid boolean payload value: $value');
  }
  Uint8List readBytes(int len) {
    if (_offset + len > _bytes.length) {
      throw StateError('buffer underflow while decoding UniFFI payload bytes');
    }
    final out = Uint8List.fromList(_bytes.sublist(_offset, _offset + len));
    _offset += len;
    return out;
  }
  String readString() {
    final len = readI32();
    if (len < 0) {
      throw StateError('invalid string length in UniFFI payload: $len');
    }
    return utf8.decode(readBytes(len));
  }
}

void _uniffiWriteDictWithDefaults(DictWithDefaults value, _UniFfiBinaryWriter writer) {
  writer.writeString(value.name);
  if (value.category == null) {
    writer.writeI8(0);
  } else {
    writer.writeI8(1);
    writer.writeString(value.category!);
  }
  writer.writeI64(value.integer);
  writer.writeI32(value.itemList.length);
  for (final item in value.itemList) {
    writer.writeString(item);
  }
  writer.writeI32(value.itemMap.length);
  for (final entry in value.itemMap.entries) {
    writer.writeString(entry.key);
    writer.writeString(entry.value);
  }
}

Uint8List _uniffiEncodeDictWithDefaults(DictWithDefaults value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteDictWithDefaults(value, writer);
  return writer.toBytes();
}

DictWithDefaults _uniffiReadDictWithDefaults(_UniFfiBinaryReader reader) {
  return DictWithDefaults(
    name: reader.readString(),
    category: (() { final int __tag = reader.readI8(); if (__tag == 0) return null; if (__tag != 1) throw StateError('invalid optional tag: $__tag'); return reader.readString(); })(),
    integer: reader.readI64(),
    itemList: (() { final int __len = reader.readI32(); final out = <String>[]; for (var i = 0; i < __len; i++) { out.add(reader.readString()); } return out; })(),
    itemMap: (() { final int __len = reader.readI32(); final out = <String, String>{}; for (var i = 0; i < __len; i++) { final key = reader.readString(); final value = reader.readString(); out[key] = value; } return out; })(),
  );
}

DictWithDefaults _uniffiDecodeDictWithDefaults(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadDictWithDefaults(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding DictWithDefaults');
  }
  return value;
}

void _uniffiWriteEmptyStruct(EmptyStruct value, _UniFfiBinaryWriter writer) {
}

Uint8List _uniffiEncodeEmptyStruct(EmptyStruct value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteEmptyStruct(value, writer);
  return writer.toBytes();
}

EmptyStruct _uniffiReadEmptyStruct(_UniFfiBinaryReader reader) {
  return EmptyStruct(
  );
}

EmptyStruct _uniffiDecodeEmptyStruct(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadEmptyStruct(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding EmptyStruct');
  }
  return value;
}

void _uniffiWriteRepair(Repair value, _UniFfiBinaryWriter writer) {
  throw UnsupportedError('UniFFI binary encode not fully supported for Repair');
}

Uint8List _uniffiEncodeRepair(Repair value) {
  throw UnsupportedError('UniFFI binary encode not fully supported for Repair');
}

Repair _uniffiReadRepair(_UniFfiBinaryReader reader) {
  throw UnsupportedError('UniFFI binary decode not fully supported for Repair');
}

Repair _uniffiDecodeRepair(Uint8List bytes) {
  throw UnsupportedError('UniFFI binary decode not fully supported for Repair');
}

void _uniffiWriteReturnOnlyDict(ReturnOnlyDict value, _UniFfiBinaryWriter writer) {
  _uniffiWriteCoverallFlatError(value.e, writer);
}

Uint8List _uniffiEncodeReturnOnlyDict(ReturnOnlyDict value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteReturnOnlyDict(value, writer);
  return writer.toBytes();
}

ReturnOnlyDict _uniffiReadReturnOnlyDict(_UniFfiBinaryReader reader) {
  return ReturnOnlyDict(
    e: _uniffiReadCoverallFlatError(reader),
  );
}

ReturnOnlyDict _uniffiDecodeReturnOnlyDict(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadReturnOnlyDict(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding ReturnOnlyDict');
  }
  return value;
}

void _uniffiWriteSimpleDict(SimpleDict value, _UniFfiBinaryWriter writer) {
  throw UnsupportedError('UniFFI binary encode not fully supported for SimpleDict');
}

Uint8List _uniffiEncodeSimpleDict(SimpleDict value) {
  throw UnsupportedError('UniFFI binary encode not fully supported for SimpleDict');
}

SimpleDict _uniffiReadSimpleDict(_UniFfiBinaryReader reader) {
  throw UnsupportedError('UniFFI binary decode not fully supported for SimpleDict');
}

SimpleDict _uniffiDecodeSimpleDict(Uint8List bytes) {
  throw UnsupportedError('UniFFI binary decode not fully supported for SimpleDict');
}

void _uniffiWriteColor(Color value, _UniFfiBinaryWriter writer) {
  final int tag = switch (value) {
    Color.red => 1,
    Color.blue => 2,
    Color.green => 3,
  };
  writer.writeI32(tag);
}

Uint8List _uniffiEncodeColor(Color value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteColor(value, writer);
  return writer.toBytes();
}

Color _uniffiReadColor(_UniFfiBinaryReader reader) {
  final int tag = reader.readI32();
  switch (tag) {
    case 1:
      return Color.red;
    case 2:
      return Color.blue;
    case 3:
      return Color.green;
    default:
      throw StateError('Unknown Color variant tag: $tag');
  }
}

Color _uniffiDecodeColor(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadColor(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding Color');
  }
  return value;
}

void _uniffiWriteComplexError(ComplexError value, _UniFfiBinaryWriter writer) {
  if (value is ComplexErrorOsError) {
    writer.writeI32(1);
    writer.writeI16(value.code);
    writer.writeI16(value.extendedCode);
  }
  else if (value is ComplexErrorPermissionDenied) {
    writer.writeI32(2);
    writer.writeString(value.reason);
  }
  else if (value is ComplexErrorUnknownError) {
    writer.writeI32(3);
  }
  else {
    throw StateError('Unknown ComplexError variant instance: $value');
  }
}

Uint8List _uniffiEncodeComplexError(ComplexError value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteComplexError(value, writer);
  return writer.toBytes();
}

ComplexError _uniffiReadComplexError(_UniFfiBinaryReader reader) {
  final int tag = reader.readI32();
  switch (tag) {
    case 1:
      return ComplexErrorOsError(
        code: reader.readI16(),
        extendedCode: reader.readI16(),
      );
    case 2:
      return ComplexErrorPermissionDenied(
        reason: reader.readString(),
      );
    case 3:
      return const ComplexErrorUnknownError();
    default:
      throw StateError('Unknown ComplexError variant tag: $tag');
  }
}

ComplexError _uniffiDecodeComplexError(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadComplexError(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding ComplexError');
  }
  return value;
}

void _uniffiWriteCoverallError(CoverallError value, _UniFfiBinaryWriter writer) {
  if (value is CoverallErrorTooManyHoles) {
    writer.writeI32(1);
  }
  else {
    throw StateError('Unknown CoverallError variant instance: $value');
  }
}

Uint8List _uniffiEncodeCoverallError(CoverallError value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteCoverallError(value, writer);
  return writer.toBytes();
}

CoverallError _uniffiReadCoverallError(_UniFfiBinaryReader reader) {
  final int tag = reader.readI32();
  switch (tag) {
    case 1:
      return const CoverallErrorTooManyHoles();
    default:
      throw StateError('Unknown CoverallError variant tag: $tag');
  }
}

CoverallError _uniffiDecodeCoverallError(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadCoverallError(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding CoverallError');
  }
  return value;
}

void _uniffiWriteCoverallFlatError(CoverallFlatError value, _UniFfiBinaryWriter writer) {
  if (value is CoverallFlatErrorTooManyVariants) {
    writer.writeI32(1);
  }
  else {
    throw StateError('Unknown CoverallFlatError variant instance: $value');
  }
}

Uint8List _uniffiEncodeCoverallFlatError(CoverallFlatError value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteCoverallFlatError(value, writer);
  return writer.toBytes();
}

CoverallFlatError _uniffiReadCoverallFlatError(_UniFfiBinaryReader reader) {
  final int tag = reader.readI32();
  switch (tag) {
    case 1:
      return const CoverallFlatErrorTooManyVariants();
    default:
      throw StateError('Unknown CoverallFlatError variant tag: $tag');
  }
}

CoverallFlatError _uniffiDecodeCoverallFlatError(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadCoverallFlatError(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding CoverallFlatError');
  }
  return value;
}

void _uniffiWriteHTMLError(HTMLError value, _UniFfiBinaryWriter writer) {
  if (value is HTMLErrorInvalidHTML) {
    writer.writeI32(1);
  }
  else {
    throw StateError('Unknown HTMLError variant instance: $value');
  }
}

Uint8List _uniffiEncodeHTMLError(HTMLError value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteHTMLError(value, writer);
  return writer.toBytes();
}

HTMLError _uniffiReadHTMLError(_UniFfiBinaryReader reader) {
  final int tag = reader.readI32();
  switch (tag) {
    case 1:
      return const HTMLErrorInvalidHTML();
    default:
      throw StateError('Unknown HTMLError variant tag: $tag');
  }
}

HTMLError _uniffiDecodeHTMLError(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadHTMLError(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding HTMLError');
  }
  return value;
}

Uint8List _uniffiEncodeMaybeObject(MaybeObject value) {
  throw UnsupportedError('UniFFI binary encode not fully supported for MaybeObject');
}

MaybeObject _uniffiDecodeMaybeObject(Uint8List bytes) {
  throw UnsupportedError('UniFFI binary decode not fully supported for MaybeObject');
}

void _uniffiWriteMaybeSimpleDict(MaybeSimpleDict value, _UniFfiBinaryWriter writer) {
  if (value is MaybeSimpleDictYeah) {
    writer.writeI32(1);
    _uniffiWriteSimpleDict(value.value, writer);
  }
  else if (value is MaybeSimpleDictNah) {
    writer.writeI32(2);
  }
  else {
    throw StateError('Unknown MaybeSimpleDict variant instance: $value');
  }
}

Uint8List _uniffiEncodeMaybeSimpleDict(MaybeSimpleDict value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteMaybeSimpleDict(value, writer);
  return writer.toBytes();
}

MaybeSimpleDict _uniffiReadMaybeSimpleDict(_UniFfiBinaryReader reader) {
  final int tag = reader.readI32();
  switch (tag) {
    case 1:
      return MaybeSimpleDictYeah(
        value: _uniffiReadSimpleDict(reader),
      );
    case 2:
      return const MaybeSimpleDictNah();
    default:
      throw StateError('Unknown MaybeSimpleDict variant tag: $tag');
  }
}

MaybeSimpleDict _uniffiDecodeMaybeSimpleDict(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadMaybeSimpleDict(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding MaybeSimpleDict');
  }
  return value;
}

void _uniffiWriteReturnOnlyEnum(ReturnOnlyEnum value, _UniFfiBinaryWriter writer) {
  if (value is ReturnOnlyEnumOne) {
    writer.writeI32(1);
    _uniffiWriteCoverallFlatError(value.e, writer);
  }
  else if (value is ReturnOnlyEnumTwo) {
    writer.writeI32(2);
    _uniffiWriteReturnOnlyDict(value.d, writer);
  }
  else if (value is ReturnOnlyEnumThree) {
    writer.writeI32(3);
    writer.writeI32(value.l.length);
    for (final item in value.l) {
      _uniffiWriteCoverallFlatError(item, writer);
    }
  }
  else if (value is ReturnOnlyEnumFour) {
    writer.writeI32(4);
    writer.writeI32(value.m.length);
    for (final entry in value.m.entries) {
      writer.writeString(entry.key);
      _uniffiWriteCoverallFlatError(entry.value, writer);
    }
  }
  else {
    throw StateError('Unknown ReturnOnlyEnum variant instance: $value');
  }
}

Uint8List _uniffiEncodeReturnOnlyEnum(ReturnOnlyEnum value) {
  final writer = _UniFfiBinaryWriter();
  _uniffiWriteReturnOnlyEnum(value, writer);
  return writer.toBytes();
}

ReturnOnlyEnum _uniffiReadReturnOnlyEnum(_UniFfiBinaryReader reader) {
  final int tag = reader.readI32();
  switch (tag) {
    case 1:
      return ReturnOnlyEnumOne(
        e: _uniffiReadCoverallFlatError(reader),
      );
    case 2:
      return ReturnOnlyEnumTwo(
        d: _uniffiReadReturnOnlyDict(reader),
      );
    case 3:
      return ReturnOnlyEnumThree(
        l: (() { final int __len = reader.readI32(); final out = <CoverallFlatError>[]; for (var i = 0; i < __len; i++) { out.add(_uniffiReadCoverallFlatError(reader)); } return out; })(),
      );
    case 4:
      return ReturnOnlyEnumFour(
        m: (() { final int __len = reader.readI32(); final out = <String, CoverallFlatError>{}; for (var i = 0; i < __len; i++) { final key = reader.readString(); final value = _uniffiReadCoverallFlatError(reader); out[key] = value; } return out; })(),
      );
    default:
      throw StateError('Unknown ReturnOnlyEnum variant tag: $tag');
  }
}

ReturnOnlyEnum _uniffiDecodeReturnOnlyEnum(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = _uniffiReadReturnOnlyEnum(reader);
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding ReturnOnlyEnum');
  }
  return value;
}

/// Callback interface exercising multiple return types and error paths.
abstract interface class Getters {
  bool getBool(bool v, bool arg2);
  String getString(String v, bool arg2);
  String? getOption(String v, bool arg2);
  List<int> getList(List<int> v, bool arg2);
  void getNothing(String v);
  Coveralls roundTripObject(Coveralls coveralls);
}

/// Callback interface for tree-like trait object graphs.
abstract interface class NodeTrait {
  String name();
  void setParent(NodeTrait? parent);
  NodeTrait? getParent();
}

final class _GettersVTable extends ffi.Struct {
  external ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 handle)>> uniffiFree;

  external ffi.Pointer<ffi.NativeFunction<ffi.Uint64 Function(ffi.Uint64 handle)>> uniffiClone;

  external ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Bool v, ffi.Bool arg2, ffi.Pointer<ffi.Bool> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>> getBool;

  external ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>> getString;

  external ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>> getOption;

  external ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>> getList;

  external ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Pointer<ffi.Void> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>> getNothing;

  external ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Uint64 coveralls, ffi.Pointer<ffi.Uint64> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>> roundTripObject;

}

final class _GettersCallbackBridge {
  _GettersCallbackBridge._();
  static final _GettersCallbackBridge instance = _GettersCallbackBridge._();

  final Map<int, Getters> _callbacks = <int, Getters>{};
  final Map<int, int> _refCounts = <int, int>{};
  int _nextHandle = 1;

  int register(Getters callback) {
    final int handle = _nextHandle++;
    _callbacks[handle] = callback;
    _refCounts[handle] = 1;
    return handle;
  }

  void release(int handle) {
    final int? refs = _refCounts[handle];
    if (refs == null) {
      return;
    }
    if (refs <= 1) {
      _refCounts.remove(handle);
      _callbacks.remove(handle);
      return;
    }
    _refCounts[handle] = refs - 1;
  }

  int cloneHandle(int handle) {
    final int? refs = _refCounts[handle];
    if (refs == null) {
      throw StateError('Invalid callback handle: $handle');
    }
    _refCounts[handle] = refs + 1;
    return handle;
  }

  Getters? lookup(int handle) => _callbacks[handle];

  static final ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle)> _freeNative = ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle)>.isolateLocal((int handle) {
    instance.release(handle);
  });

  static final ffi.NativeCallable<ffi.Uint64 Function(ffi.Uint64 handle)> _cloneNative = ffi.NativeCallable<ffi.Uint64 Function(ffi.Uint64 handle)>.isolateLocal((int handle) {
    return instance.cloneHandle(handle);
  }, exceptionalReturn: 0);

  static final ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Bool v, ffi.Bool arg2, ffi.Pointer<ffi.Bool> outReturn, ffi.Pointer<_RustCallStatus> outStatus)> _getBoolNative = ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Bool v, ffi.Bool arg2, ffi.Pointer<ffi.Bool> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>.isolateLocal((int handle, bool v, bool arg2, ffi.Pointer<ffi.Bool> outReturn, ffi.Pointer<_RustCallStatus> outStatus) {
    final Getters? callback = instance.lookup(handle);
    if (callback == null) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = 'Invalid callback handle'.toNativeUtf8();
      return;
    }
    try {
      final result = callback.getBool(v, arg2);
      outReturn.value = result;
      outStatus.ref
        ..code = _rustCallStatusSuccess
        ..errorBuf = ffi.nullptr;
    } catch (err) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = err.toString().toNativeUtf8();
    }
  });

  static final ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus)> _getStringNative = ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>.isolateLocal((int handle, ffi.Pointer<Utf8> v, bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus) {
    final Getters? callback = instance.lookup(handle);
    if (callback == null) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = 'Invalid callback handle'.toNativeUtf8();
      return;
    }
    try {
      final result = callback.getString(v == ffi.nullptr ? (throw StateError('Rust passed null string callback arg')) : v.toDartString(), arg2);
      outReturn.value = result.toNativeUtf8();
      outStatus.ref
        ..code = _rustCallStatusSuccess
        ..errorBuf = ffi.nullptr;
    } catch (err) {
      if (err is CoverallErrorException) {
        outStatus.ref
          ..code = _rustCallStatusError
          ..errorBuf = CoverallErrorExceptionFfiCodec.encode(err).toNativeUtf8();
      } else {
        outStatus.ref
          ..code = _rustCallStatusUnexpectedError
          ..errorBuf = err.toString().toNativeUtf8();
      }
    }
  });

  static final ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus)> _getOptionNative = ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>.isolateLocal((int handle, ffi.Pointer<Utf8> v, bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus) {
    final Getters? callback = instance.lookup(handle);
    if (callback == null) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = 'Invalid callback handle'.toNativeUtf8();
      return;
    }
    try {
      final result = callback.getOption(v == ffi.nullptr ? (throw StateError('Rust passed null string callback arg')) : v.toDartString(), arg2);
      outReturn.value = result == null ? ffi.nullptr : result.toNativeUtf8();
      outStatus.ref
        ..code = _rustCallStatusSuccess
        ..errorBuf = ffi.nullptr;
    } catch (err) {
      if (err is ComplexErrorException) {
        outStatus.ref
          ..code = _rustCallStatusError
          ..errorBuf = ComplexErrorExceptionFfiCodec.encode(err).toNativeUtf8();
      } else {
        outStatus.ref
          ..code = _rustCallStatusUnexpectedError
          ..errorBuf = err.toString().toNativeUtf8();
      }
    }
  });

  static final ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus)> _getListNative = ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>.isolateLocal((int handle, ffi.Pointer<Utf8> v, bool arg2, ffi.Pointer<ffi.Pointer<Utf8>> outReturn, ffi.Pointer<_RustCallStatus> outStatus) {
    final Getters? callback = instance.lookup(handle);
    if (callback == null) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = 'Invalid callback handle'.toNativeUtf8();
      return;
    }
    try {
      final result = callback.getList(v == ffi.nullptr ? (throw StateError('Rust passed null sequence callback arg')) : (jsonDecode(v.toDartString()) as List).map((item) => (item as num).toInt()).toList(), arg2);
      outReturn.value = jsonEncode(result.map((item) => item).toList()).toNativeUtf8();
      outStatus.ref
        ..code = _rustCallStatusSuccess
        ..errorBuf = ffi.nullptr;
    } catch (err) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = err.toString().toNativeUtf8();
    }
  });

  static final ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Pointer<ffi.Void> outReturn, ffi.Pointer<_RustCallStatus> outStatus)> _getNothingNative = ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> v, ffi.Pointer<ffi.Void> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>.isolateLocal((int handle, ffi.Pointer<Utf8> v, ffi.Pointer<ffi.Void> outReturn, ffi.Pointer<_RustCallStatus> outStatus) {
    final Getters? callback = instance.lookup(handle);
    if (callback == null) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = 'Invalid callback handle'.toNativeUtf8();
      return;
    }
    try {
      callback.getNothing(v == ffi.nullptr ? (throw StateError('Rust passed null string callback arg')) : v.toDartString());
      outStatus.ref
        ..code = _rustCallStatusSuccess
        ..errorBuf = ffi.nullptr;
    } catch (err) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = err.toString().toNativeUtf8();
    }
  });

  static final ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Uint64 coveralls, ffi.Pointer<ffi.Uint64> outReturn, ffi.Pointer<_RustCallStatus> outStatus)> _roundTripObjectNative = ffi.NativeCallable<ffi.Void Function(ffi.Uint64 handle, ffi.Uint64 coveralls, ffi.Pointer<ffi.Uint64> outReturn, ffi.Pointer<_RustCallStatus> outStatus)>.isolateLocal((int handle, int coveralls, ffi.Pointer<ffi.Uint64> outReturn, ffi.Pointer<_RustCallStatus> outStatus) {
    final Getters? callback = instance.lookup(handle);
    if (callback == null) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = 'Invalid callback handle'.toNativeUtf8();
      return;
    }
    try {
      final result = callback.roundTripObject(CoverallsFfiCodec.lift(coveralls));
      outReturn.value = CoverallsFfiCodec.lower(result);
      outStatus.ref
        ..code = _rustCallStatusSuccess
        ..errorBuf = ffi.nullptr;
    } catch (err) {
      outStatus.ref
        ..code = _rustCallStatusUnexpectedError
        ..errorBuf = err.toString().toNativeUtf8();
    }
  });

  static ffi.Pointer<_GettersVTable> createVTable() {
    final ffi.Pointer<_GettersVTable> vtablePtr = calloc<_GettersVTable>();
    vtablePtr.ref
      ..uniffiFree = _freeNative.nativeFunction
      ..uniffiClone = _cloneNative.nativeFunction
      ..getBool = _getBoolNative.nativeFunction
      ..getString = _getStringNative.nativeFunction
      ..getOption = _getOptionNative.nativeFunction
      ..getList = _getListNative.nativeFunction
      ..getNothing = _getNothingNative.nativeFunction
      ..roundTripObject = _roundTripObjectNative.nativeFunction
    ;
    return vtablePtr;
  }
}

class CoverallDemoFfi {
  CoverallDemoFfi({ffi.DynamicLibrary? dynamicLibrary, String? libraryPath})
      : _dynamicLibrary = dynamicLibrary,
        _libraryPath = libraryPath;

  final ffi.DynamicLibrary? _dynamicLibrary;
  final String? _libraryPath;

  static const String libraryName = 'uniffi_coverall_demo';

  ffi.DynamicLibrary open() {
    final provided = _dynamicLibrary;
    if (provided != null) {
      return provided;
    }
    return ffi.DynamicLibrary.open(_libraryPath ?? libraryName);
  }

  late final ffi.DynamicLibrary _lib = open();

  late final void Function(ffi.Pointer<Utf8>) _rustStringFree = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<Utf8>), void Function(ffi.Pointer<Utf8>)>('rust_string_free');

  late final void Function(_RustBuffer) _rustBytesFree = _lib.lookupFunction<ffi.Void Function(_RustBuffer), void Function(_RustBuffer)>('rust_bytes_free');

  late final void Function(ffi.Pointer<_GettersVTable>) _gettersCallbackInit = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_GettersVTable>), void Function(ffi.Pointer<_GettersVTable>)>('getters_callback_init');
  late final ffi.Pointer<_GettersVTable> _gettersCallbackVTable = _GettersCallbackBridge.createVTable();
  late final bool _gettersCallbackInitDone = (() {
    _gettersCallbackInit(_gettersCallbackVTable);
    return true;
  })();
  // WARNING: Function 'ancestor_names(NodeTrait node)' was omitted because
  // the function signature is not yet supported in this FFI binding mode.


  late final ffi.Pointer<Utf8> Function() _createNoneDict = _lib.lookupFunction<ffi.Pointer<Utf8> Function(), ffi.Pointer<Utf8> Function()>('create_none_dict');

  SimpleDict createNoneDict() {
      final ffi.Pointer<Utf8> resultPtr = _createNoneDict();
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for create_none_dict');
      }
      try {
        final String payload = resultPtr.toDartString();
        return SimpleDict.fromJson(jsonDecode(payload) as Map<String, dynamic>);
      } finally {
        _rustStringFree(resultPtr);
      }
  }

  late final int Function(ffi.Pointer<Utf8> color) _createPatch = _lib.lookupFunction<ffi.Uint64 Function(ffi.Pointer<Utf8> color), int Function(ffi.Pointer<Utf8> color)>('create_patch');

  Patch createPatch(Color color) {
    final String colorNativeJson = ColorFfiCodec.encode(color);
    final ffi.Pointer<Utf8> colorNative = colorNativeJson.toNativeUtf8();
    try {
      return Patch._(this, _createPatch(colorNative));
    } finally {
    calloc.free(colorNative);
    }
  }

  late final ffi.Pointer<Utf8> Function() _createSomeDict = _lib.lookupFunction<ffi.Pointer<Utf8> Function(), ffi.Pointer<Utf8> Function()>('create_some_dict');

  SimpleDict createSomeDict() {
      final ffi.Pointer<Utf8> resultPtr = _createSomeDict();
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for create_some_dict');
      }
      try {
        final String payload = resultPtr.toDartString();
        return SimpleDict.fromJson(jsonDecode(payload) as Map<String, dynamic>);
      } finally {
        _rustStringFree(resultPtr);
      }
  }

  late final ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> input) _describeMaybeColor = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> input), ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> input)>('describe_maybe_color');

  String describeMaybeColor(Color? input) {
    final ffi.Pointer<Utf8> inputNative = input == null ? ffi.nullptr : ColorFfiCodec.encode(input).toNativeUtf8();
    try {
      final ffi.Pointer<Utf8> resultPtr = _describeMaybeColor(inputNative);
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for describe_maybe_color');
      }
      try {
        return resultPtr.toDartString();
      } finally {
        _rustStringFree(resultPtr);
      }
    } finally {
    if (inputNative != ffi.nullptr) calloc.free(inputNative);
    }
  }

  late final ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> input) _describeMaybeDict = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> input), ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> input)>('describe_maybe_dict');

  String describeMaybeDict(SimpleDict? input) {
    final ffi.Pointer<Utf8> inputNative = input == null ? ffi.nullptr : jsonEncode(input.toJson()).toNativeUtf8();
    try {
      final ffi.Pointer<Utf8> resultPtr = _describeMaybeDict(inputNative);
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for describe_maybe_dict');
      }
      try {
        return resultPtr.toDartString();
      } finally {
        _rustStringFree(resultPtr);
      }
    } finally {
    if (inputNative != ffi.nullptr) calloc.free(inputNative);
    }
  }

  late final ffi.Pointer<Utf8> Function(double value, ffi.Pointer<Utf8> divisor) _divideByText = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Double value, ffi.Pointer<Utf8> divisor), ffi.Pointer<Utf8> Function(double value, ffi.Pointer<Utf8> divisor)>('divide_by_text');

  double divideByText(double value, String divisor) {
    final ffi.Pointer<Utf8> divisorNative = divisor.toNativeUtf8();
    try {
      final ffi.Pointer<Utf8> resultPtr = _divideByText(value, divisorNative);
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for divide_by_text');
      }
      final String payload;
      try {
        payload = resultPtr.toDartString();
      } finally {
        _rustStringFree(resultPtr);
      }
      final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
      final Object? errRaw = envelope['err'];
      if (errRaw != null) {
        throw ComplexErrorExceptionFfiCodec.decode(errRaw);
      }
      if (!envelope.containsKey('ok')) {
        throw StateError('Rust returned malformed result for divide_by_text');
      }
      final Object? okRaw = envelope['ok'];
      return (okRaw as num).toDouble();
    } finally {
    calloc.free(divisorNative);
    }
  }

  late final _RustBuffer Function(int key, int value) _getIntMap = _lib.lookupFunction<_RustBuffer Function(ffi.Uint32 key, ffi.Uint64 value), _RustBuffer Function(int key, int value)>('get_int_map');

  Map<int, int> getIntMap(int key, int value) {
      final _RustBuffer resultBuf = _getIntMap(key, value);
      final ffi.Pointer<ffi.Uint8> resultData = resultBuf.data;
      final int resultLen = resultBuf.len;
      if (resultData == ffi.nullptr) {
        _rustBytesFree(resultBuf);
        final mapReader = _UniFfiBinaryReader(Uint8List(0));
        return (() { final int __len = mapReader.readI32(); final out = <int, int>{}; for (var i = 0; i < __len; i++) { final key = mapReader.readU32(); final value = mapReader.readU64(); out[key] = value; } return out; })();
      }
      try {
        final Uint8List resultBytes = Uint8List.fromList(resultData.asTypedList(resultLen));
        final mapReader = _UniFfiBinaryReader(resultBytes);
        return (() { final int __len = mapReader.readI32(); final out = <int, int>{}; for (var i = 0; i < __len; i++) { final key = mapReader.readU32(); final value = mapReader.readU64(); out[key] = value; } return out; })();
      } finally {
        _rustBytesFree(resultBuf);
      }
  }

  late final ffi.Pointer<Utf8> Function(bool returnValue) _getMaybeColor = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Bool returnValue), ffi.Pointer<Utf8> Function(bool returnValue)>('get_maybe_color');

  Color? getMaybeColor(bool returnValue) {
      final ffi.Pointer<Utf8> resultPtr = _getMaybeColor(returnValue);
      if (resultPtr == ffi.nullptr) {
        return null;
      }
      try {
        final String payload = resultPtr.toDartString();
        return ColorFfiCodec.decode(payload);
      } finally {
        _rustStringFree(resultPtr);
      }
  }

  late final ffi.Pointer<Utf8> Function(bool returnValue) _getMaybeCount = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Bool returnValue), ffi.Pointer<Utf8> Function(bool returnValue)>('get_maybe_count');

  int? getMaybeCount(bool returnValue) {
      final ffi.Pointer<Utf8> resultPtr = _getMaybeCount(returnValue);
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for get_maybe_count');
      }
      try {
        final String payload = resultPtr.toDartString();
        final Object? decoded = jsonDecode(payload);
        return decoded == null ? null : (decoded as num).toInt();
      } finally {
        _rustStringFree(resultPtr);
      }
  }

  late final ffi.Pointer<Utf8> Function(bool returnValue) _getMaybeDict = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Bool returnValue), ffi.Pointer<Utf8> Function(bool returnValue)>('get_maybe_dict');

  SimpleDict? getMaybeDict(bool returnValue) {
      final ffi.Pointer<Utf8> resultPtr = _getMaybeDict(returnValue);
      if (resultPtr == ffi.nullptr) {
        return null;
      }
      try {
        final String payload = resultPtr.toDartString();
        return SimpleDict.fromJson(jsonDecode(payload) as Map<String, dynamic>);
      } finally {
        _rustStringFree(resultPtr);
      }
  }

  late final ffi.Pointer<Utf8> Function(bool returnValue) _getMaybeFlag = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Bool returnValue), ffi.Pointer<Utf8> Function(bool returnValue)>('get_maybe_flag');

  bool? getMaybeFlag(bool returnValue) {
      final ffi.Pointer<Utf8> resultPtr = _getMaybeFlag(returnValue);
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for get_maybe_flag');
      }
      try {
        final String payload = resultPtr.toDartString();
        final Object? decoded = jsonDecode(payload);
        return decoded == null ? null : decoded as bool;
      } finally {
        _rustStringFree(resultPtr);
      }
  }

  late final ffi.Pointer<Utf8> Function(int index) _getMaybeSimpleDict = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Int8 index), ffi.Pointer<Utf8> Function(int index)>('get_maybe_simple_dict');

  MaybeSimpleDict getMaybeSimpleDict(int index) {
      final ffi.Pointer<Utf8> resultPtr = _getMaybeSimpleDict(index);
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for get_maybe_simple_dict');
      }
      try {
        final String payload = resultPtr.toDartString();
        return MaybeSimpleDictFfiCodec.decode(payload);
      } finally {
        _rustStringFree(resultPtr);
      }
  }

  late final int Function() _getNumAlive = _lib.lookupFunction<ffi.Uint64 Function(), int Function()>('get_num_alive');

  int getNumAlive() {
      return _getNumAlive();
  }
  // WARNING: Function 'make_rust_getters()' was omitted because
  // the function signature is not yet supported in this FFI binding mode.


  late final ffi.Pointer<Utf8> Function() _outputReturnOnlyDict = _lib.lookupFunction<ffi.Pointer<Utf8> Function(), ffi.Pointer<Utf8> Function()>('output_return_only_dict');

  ReturnOnlyDict outputReturnOnlyDict() {
      final ffi.Pointer<Utf8> resultPtr = _outputReturnOnlyDict();
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for output_return_only_dict');
      }
      try {
        final String payload = resultPtr.toDartString();
        return ReturnOnlyDict.fromJson(jsonDecode(payload) as Map<String, dynamic>);
      } finally {
        _rustStringFree(resultPtr);
      }
  }

  late final ffi.Pointer<Utf8> Function() _outputReturnOnlyEnum = _lib.lookupFunction<ffi.Pointer<Utf8> Function(), ffi.Pointer<Utf8> Function()>('output_return_only_enum');

  ReturnOnlyEnum outputReturnOnlyEnum() {
      final ffi.Pointer<Utf8> resultPtr = _outputReturnOnlyEnum();
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for output_return_only_enum');
      }
      try {
        final String payload = resultPtr.toDartString();
        return ReturnOnlyEnumFfiCodec.decode(payload);
      } finally {
        _rustStringFree(resultPtr);
      }
  }

  late final ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> value) _println = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> value), ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> value)>('println');

  String println(String value) {
    final ffi.Pointer<Utf8> valueNative = value.toNativeUtf8();
    try {
      final ffi.Pointer<Utf8> resultPtr = _println(valueNative);
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for println');
      }
      final String payload;
      try {
        payload = resultPtr.toDartString();
      } finally {
        _rustStringFree(resultPtr);
      }
      final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
      final Object? errRaw = envelope['err'];
      if (errRaw != null) {
        throw ComplexErrorExceptionFfiCodec.decode(errRaw);
      }
      if (!envelope.containsKey('ok')) {
        throw StateError('Rust returned malformed result for println');
      }
      final Object? okRaw = envelope['ok'];
      return okRaw as String;
    } finally {
    calloc.free(valueNative);
    }
  }

  late final _RustBuffer Function(_RustBuffer input) _reverseBytes = _lib.lookupFunction<_RustBuffer Function(_RustBuffer input), _RustBuffer Function(_RustBuffer input)>('reverse_bytes');

  Uint8List reverseBytes(Uint8List input) {
    final ffi.Pointer<ffi.Uint8> inputData = input.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(input.length);
    if (inputData != ffi.nullptr) {
      inputData.asTypedList(input.length).setAll(0, input);
    }
    final ffi.Pointer<_RustBuffer> inputBufferPtr = calloc<_RustBuffer>();
    inputBufferPtr.ref.data = inputData;
    inputBufferPtr.ref.len = input.length;
    final _RustBuffer inputNative = inputBufferPtr.ref;
    try {
      final _RustBuffer resultBuf = _reverseBytes(inputNative);
      final ffi.Pointer<ffi.Uint8> resultData = resultBuf.data;
      final int resultLen = resultBuf.len;
      if (resultData == ffi.nullptr) {
        if (resultLen == 0) {
          _rustBytesFree(resultBuf);
          return Uint8List(0);
        }
        throw StateError('Rust returned invalid buffer for reverse_bytes');
      }
      try {
        return Uint8List.fromList(resultData.asTypedList(resultLen));
      } finally {
        _rustBytesFree(resultBuf);
      }
    } finally {
    if (inputData != ffi.nullptr) calloc.free(inputData);
    calloc.free(inputBufferPtr);
    }
  }

  late final ffi.Pointer<Utf8> Function(int getters) _testGetters = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 getters), ffi.Pointer<Utf8> Function(int getters)>('test_getters');

  void testGetters(Getters getters) {
    _gettersCallbackInitDone;
    final int gettersHandle = _GettersCallbackBridge.instance.register(getters);
    try {
      final ffi.Pointer<Utf8> resultPtr = _testGetters(gettersHandle);
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for test_getters');
      }
      final String payload;
      try {
        payload = resultPtr.toDartString();
      } finally {
        _rustStringFree(resultPtr);
      }
      final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
      final Object? errRaw = envelope['err'];
      if (errRaw != null) {
        throw CoverallErrorExceptionFfiCodec.decode(errRaw);
      }
      return;
    } finally {
    _GettersCallbackBridge.instance.release(gettersHandle);
    }
  }

  late final ffi.Pointer<Utf8> Function() _throwFlatError = _lib.lookupFunction<ffi.Pointer<Utf8> Function(), ffi.Pointer<Utf8> Function()>('throw_flat_error');

  void throwFlatError() {
      final ffi.Pointer<Utf8> resultPtr = _throwFlatError();
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for throw_flat_error');
      }
      final String payload;
      try {
        payload = resultPtr.toDartString();
      } finally {
        _rustStringFree(resultPtr);
      }
      final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
      final Object? errRaw = envelope['err'];
      if (errRaw != null) {
        throw CoverallFlatErrorExceptionFfiCodec.decode(errRaw);
      }
      return;
  }

  late final ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> source) _validateHtml = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> source), ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> source)>('validate_html');

  void validateHtml(String source) {
    final ffi.Pointer<Utf8> sourceNative = source.toNativeUtf8();
    try {
      final ffi.Pointer<Utf8> resultPtr = _validateHtml(sourceNative);
      if (resultPtr == ffi.nullptr) {
        throw StateError('Rust returned null for validate_html');
      }
      final String payload;
      try {
        payload = resultPtr.toDartString();
      } finally {
        _rustStringFree(resultPtr);
      }
      final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
      final Object? errRaw = envelope['err'];
      if (errRaw != null) {
        throw HTMLErrorExceptionFfiCodec.decode(errRaw);
      }
      return;
    } finally {
    calloc.free(sourceNative);
    }
  }

  late final void Function(int handle) _coverallsFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('coveralls_free');

  late final ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> name, bool shouldFail) _coverallsCtorFallibleNew = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> name, ffi.Bool shouldFail), ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> name, bool shouldFail)>('coveralls_fallible_new');

  Coveralls coverallsCreateFallibleNew(String name, bool shouldFail) {
    final ffi.Pointer<Utf8> nameNative = name.toNativeUtf8();
    try {
    final ffi.Pointer<Utf8> resultPtr = _coverallsCtorFallibleNew(nameNative, shouldFail);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_fallible_new');
    }
    final String payload;
    try {
      payload = resultPtr.toDartString();
    } finally {
      _rustStringFree(resultPtr);
    }
    final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
    final Object? errRaw = envelope['err'];
    if (errRaw != null) {
      throw CoverallErrorExceptionFfiCodec.decode(errRaw);
    }
    final Object? okRaw = envelope['ok'];
    final int handle = (okRaw as num).toInt();
    return Coveralls._(this, handle);
    } finally {
    calloc.free(nameNative);
    }
  }

  late final int Function(ffi.Pointer<Utf8> name) _coverallsCtorNew = _lib.lookupFunction<ffi.Uint64 Function(ffi.Pointer<Utf8> name), int Function(ffi.Pointer<Utf8> name)>('coveralls_new');

  Coveralls coverallsCreateNew(String name) {
    final ffi.Pointer<Utf8> nameNative = name.toNativeUtf8();
    try {
    final int handle = _coverallsCtorNew(nameNative);
    return Coveralls._(this, handle);
    } finally {
    calloc.free(nameNative);
    }
  }

  late final void Function(int handle, int patch) _coverallsAddPatch = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Uint64 patch), void Function(int handle, int patch)>('coveralls_add_patch');

  void coverallsInvokeAddPatch(int handle, Patch patch) {
    final int patchHandle = PatchFfiCodec.lower(patch);
    _coverallsAddPatch(handle, patchHandle);
  }

  late final void Function(int handle, ffi.Pointer<Utf8> repair) _coverallsAddRepair = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> repair), void Function(int handle, ffi.Pointer<Utf8> repair)>('coveralls_add_repair');

  void coverallsInvokeAddRepair(int handle, Repair repair) {
    final String repairNativeJson = jsonEncode(repair.toJson());
    final ffi.Pointer<Utf8> repairNative = repairNativeJson.toNativeUtf8();
    try {
    _coverallsAddRepair(handle, repairNative);
    } finally {
    calloc.free(repairNative);
    }
  }

  late final int Function(int handle) _coverallsCloneMe = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle), int Function(int handle)>('coveralls_clone_me');

  Coveralls coverallsInvokeCloneMe(int handle) {
    return Coveralls._(_bindings(), _coverallsCloneMe(handle));
  }

  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<Utf8> key, int value) _coverallsGetDict2 = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<Utf8> key, ffi.Uint64 value), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<Utf8> key, int value)>('coveralls_get_dict2');

  Map<String, int> coverallsInvokeGetDict2(int handle, String key, int value) {
    final ffi.Pointer<Utf8> keyNative = key.toNativeUtf8();
    try {
    final ffi.Pointer<Utf8> resultPtr = _coverallsGetDict2(handle, keyNative, value);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_get_dict2');
    }
    try {
      final String payload = resultPtr.toDartString();
      return (jsonDecode(payload) as Map<String, dynamic>).map((key, value) => MapEntry(key, (value as num).toInt()));
    } finally {
      _rustStringFree(resultPtr);
    }
    } finally {
    calloc.free(keyNative);
    }
  }

  late final _RustBuffer Function(int handle, int key, int value) _coverallsGetDict3 = _lib.lookupFunction<_RustBuffer Function(ffi.Uint64 handle, ffi.Uint32 key, ffi.Uint64 value), _RustBuffer Function(int handle, int key, int value)>('coveralls_get_dict3');

  Map<int, int> coverallsInvokeGetDict3(int handle, int key, int value) {
    final _RustBuffer resultBuf = _coverallsGetDict3(handle, key, value);
    final ffi.Pointer<ffi.Uint8> resultData = resultBuf.data;
    final int resultLen = resultBuf.len;
    if (resultData == ffi.nullptr) {
      _rustBytesFree(resultBuf);
      final mapReader = _UniFfiBinaryReader(Uint8List(0));
      return (() { final int __len = mapReader.readI32(); final out = <int, int>{}; for (var i = 0; i < __len; i++) { final key = mapReader.readU32(); final value = mapReader.readU64(); out[key] = value; } return out; })();
    }
    try {
      final Uint8List resultBytes = Uint8List.fromList(resultData.asTypedList(resultLen));
      final mapReader = _UniFfiBinaryReader(resultBytes);
      return (() { final int __len = mapReader.readI32(); final out = <int, int>{}; for (var i = 0; i < __len; i++) { final key = mapReader.readU32(); final value = mapReader.readU64(); out[key] = value; } return out; })();
    } finally {
      _rustBytesFree(resultBuf);
    }
  }

  late final ffi.Pointer<Utf8> Function(int handle) _coverallsGetMetadata = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle), ffi.Pointer<Utf8> Function(int handle)>('coveralls_get_metadata');

  Map<String, String?> coverallsInvokeGetMetadata(int handle) {
    final ffi.Pointer<Utf8> resultPtr = _coverallsGetMetadata(handle);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_get_metadata');
    }
    try {
      final String payload = resultPtr.toDartString();
      return (jsonDecode(payload) as Map<String, dynamic>).map((key, value) => MapEntry(key, value == null ? null : value as String));
    } finally {
      _rustStringFree(resultPtr);
    }
  }

  late final ffi.Pointer<Utf8> Function(int handle) _coverallsGetName = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle), ffi.Pointer<Utf8> Function(int handle)>('coveralls_get_name');

  String coverallsInvokeGetName(int handle) {
    final ffi.Pointer<Utf8> resultPtr = _coverallsGetName(handle);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_get_name');
    }
    try {
      return resultPtr.toDartString();
    } finally {
      _rustStringFree(resultPtr);
    }
  }

  late final int Function(int handle) _coverallsGetOther = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle), int Function(int handle)>('coveralls_get_other');

  Coveralls? coverallsInvokeGetOther(int handle) {
    final int resultHandle = _coverallsGetOther(handle);
    if (resultHandle == 0) {
      return null;
    }
    return Coveralls._(_bindings(), resultHandle);
  }

  late final ffi.Pointer<Utf8> Function(int handle) _coverallsGetRepairs = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle), ffi.Pointer<Utf8> Function(int handle)>('coveralls_get_repairs');

  List<Repair> coverallsInvokeGetRepairs(int handle) {
    final ffi.Pointer<Utf8> resultPtr = _coverallsGetRepairs(handle);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_get_repairs');
    }
    try {
      final String payload = resultPtr.toDartString();
      return (jsonDecode(payload) as List).map((item) => Repair.fromJson(item as Map<String, dynamic>)).toList();
    } finally {
      _rustStringFree(resultPtr);
    }
  }

  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<Utf8> status) _coverallsGetStatus = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<Utf8> status), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<Utf8> status)>('coveralls_get_status');

  String coverallsInvokeGetStatus(int handle, String status) {
    final ffi.Pointer<Utf8> statusNative = status.toNativeUtf8();
    try {
    final ffi.Pointer<Utf8> resultPtr = _coverallsGetStatus(handle, statusNative);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_get_status');
    }
    try {
      return resultPtr.toDartString();
    } finally {
      _rustStringFree(resultPtr);
    }
    } finally {
    calloc.free(statusNative);
    }
  }

  late final ffi.Pointer<Utf8> Function(int handle) _coverallsGetTags = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle), ffi.Pointer<Utf8> Function(int handle)>('coveralls_get_tags');

  List<String?> coverallsInvokeGetTags(int handle) {
    final ffi.Pointer<Utf8> resultPtr = _coverallsGetTags(handle);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_get_tags');
    }
    try {
      final String payload = resultPtr.toDartString();
      return (jsonDecode(payload) as List).map((item) => item == null ? null : item as String).toList();
    } finally {
      _rustStringFree(resultPtr);
    }
  }

  late final ffi.Pointer<Utf8> Function(int handle, bool shouldThrow) _coverallsMaybeThrow = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Bool shouldThrow), ffi.Pointer<Utf8> Function(int handle, bool shouldThrow)>('coveralls_maybe_throw');

  bool coverallsInvokeMaybeThrow(int handle, bool shouldThrow) {
    final ffi.Pointer<Utf8> resultPtr = _coverallsMaybeThrow(handle, shouldThrow);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_maybe_throw');
    }
    final String payload;
    try {
      payload = resultPtr.toDartString();
    } finally {
      _rustStringFree(resultPtr);
    }
    final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
    final Object? errRaw = envelope['err'];
    if (errRaw != null) {
      throw CoverallErrorExceptionFfiCodec.decode(errRaw);
    }
    final Object? okRaw = envelope['ok'];
    return okRaw as bool;
  }

  late final ffi.Pointer<Utf8> Function(int handle, int selector) _coverallsMaybeThrowComplex = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Int8 selector), ffi.Pointer<Utf8> Function(int handle, int selector)>('coveralls_maybe_throw_complex');

  bool coverallsInvokeMaybeThrowComplex(int handle, int selector) {
    final ffi.Pointer<Utf8> resultPtr = _coverallsMaybeThrowComplex(handle, selector);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_maybe_throw_complex');
    }
    final String payload;
    try {
      payload = resultPtr.toDartString();
    } finally {
      _rustStringFree(resultPtr);
    }
    final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
    final Object? errRaw = envelope['err'];
    if (errRaw != null) {
      throw ComplexErrorExceptionFfiCodec.decode(errRaw);
    }
    final Object? okRaw = envelope['ok'];
    return okRaw as bool;
  }

  late final ffi.Pointer<Utf8> Function(int handle, bool shouldThrow) _coverallsMaybeThrowInto = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Bool shouldThrow), ffi.Pointer<Utf8> Function(int handle, bool shouldThrow)>('coveralls_maybe_throw_into');

  bool coverallsInvokeMaybeThrowInto(int handle, bool shouldThrow) {
    final ffi.Pointer<Utf8> resultPtr = _coverallsMaybeThrowInto(handle, shouldThrow);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_maybe_throw_into');
    }
    final String payload;
    try {
      payload = resultPtr.toDartString();
    } finally {
      _rustStringFree(resultPtr);
    }
    final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
    final Object? errRaw = envelope['err'];
    if (errRaw != null) {
      throw CoverallErrorExceptionFfiCodec.decode(errRaw);
    }
    final Object? okRaw = envelope['ok'];
    return okRaw as bool;
  }

  late final _RustBuffer Function(int handle, _RustBuffer input) _coverallsReverseBytes = _lib.lookupFunction<_RustBuffer Function(ffi.Uint64 handle, _RustBuffer input), _RustBuffer Function(int handle, _RustBuffer input)>('coveralls_reverse_bytes');

  Uint8List coverallsInvokeReverseBytes(int handle, Uint8List input) {
    final ffi.Pointer<ffi.Uint8> inputData = input.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(input.length);
    if (inputData != ffi.nullptr) {
      inputData.asTypedList(input.length).setAll(0, input);
    }
    final ffi.Pointer<_RustBuffer> inputBufferPtr = calloc<_RustBuffer>();
    inputBufferPtr.ref.data = inputData;
    inputBufferPtr.ref.len = input.length;
    final _RustBuffer inputNative = inputBufferPtr.ref;
    try {
    final _RustBuffer resultBuf = _coverallsReverseBytes(handle, inputNative);
    final ffi.Pointer<ffi.Uint8> resultData = resultBuf.data;
    final int resultLen = resultBuf.len;
    if (resultData == ffi.nullptr) {
      if (resultLen == 0) {
        _rustBytesFree(resultBuf);
        return Uint8List(0);
      }
      throw StateError('Rust returned invalid buffer for coveralls_reverse_bytes');
    }
    try {
      return Uint8List.fromList(resultData.asTypedList(resultLen));
    } finally {
      _rustBytesFree(resultBuf);
    }
    } finally {
    if (inputData != ffi.nullptr) calloc.free(inputData);
    calloc.free(inputBufferPtr);
    }
  }

  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<Utf8> emptyStruct) _coverallsSetAndGetEmptyStruct = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<Utf8> emptyStruct), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<Utf8> emptyStruct)>('coveralls_set_and_get_empty_struct');

  EmptyStruct coverallsInvokeSetAndGetEmptyStruct(int handle, EmptyStruct emptyStruct) {
    final String emptyStructNativeJson = jsonEncode(emptyStruct.toJson());
    final ffi.Pointer<Utf8> emptyStructNative = emptyStructNativeJson.toNativeUtf8();
    try {
    final ffi.Pointer<Utf8> resultPtr = _coverallsSetAndGetEmptyStruct(handle, emptyStructNative);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for coveralls_set_and_get_empty_struct');
    }
    try {
      final String payload = resultPtr.toDartString();
      return EmptyStruct.fromJson(jsonDecode(payload) as Map<String, dynamic>);
    } finally {
      _rustStringFree(resultPtr);
    }
    } finally {
    calloc.free(emptyStructNative);
    }
  }

  late final void Function(int handle, ffi.Pointer<Utf8> name) _coverallsSetName = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<Utf8> name), void Function(int handle, ffi.Pointer<Utf8> name)>('coveralls_set_name');

  void coverallsInvokeSetName(int handle, String name) {
    final ffi.Pointer<Utf8> nameNative = name.toNativeUtf8();
    try {
    _coverallsSetName(handle, nameNative);
    } finally {
    calloc.free(nameNative);
    }
  }

  late final int Function(int handle) _coverallsStrongCount = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle), int Function(int handle)>('coveralls_strong_count');

  int coverallsInvokeStrongCount(int handle) {
    return _coverallsStrongCount(handle);
  }

  late final void Function(int handle, int other) _coverallsTakeOther = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Uint64 other), void Function(int handle, int other)>('coveralls_take_other');

  void coverallsInvokeTakeOther(int handle, Coveralls? other) {
    final int otherHandle = other == null ? 0 : CoverallsFfiCodec.lower(other);
    _coverallsTakeOther(handle, otherHandle);
  }

  late final void Function(int handle) _falliblePatchFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('falliblepatch_free');

  late final ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> color, bool shouldFail) _falliblePatchCtorNew = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> color, ffi.Bool shouldFail), ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> color, bool shouldFail)>('falliblepatch_new');

  FalliblePatch falliblePatchCreateNew(Color color, bool shouldFail) {
    final String colorNativeJson = ColorFfiCodec.encode(color);
    final ffi.Pointer<Utf8> colorNative = colorNativeJson.toNativeUtf8();
    try {
    final ffi.Pointer<Utf8> resultPtr = _falliblePatchCtorNew(colorNative, shouldFail);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for falliblepatch_new');
    }
    final String payload;
    try {
      payload = resultPtr.toDartString();
    } finally {
      _rustStringFree(resultPtr);
    }
    final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
    final Object? errRaw = envelope['err'];
    if (errRaw != null) {
      throw CoverallErrorExceptionFfiCodec.decode(errRaw);
    }
    final Object? okRaw = envelope['ok'];
    final int handle = (okRaw as num).toInt();
    return FalliblePatch._(this, handle);
    } finally {
    calloc.free(colorNative);
    }
  }

  late final ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> color, bool shouldFail) _falliblePatchCtorSecondary = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> color, ffi.Bool shouldFail), ffi.Pointer<Utf8> Function(ffi.Pointer<Utf8> color, bool shouldFail)>('falliblepatch_secondary');

  FalliblePatch falliblePatchCreateSecondary(Color color, bool shouldFail) {
    final String colorNativeJson = ColorFfiCodec.encode(color);
    final ffi.Pointer<Utf8> colorNative = colorNativeJson.toNativeUtf8();
    try {
    final ffi.Pointer<Utf8> resultPtr = _falliblePatchCtorSecondary(colorNative, shouldFail);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for falliblepatch_secondary');
    }
    final String payload;
    try {
      payload = resultPtr.toDartString();
    } finally {
      _rustStringFree(resultPtr);
    }
    final Map<String, dynamic> envelope = jsonDecode(payload) as Map<String, dynamic>;
    final Object? errRaw = envelope['err'];
    if (errRaw != null) {
      throw CoverallErrorExceptionFfiCodec.decode(errRaw);
    }
    final Object? okRaw = envelope['ok'];
    final int handle = (okRaw as num).toInt();
    return FalliblePatch._(this, handle);
    } finally {
    calloc.free(colorNative);
    }
  }

  late final ffi.Pointer<Utf8> Function(int handle) _falliblePatchGetColor = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle), ffi.Pointer<Utf8> Function(int handle)>('falliblepatch_get_color');

  Color falliblePatchInvokeGetColor(int handle) {
    final ffi.Pointer<Utf8> resultPtr = _falliblePatchGetColor(handle);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for falliblepatch_get_color');
    }
    try {
      final String payload = resultPtr.toDartString();
      return ColorFfiCodec.decode(payload);
    } finally {
      _rustStringFree(resultPtr);
    }
  }

  late final void Function(int handle) _iFirstFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('ifirst_free');

  late final int Function() _iFirstCtorNew = _lib.lookupFunction<ffi.Uint64 Function(), int Function()>('ifirst_new');

  IFirst iFirstCreateNew() {
    final int handle = _iFirstCtorNew();
    return IFirst._(this, handle);
  }

  late final bool Function(int handle, int other) _iFirstCompare = _lib.lookupFunction<ffi.Bool Function(ffi.Uint64 handle, ffi.Uint64 other), bool Function(int handle, int other)>('ifirst_compare');

  bool iFirstInvokeCompare(int handle, ISecond? other) {
    final int otherHandle = other == null ? 0 : ISecondFfiCodec.lower(other);
    return _iFirstCompare(handle, otherHandle);
  }

  late final void Function(int handle) _iSecondFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('isecond_free');

  late final int Function() _iSecondCtorNew = _lib.lookupFunction<ffi.Uint64 Function(), int Function()>('isecond_new');

  ISecond iSecondCreateNew() {
    final int handle = _iSecondCtorNew();
    return ISecond._(this, handle);
  }

  late final bool Function(int handle, int other) _iSecondCompare = _lib.lookupFunction<ffi.Bool Function(ffi.Uint64 handle, ffi.Uint64 other), bool Function(int handle, int other)>('isecond_compare');

  bool iSecondInvokeCompare(int handle, IFirst? other) {
    final int otherHandle = other == null ? 0 : IFirstFfiCodec.lower(other);
    return _iSecondCompare(handle, otherHandle);
  }

  late final void Function(int handle) _patchFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('patch_free');

  late final int Function(ffi.Pointer<Utf8> color) _patchCtorNew = _lib.lookupFunction<ffi.Uint64 Function(ffi.Pointer<Utf8> color), int Function(ffi.Pointer<Utf8> color)>('patch_new');

  Patch patchCreateNew(Color color) {
    final String colorNativeJson = ColorFfiCodec.encode(color);
    final ffi.Pointer<Utf8> colorNative = colorNativeJson.toNativeUtf8();
    try {
    final int handle = _patchCtorNew(colorNative);
    return Patch._(this, handle);
    } finally {
    calloc.free(colorNative);
    }
  }

  late final ffi.Pointer<Utf8> Function(int handle) _patchGetColor = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle), ffi.Pointer<Utf8> Function(int handle)>('patch_get_color');

  Color patchInvokeGetColor(int handle) {
    final ffi.Pointer<Utf8> resultPtr = _patchGetColor(handle);
    if (resultPtr == ffi.nullptr) {
      throw StateError('Rust returned null for patch_get_color');
    }
    try {
      final String payload = resultPtr.toDartString();
      return ColorFfiCodec.decode(payload);
    } finally {
      _rustStringFree(resultPtr);
    }
  }

  late final void Function(int handle) _threadsafeCounterFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('threadsafecounter_free');

  late final int Function() _threadsafeCounterCtorNew = _lib.lookupFunction<ffi.Uint64 Function(), int Function()>('threadsafecounter_new');

  ThreadsafeCounter threadsafeCounterCreateNew() {
    final int handle = _threadsafeCounterCtorNew();
    return ThreadsafeCounter._(this, handle);
  }

  late final int Function(int handle) _threadsafeCounterGetCount = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle), int Function(int handle)>('threadsafecounter_get_count');

  int threadsafeCounterInvokeGetCount(int handle) {
    return _threadsafeCounterGetCount(handle);
  }

  late final void Function(int handle) _threadsafeCounterIncrement = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('threadsafecounter_increment');

  void threadsafeCounterInvokeIncrement(int handle) {
    _threadsafeCounterIncrement(handle);
  }
}

final class _CoverallsFinalizerToken {
  const _CoverallsFinalizerToken(this.free, this.handle);
  final void Function(int) free;
  final int handle;
}

/// Main object exercising self-references, state, errors, and Arc semantics.
final class Coveralls {
  Coveralls._(this._ffi, this._handle) {
    _finalizer.attach(this, _CoverallsFinalizerToken(_ffi._coverallsFree, _handle), detach: this);
  }

  final CoverallDemoFfi _ffi;
  int _handle;
  bool _closed = false;

  static final Finalizer<_CoverallsFinalizerToken> _finalizer = Finalizer((token) {
    token.free(token.handle);
  });

  bool get isClosed => _closed;

  void close() {
    if (_closed) {
      return;
    }
    _closed = true;
    _finalizer.detach(this);
    _ffi._coverallsFree(_handle);
  }

  void _ensureOpen() {
    if (_closed) {
      throw StateError('Coveralls is closed');
    }
  }

  static Coveralls fallibleNew(String name, bool shouldFail) {
    return _bindings().coverallsCreateFallibleNew(name, shouldFail);
  }

  static Coveralls create(String name) {
    return _bindings().coverallsCreateNew(name);
  }

  /// Add a patch to this coveralls.
  void addPatch(Patch patch) {
    _ensureOpen();
    _ffi.coverallsInvokeAddPatch(_handle, patch);
  }

  /// Add a repair record.
  void addRepair(Repair repair) {
    _ensureOpen();
    _ffi.coverallsInvokeAddRepair(_handle, repair);
  }

  /// Clone into a new instance (tests object return from method).
  Coveralls cloneMe() {
    _ensureOpen();
    return _ffi.coverallsInvokeCloneMe(_handle);
  }

  /// Return a string-keyed map.
  Map<String, int> getDict2(String key, int value) {
    _ensureOpen();
    return _ffi.coverallsInvokeGetDict2(_handle, key, value);
  }

  /// Return a non-string-keyed map.
  Map<int, int> getDict3(int key, int value) {
    _ensureOpen();
    return _ffi.coverallsInvokeGetDict3(_handle, key, value);
  }

  /// Return a map with string keys and optional string values.
  Map<String, String?> getMetadata() {
    _ensureOpen();
    return _ffi.coverallsInvokeGetMetadata(_handle);
  }

  String getName() {
    _ensureOpen();
    return _ffi.coverallsInvokeGetName(_handle);
  }

  Coveralls? getOther() {
    _ensureOpen();
    return _ffi.coverallsInvokeGetOther(_handle);
  }

  /// Return all stored repairs.
  List<Repair> getRepairs() {
    _ensureOpen();
    return _ffi.coverallsInvokeGetRepairs(_handle);
  }

  /// Simple pass-through returning the status string.
  String getStatus(String status) {
    _ensureOpen();
    return _ffi.coverallsInvokeGetStatus(_handle, status);
  }

  /// Return a sequence of optional strings.
  List<String?> getTags() {
    _ensureOpen();
    return _ffi.coverallsInvokeGetTags(_handle);
  }

  /// Method that always throws a simple error.
  bool maybeThrow(bool shouldThrow) {
    _ensureOpen();
    return _ffi.coverallsInvokeMaybeThrow(_handle, shouldThrow);
  }

  /// Method that throws a complex (rich) error.
  bool maybeThrowComplex(int selector) {
    _ensureOpen();
    return _ffi.coverallsInvokeMaybeThrowComplex(_handle, selector);
  }

  /// Another throws variant — returns boolean.
  bool maybeThrowInto(bool shouldThrow) {
    _ensureOpen();
    return _ffi.coverallsInvokeMaybeThrowInto(_handle, shouldThrow);
  }

  /// Reverse some bytes via a method (tests binary on object).
  Uint8List reverseBytes(Uint8List input) {
    _ensureOpen();
    return _ffi.coverallsInvokeReverseBytes(_handle, input);
  }

  /// Empty record round-trip.
  EmptyStruct setAndGetEmptyStruct(EmptyStruct emptyStruct) {
    _ensureOpen();
    return _ffi.coverallsInvokeSetAndGetEmptyStruct(_handle, emptyStruct);
  }

  void setName(String name) {
    _ensureOpen();
    _ffi.coverallsInvokeSetName(_handle, name);
  }

  /// Strong reference count (tests Self=ByArc on the Rust side).
  int strongCount() {
    _ensureOpen();
    return _ffi.coverallsInvokeStrongCount(_handle);
  }

  /// Take another Coveralls, creating a reference chain.
  void takeOther(Coveralls? other) {
    _ensureOpen();
    _ffi.coverallsInvokeTakeOther(_handle, other);
  }

}

final class CoverallsFfiCodec {
  const CoverallsFfiCodec._();

  static int lower(Coveralls value) => value._handle;

  static Coveralls lift(int handle) => Coveralls._(_bindings(), handle);
}

final class _FalliblePatchFinalizerToken {
  const _FalliblePatchFinalizerToken(this.free, this.handle);
  final void Function(int) free;
  final int handle;
}

/// Object with fallible constructors — tests error on construction.
final class FalliblePatch {
  FalliblePatch._(this._ffi, this._handle) {
    _finalizer.attach(this, _FalliblePatchFinalizerToken(_ffi._falliblePatchFree, _handle), detach: this);
  }

  final CoverallDemoFfi _ffi;
  int _handle;
  bool _closed = false;

  static final Finalizer<_FalliblePatchFinalizerToken> _finalizer = Finalizer((token) {
    token.free(token.handle);
  });

  bool get isClosed => _closed;

  void close() {
    if (_closed) {
      return;
    }
    _closed = true;
    _finalizer.detach(this);
    _ffi._falliblePatchFree(_handle);
  }

  void _ensureOpen() {
    if (_closed) {
      throw StateError('FalliblePatch is closed');
    }
  }

  static FalliblePatch create(Color color, bool shouldFail) {
    return _bindings().falliblePatchCreateNew(color, shouldFail);
  }

  static FalliblePatch secondary(Color color, bool shouldFail) {
    return _bindings().falliblePatchCreateSecondary(color, shouldFail);
  }

  Color getColor() {
    _ensureOpen();
    return _ffi.falliblePatchInvokeGetColor(_handle);
  }

}

final class FalliblePatchFfiCodec {
  const FalliblePatchFfiCodec._();

  static int lower(FalliblePatch value) => value._handle;

  static FalliblePatch lift(int handle) => FalliblePatch._(_bindings(), handle);
}

final class _IFirstFinalizerToken {
  const _IFirstFinalizerToken(this.free, this.handle);
  final void Function(int) free;
  final int handle;
}

/// Forward-reference test: IFirst refers to ISecond and vice versa.
final class IFirst {
  IFirst._(this._ffi, this._handle) {
    _finalizer.attach(this, _IFirstFinalizerToken(_ffi._iFirstFree, _handle), detach: this);
  }

  final CoverallDemoFfi _ffi;
  int _handle;
  bool _closed = false;

  static final Finalizer<_IFirstFinalizerToken> _finalizer = Finalizer((token) {
    token.free(token.handle);
  });

  bool get isClosed => _closed;

  void close() {
    if (_closed) {
      return;
    }
    _closed = true;
    _finalizer.detach(this);
    _ffi._iFirstFree(_handle);
  }

  void _ensureOpen() {
    if (_closed) {
      throw StateError('IFirst is closed');
    }
  }

  static IFirst create() {
    return _bindings().iFirstCreateNew();
  }

  bool compare({ISecond? other = null}) {
    _ensureOpen();
    return _ffi.iFirstInvokeCompare(_handle, other);
  }

}

final class IFirstFfiCodec {
  const IFirstFfiCodec._();

  static int lower(IFirst value) => value._handle;

  static IFirst lift(int handle) => IFirst._(_bindings(), handle);
}

final class _ISecondFinalizerToken {
  const _ISecondFinalizerToken(this.free, this.handle);
  final void Function(int) free;
  final int handle;
}

final class ISecond {
  ISecond._(this._ffi, this._handle) {
    _finalizer.attach(this, _ISecondFinalizerToken(_ffi._iSecondFree, _handle), detach: this);
  }

  final CoverallDemoFfi _ffi;
  int _handle;
  bool _closed = false;

  static final Finalizer<_ISecondFinalizerToken> _finalizer = Finalizer((token) {
    token.free(token.handle);
  });

  bool get isClosed => _closed;

  void close() {
    if (_closed) {
      return;
    }
    _closed = true;
    _finalizer.detach(this);
    _ffi._iSecondFree(_handle);
  }

  void _ensureOpen() {
    if (_closed) {
      throw StateError('ISecond is closed');
    }
  }

  static ISecond create() {
    return _bindings().iSecondCreateNew();
  }

  bool compare(IFirst? other) {
    _ensureOpen();
    return _ffi.iSecondInvokeCompare(_handle, other);
  }

}

final class ISecondFfiCodec {
  const ISecondFfiCodec._();

  static int lower(ISecond value) => value._handle;

  static ISecond lift(int handle) => ISecond._(_bindings(), handle);
}

final class _PatchFinalizerToken {
  const _PatchFinalizerToken(this.free, this.handle);
  final void Function(int) free;
  final int handle;
}

/// A simple object that wraps a Color.
final class Patch {
  Patch._(this._ffi, this._handle) {
    _finalizer.attach(this, _PatchFinalizerToken(_ffi._patchFree, _handle), detach: this);
  }

  final CoverallDemoFfi _ffi;
  int _handle;
  bool _closed = false;

  static final Finalizer<_PatchFinalizerToken> _finalizer = Finalizer((token) {
    token.free(token.handle);
  });

  bool get isClosed => _closed;

  void close() {
    if (_closed) {
      return;
    }
    _closed = true;
    _finalizer.detach(this);
    _ffi._patchFree(_handle);
  }

  void _ensureOpen() {
    if (_closed) {
      throw StateError('Patch is closed');
    }
  }

  static Patch create(Color color) {
    return _bindings().patchCreateNew(color);
  }

  Color getColor() {
    _ensureOpen();
    return _ffi.patchInvokeGetColor(_handle);
  }

}

final class PatchFfiCodec {
  const PatchFfiCodec._();

  static int lower(Patch value) => value._handle;

  static Patch lift(int handle) => Patch._(_bindings(), handle);
}

final class _ThreadsafeCounterFinalizerToken {
  const _ThreadsafeCounterFinalizerToken(this.free, this.handle);
  final void Function(int) free;
  final int handle;
}

/// Thread-safe counter (tests atomic state and thread safety).
final class ThreadsafeCounter {
  ThreadsafeCounter._(this._ffi, this._handle) {
    _finalizer.attach(this, _ThreadsafeCounterFinalizerToken(_ffi._threadsafeCounterFree, _handle), detach: this);
  }

  final CoverallDemoFfi _ffi;
  int _handle;
  bool _closed = false;

  static final Finalizer<_ThreadsafeCounterFinalizerToken> _finalizer = Finalizer((token) {
    token.free(token.handle);
  });

  bool get isClosed => _closed;

  void close() {
    if (_closed) {
      return;
    }
    _closed = true;
    _finalizer.detach(this);
    _ffi._threadsafeCounterFree(_handle);
  }

  void _ensureOpen() {
    if (_closed) {
      throw StateError('ThreadsafeCounter is closed');
    }
  }

  static ThreadsafeCounter create() {
    return _bindings().threadsafeCounterCreateNew();
  }

  int getCount() {
    _ensureOpen();
    return _ffi.threadsafeCounterInvokeGetCount(_handle);
  }

  void increment() {
    _ensureOpen();
    _ffi.threadsafeCounterInvokeIncrement(_handle);
  }

}

final class ThreadsafeCounterFfiCodec {
  const ThreadsafeCounterFfiCodec._();

  static int lower(ThreadsafeCounter value) => value._handle;

  static ThreadsafeCounter lift(int handle) => ThreadsafeCounter._(_bindings(), handle);
}

CoverallDemoFfi? _defaultBindings;

CoverallDemoFfi _bindings() => _defaultBindings ??= CoverallDemoFfi();

void configureDefaultBindings({ffi.DynamicLibrary? dynamicLibrary, String? libraryPath}) {
  _defaultBindings = CoverallDemoFfi(dynamicLibrary: dynamicLibrary, libraryPath: libraryPath);
}

void resetDefaultBindings() {
  _defaultBindings = null;
}

/// Walk up a NodeTrait tree, collecting ancestor names.
List<String> ancestorNames(NodeTrait node) {
  throw UnimplementedError('TODO: bind to Rust FFI');
}

/// Create a SimpleDict whose optional fields are all null.
SimpleDict createNoneDict() {
  return _bindings().createNoneDict();
}

/// Create a Patch with a specific color.
Patch createPatch(Color color) {
  return _bindings().createPatch(color);
}

/// Create a SimpleDict with sample values filled in.
SimpleDict createSomeDict() {
  return _bindings().createSomeDict();
}

/// Accept an optional Color enum parameter.
String describeMaybeColor(Color? input) {
  return _bindings().describeMaybeColor(input);
}

/// Accept an optional SimpleDict parameter.
String describeMaybeDict(SimpleDict? input) {
  return _bindings().describeMaybeDict(input);
}

/// Divide a float by a text-parsed divisor; exercises error paths.
double divideByText(double value, String divisor) {
  return _bindings().divideByText(value, divisor);
}

/// Round-trip a map with non-string keys (record<u32, u64>).
Map<int, int> getIntMap(int key, int value) {
  return _bindings().getIntMap(key, value);
}

/// Return an optional Color enum based on a flag.
Color? getMaybeColor(bool returnValue) {
  return _bindings().getMaybeColor(returnValue);
}

/// Return an optional u32 based on a flag.
int? getMaybeCount(bool returnValue) {
  return _bindings().getMaybeCount(returnValue);
}

/// Return an optional SimpleDict based on a flag.
SimpleDict? getMaybeDict(bool returnValue) {
  return _bindings().getMaybeDict(returnValue);
}

/// Return an optional boolean based on a flag.
bool? getMaybeFlag(bool returnValue) {
  return _bindings().getMaybeFlag(returnValue);
}

/// Return an enum variant based on a numeric selector.
MaybeSimpleDict getMaybeSimpleDict(int index) {
  return _bindings().getMaybeSimpleDict(index);
}

/// Return the number of live Coveralls instances (tracking via constructor/destructor).
int getNumAlive() {
  return _bindings().getNumAlive();
}

/// Create a Rust-implemented Getters for trait round-trip tests.
Getters makeRustGetters() {
  throw UnimplementedError('TODO: bind to Rust FFI');
}

/// Return a ReturnOnlyDict (tests return-only record type).
ReturnOnlyDict outputReturnOnlyDict() {
  return _bindings().outputReturnOnlyDict();
}

/// Return a ReturnOnlyEnum (tests return-only enum type).
ReturnOnlyEnum outputReturnOnlyEnum() {
  return _bindings().outputReturnOnlyEnum();
}

/// Round-trip a string; throws ComplexError on certain inputs.
String println(String value) {
  return _bindings().println(value);
}

/// Reverse arbitrary bytes (binary data round-trip).
Uint8List reverseBytes(Uint8List input) {
  return _bindings().reverseBytes(input);
}

/// Exercise a foreign-implemented Getters.
void testGetters(Getters getters) {
  _bindings().testGetters(getters);
}

/// Throw a CoverallFlatError.
void throwFlatError() {
  _bindings().throwFlatError();
}

/// Validate HTML source; throws HTMLError on invalid input.
void validateHtml(String source) {
  _bindings().validateHtml(source);
}

