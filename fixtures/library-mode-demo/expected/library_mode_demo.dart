// Generated by uniffi-bindgen-dart. DO NOT EDIT.
// ignore_for_file: unused_element, unused_import, unused_field, unnecessary_non_null_assertion, dead_code
library library_mode_demo;

import 'dart:async';
import 'dart:convert';
import 'dart:ffi' as ffi;
import 'package:ffi/ffi.dart';
import 'dart:typed_data';

typedef Label = String;

final class _UniFfiFfiBufferElement extends ffi.Union {
  @ffi.Uint8()
  external int u8;

  @ffi.Int8()
  external int i8;

  @ffi.Uint16()
  external int u16;

  @ffi.Int16()
  external int i16;

  @ffi.Uint32()
  external int u32;

  @ffi.Int32()
  external int i32;

  @ffi.Uint64()
  external int u64;

  @ffi.Int64()
  external int i64;

  @ffi.Float()
  external double float32;

  @ffi.Double()
  external double float64;

  external ffi.Pointer<ffi.Void> ptr;
}

final class _UniFfiRustBuffer extends ffi.Struct {
  @ffi.Uint64()
  external int capacity;

  @ffi.Uint64()
  external int len;

  external ffi.Pointer<ffi.Uint8> data;
}

final class _UniFfiForeignBytes extends ffi.Struct {
  @ffi.Int32()
  external int len;

  external ffi.Pointer<ffi.Uint8> data;
}

final class _UniFfiRustCallStatus extends ffi.Struct {
  @ffi.Int8()
  external int code;

  external _UniFfiRustBuffer errorBuf;
}

const int _uniFfiRustCallStatusSuccess = 0;
const int _uniFfiRustCallStatusError = 1;
const int _uniFfiRustCallStatusUnexpectedError = 2;
const int _uniFfiRustCallStatusCancelled = 3;

final class _RustCallStatus extends ffi.Struct {
  @ffi.Int8()
  external int code;

  external ffi.Pointer<Utf8> errorBuf;
}

const int _rustCallStatusSuccess = 0;
const int _rustCallStatusError = 1;
const int _rustCallStatusUnexpectedError = 2;
const int _rustCallStatusCancelled = 3;
const int _rustFuturePollReady = 0;
const int _rustFuturePollWake = 1;

class Point {
  const Point({
    required this.x,
    required this.y,
  });

  final double x;
  final double y;

  Map<String, dynamic> toJson() {
    return {
      'x': this.x,
      'y': this.y,
    };
  }

  static Point fromJson(Map<String, dynamic> json) {
    return Point(
      x: (json['x'] as num).toDouble(),
      y: (json['y'] as num).toDouble(),
    );
  }

  Point copyWith({
    double? x,
    double? y,
  }) {
    return Point(
      x: x ?? this.x,
      y: y ?? this.y,
    );
  }
}

sealed class ArithError {
  const ArithError();
}

final class ArithErrorDivisionByZero extends ArithError {
  const ArithErrorDivisionByZero();
}

sealed class Shape {
  const Shape();
}

final class ShapeCircle extends Shape {
  const ShapeCircle({
    required this.radius,
  });
  final double radius;
}

final class ShapeRect extends Shape {
  const ShapeRect({
    required this.w,
    required this.h,
  });
  final double w;
  final double h;
}

sealed class ArithErrorException implements Exception {
  const ArithErrorException();
}

final class ArithErrorExceptionDivisionByZero extends ArithErrorException {
  const ArithErrorExceptionDivisionByZero();
}

ArithErrorException _uniffiLiftArithErrorException(Uint8List bytes) {
  final ArithError value = _uniffiDecodeArithError(bytes);
  if (value is ArithErrorDivisionByZero) return const ArithErrorExceptionDivisionByZero();
  throw StateError('Unknown ArithError error variant while lifting exception: $value');
}

String _encodeArithError(ArithError value) {
  if (value is ArithErrorDivisionByZero) {
    return jsonEncode({
      'tag': 'divisionByZero',
    });
  }
  throw StateError('Unknown ArithError variant instance: $value');
}

ArithError _decodeArithError(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'divisionByZero':
      return ArithErrorDivisionByZero(
      );
    default:
      throw StateError('Unknown ArithError variant tag: $tag');
  }
}

String _encodeShape(Shape value) {
  if (value is ShapeCircle) {
    return jsonEncode({
      'tag': 'circle',
      'radius': value.radius,
    });
  }
  if (value is ShapeRect) {
    return jsonEncode({
      'tag': 'rect',
      'w': value.w,
      'h': value.h,
    });
  }
  throw StateError('Unknown Shape variant instance: $value');
}

Shape _decodeShape(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'circle':
      return ShapeCircle(
        radius: (map['radius'] as num).toDouble(),
      );
    case 'rect':
      return ShapeRect(
        w: (map['w'] as num).toDouble(),
        h: (map['h'] as num).toDouble(),
      );
    default:
      throw StateError('Unknown Shape variant tag: $tag');
  }
}

String _encodeArithErrorException(ArithErrorException value) {
  if (value is ArithErrorExceptionDivisionByZero) {
    return jsonEncode({
      'tag': 'divisionByZero',
    });
  }
  throw StateError('Unknown ArithErrorException exception instance: $value');
}

ArithErrorException _decodeArithErrorException(Object? raw) {
  final Map<String, dynamic> map = raw is String ? (jsonDecode(raw) as Map<String, dynamic>) : (raw as Map<String, dynamic>);
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'divisionByZero':
      return const ArithErrorExceptionDivisionByZero();
    default:
      throw StateError('Unknown ArithErrorException exception tag: $tag');
  }
}

final class ArithErrorFfiCodec {
  const ArithErrorFfiCodec._();

  static String encode(ArithError value) => _encodeArithError(value);

  static ArithError decode(String raw) => _decodeArithError(raw);
}

final class ShapeFfiCodec {
  const ShapeFfiCodec._();

  static String encode(Shape value) => _encodeShape(value);

  static Shape decode(String raw) => _decodeShape(raw);
}

final class ArithErrorExceptionFfiCodec {
  const ArithErrorExceptionFfiCodec._();

  static String encode(ArithErrorException value) => _encodeArithErrorException(value);

  static ArithErrorException decode(Object? raw) => _decodeArithErrorException(raw);
}

final class _UniFfiBinaryWriter {
  final BytesBuilder _builder = BytesBuilder(copy: false);

  void writeU8(int value) => _builder.add([value & 0xFF]);
  void writeI8(int value) => _builder.add([(value) & 0xFF]);
  void writeU16(int value) {
    final data = ByteData(2)..setUint16(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeI16(int value) {
    final data = ByteData(2)..setInt16(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeU32(int value) {
    final data = ByteData(4)..setUint32(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeI32(int value) {
    final data = ByteData(4)..setInt32(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeU64(int value) {
    final data = ByteData(8)..setUint64(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeI64(int value) {
    final data = ByteData(8)..setInt64(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeF32(double value) {
    final data = ByteData(4)..setFloat32(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeF64(double value) {
    final data = ByteData(8)..setFloat64(0, value, Endian.big);
    _builder.add(data.buffer.asUint8List());
  }
  void writeBool(bool value) => writeI8(value ? 1 : 0);
  void writeBytes(Uint8List bytes) => _builder.add(bytes);
  void writeString(String value) {
    final bytes = Uint8List.fromList(utf8.encode(value));
    writeI32(bytes.length);
    writeBytes(bytes);
  }

  Uint8List toBytes() => _builder.takeBytes();
}

final class _UniFfiBinaryReader {
  _UniFfiBinaryReader(this._bytes);
  final Uint8List _bytes;
  int _offset = 0;

  bool get isDone => _offset == _bytes.length;

  ByteData _readData(int len) {
    if (_offset + len > _bytes.length) {
      throw StateError('buffer underflow while decoding UniFFI payload');
    }
    final data = ByteData.sublistView(_bytes, _offset, _offset + len);
    _offset += len;
    return data;
  }

  int readU8() => _readData(1).getUint8(0);
  int readI8() => _readData(1).getInt8(0);
  int readU16() => _readData(2).getUint16(0, Endian.big);
  int readI16() => _readData(2).getInt16(0, Endian.big);
  int readU32() => _readData(4).getUint32(0, Endian.big);
  int readI32() => _readData(4).getInt32(0, Endian.big);
  int readU64() => _readData(8).getUint64(0, Endian.big);
  int readI64() => _readData(8).getInt64(0, Endian.big);
  double readF32() => _readData(4).getFloat32(0, Endian.big);
  double readF64() => _readData(8).getFloat64(0, Endian.big);
  bool readBool() {
    final value = readI8();
    if (value == 0) return false;
    if (value == 1) return true;
    throw StateError('invalid boolean payload value: $value');
  }
  Uint8List readBytes(int len) {
    if (_offset + len > _bytes.length) {
      throw StateError('buffer underflow while decoding UniFFI payload bytes');
    }
    final out = Uint8List.fromList(_bytes.sublist(_offset, _offset + len));
    _offset += len;
    return out;
  }
  String readString() {
    final len = readI32();
    if (len < 0) {
      throw StateError('invalid string length in UniFFI payload: $len');
    }
    return utf8.decode(readBytes(len));
  }
}

Uint8List _uniffiEncodePoint(Point value) {
  final writer = _UniFfiBinaryWriter();
  writer.writeF64(value.x);
  writer.writeF64(value.y);
  return writer.toBytes();
}

Point _uniffiDecodePoint(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final value = Point(
    x: reader.readF64(),
    y: reader.readF64(),
  );
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding Point');
  }
  return value;
}

Uint8List _uniffiEncodeArithError(ArithError value) {
  final writer = _UniFfiBinaryWriter();
  if (value is ArithErrorDivisionByZero) {
    writer.writeI32(1);
  }
  else {
    throw StateError('Unknown ArithError variant instance: $value');
  }
  return writer.toBytes();
}

ArithError _uniffiDecodeArithError(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final int tag = reader.readI32();
  final ArithError value;
  switch (tag) {
    case 1:
      value = const ArithErrorDivisionByZero();
      break;
    default:
      throw StateError('Unknown ArithError variant tag: $tag');
  }
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding ArithError');
  }
  return value;
}

Uint8List _uniffiEncodeShape(Shape value) {
  final writer = _UniFfiBinaryWriter();
  if (value is ShapeCircle) {
    writer.writeI32(1);
    writer.writeF64(value.radius);
  }
  else if (value is ShapeRect) {
    writer.writeI32(2);
    writer.writeF64(value.w);
    writer.writeF64(value.h);
  }
  else {
    throw StateError('Unknown Shape variant instance: $value');
  }
  return writer.toBytes();
}

Shape _uniffiDecodeShape(Uint8List bytes) {
  final reader = _UniFfiBinaryReader(bytes);
  final int tag = reader.readI32();
  final Shape value;
  switch (tag) {
    case 1:
      value = ShapeCircle(
        radius: reader.readF64(),
      );
      break;
    case 2:
      value = ShapeRect(
        w: reader.readF64(),
        h: reader.readF64(),
      );
      break;
    default:
      throw StateError('Unknown Shape variant tag: $tag');
  }
  if (!reader.isDone) {
    throw StateError('extra bytes remaining while decoding Shape');
  }
  return value;
}

class LibraryModeDemoFfi {
  LibraryModeDemoFfi({ffi.DynamicLibrary? dynamicLibrary, String? libraryPath})
      : _dynamicLibrary = dynamicLibrary,
        _libraryPath = libraryPath;

  final ffi.DynamicLibrary? _dynamicLibrary;
  final String? _libraryPath;

  static const String libraryName = 'uniffi_library_mode_demo';

  ffi.DynamicLibrary open() {
    final provided = _dynamicLibrary;
    if (provided != null) {
      return provided;
    }
    return ffi.DynamicLibrary.open(_libraryPath ?? libraryName);
  }

  void _ensureApiIntegrity(ffi.DynamicLibrary lib) {
    const int bindingsContractVersion = 30;
    final int scaffoldingContractVersion;
    try {
      final int Function() ffiContractVersion = lib.lookupFunction<ffi.Uint32 Function(), int Function()>('ffi_uniffi_library_mode_demo_uniffi_contract_version');
      scaffoldingContractVersion = ffiContractVersion();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI contract-version symbol `ffi_uniffi_library_mode_demo_uniffi_contract_version`: $err');
    }
    if (bindingsContractVersion != scaffoldingContractVersion) {
      throw StateError('UniFFI contract version mismatch: expected $bindingsContractVersion, got $scaffoldingContractVersion');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_describe_shape;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_describe_shape');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_describe_shape = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_describe_shape`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_describe_shape != 51108) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_describe_shape`: expected 51108, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_describe_shape');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_divide;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_divide');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_divide = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_divide`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_divide != 55250) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_divide`: expected 55250, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_divide');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_divide_async;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_divide_async');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_divide_async = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_divide_async`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_divide_async != 15218) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_divide_async`: expected 15218, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_divide_async');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_label;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_echo_label');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_label = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_echo_label`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_label != 2529) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_echo_label`: expected 2529, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_label');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_map;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_echo_map');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_map = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_echo_map`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_map != 56419) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_echo_map`: expected 56419, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_map');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_strings;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_echo_strings');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_strings = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_echo_strings`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_strings != 8754) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_echo_strings`: expected 8754, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_echo_strings');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_greet;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_greet');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_greet = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_greet`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_greet != 8324) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_greet`: expected 8324, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_greet');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_greet_async;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_greet_async');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_greet_async = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_greet_async`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_greet_async != 11130) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_greet_async`: expected 11130, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_greet_async');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_make_point;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_make_point');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_make_point = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_make_point`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_make_point != 32456) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_make_point`: expected 32456, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_make_point');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_func_maybe_greet;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_func_maybe_greet');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_func_maybe_greet = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_func_maybe_greet`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_func_maybe_greet != 46882) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_func_maybe_greet`: expected 46882, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_func_maybe_greet');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_async_divide;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_method_counter_async_divide');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_async_divide = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_method_counter_async_divide`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_async_divide != 27466) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_method_counter_async_divide`: expected 27466, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_async_divide');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_async_get;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_method_counter_async_get');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_async_get = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_method_counter_async_get`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_async_get != 31515) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_method_counter_async_get`: expected 31515, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_async_get');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_get;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_method_counter_get');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_get = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_method_counter_get`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_get != 63821) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_method_counter_get`: expected 63821, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_get');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_increment;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_method_counter_increment');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_increment = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_method_counter_increment`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_increment != 54696) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_method_counter_increment`: expected 54696, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_method_counter_increment');
    }
    final int _checksum_uniffi_uniffi_library_mode_demo_checksum_constructor_counter_new;
    try {
      final int Function() checksumFn = lib.lookupFunction<ffi.Uint16 Function(), int Function()>('uniffi_uniffi_library_mode_demo_checksum_constructor_counter_new');
      _checksum_uniffi_uniffi_library_mode_demo_checksum_constructor_counter_new = checksumFn();
    } catch (err) {
      throw StateError('Missing or invalid UniFFI checksum symbol `uniffi_uniffi_library_mode_demo_checksum_constructor_counter_new`: $err');
    }
    if (_checksum_uniffi_uniffi_library_mode_demo_checksum_constructor_counter_new != 16809) {
      throw StateError('UniFFI API checksum mismatch for `uniffi_uniffi_library_mode_demo_checksum_constructor_counter_new`: expected 16809, got $_checksum_uniffi_uniffi_library_mode_demo_checksum_constructor_counter_new');
    }
  }

  late final ffi.DynamicLibrary _lib = (() {
    final ffi.DynamicLibrary lib = open();
    _ensureApiIntegrity(lib);
    return lib;
  })();

  late final _UniFfiRustBuffer Function(_UniFfiForeignBytes bytes, ffi.Pointer<_UniFfiRustCallStatus> outStatus) _uniFfiRustBufferFromBytes = _lib.lookupFunction<_UniFfiRustBuffer Function(_UniFfiForeignBytes bytes, ffi.Pointer<_UniFfiRustCallStatus> outStatus), _UniFfiRustBuffer Function(_UniFfiForeignBytes bytes, ffi.Pointer<_UniFfiRustCallStatus> outStatus)>('ffi_uniffi_library_mode_demo_rustbuffer_from_bytes');
  late final void Function(_UniFfiRustBuffer buf, ffi.Pointer<_UniFfiRustCallStatus> outStatus) _uniFfiRustBufferFree = _lib.lookupFunction<ffi.Void Function(_UniFfiRustBuffer buf, ffi.Pointer<_UniFfiRustCallStatus> outStatus), void Function(_UniFfiRustBuffer buf, ffi.Pointer<_UniFfiRustCallStatus> outStatus)>('ffi_uniffi_library_mode_demo_rustbuffer_free');

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _describeShapeFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_describe_shape');

  String describeShape(Shape shape) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(3);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(7);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final Uint8List shapeBytes = _uniffiEncodeShape(shape);
      final ffi.Pointer<ffi.Uint8> shapePtr = shapeBytes.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(shapeBytes.length);
      if (shapeBytes.isNotEmpty) { shapePtr.asTypedList(shapeBytes.length).setAll(0, shapeBytes); }
      foreignArgPtrs.add(shapePtr);
      final ffi.Pointer<_UniFfiRustCallStatus> shapeFromBytesStatusPtr = calloc<_UniFfiRustCallStatus>();
      shapeFromBytesStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
      shapeFromBytesStatusPtr.ref.errorBuf
        ..capacity = 0
        ..len = 0
        ..data = ffi.nullptr;
      final ffi.Pointer<_UniFfiForeignBytes> shapeForeignPtr = calloc<_UniFfiForeignBytes>();
      shapeForeignPtr.ref
        ..len = shapeBytes.length
        ..data = shapePtr;
      final _UniFfiRustBuffer shapeRustBuffer = _uniFfiRustBufferFromBytes(shapeForeignPtr.ref, shapeFromBytesStatusPtr);
      calloc.free(shapeForeignPtr);
      final int shapeFromBytesCode = shapeFromBytesStatusPtr.ref.code;
      final _UniFfiRustBuffer shapeFromBytesErrBuf = shapeFromBytesStatusPtr.ref.errorBuf;
      calloc.free(shapeFromBytesStatusPtr);
      if (shapeFromBytesCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> shapeFromBytesErrBufPtr = calloc<_UniFfiRustBuffer>();
        shapeFromBytesErrBufPtr.ref
          ..capacity = shapeFromBytesErrBuf.capacity
          ..len = shapeFromBytesErrBuf.len
          ..data = shapeFromBytesErrBuf.data;
        rustRetBufferPtrs.add(shapeFromBytesErrBufPtr);
        throw StateError('UniFFI rustbuffer_from_bytes failed with status $shapeFromBytesCode');
      }
      (argBuf + 0).ref.u64 = shapeRustBuffer.capacity;
      (argBuf + 1).ref.u64 = shapeRustBuffer.len;
      (argBuf + 2).ref.ptr = shapeRustBuffer.data.cast<ffi.Void>();
      _describeShapeFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 3).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 4).ref.u64
          ..len = (returnBuf + 5).ref.u64
          ..data = (returnBuf + 6).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      final ffi.Pointer<_UniFfiRustBuffer> retBufPtr = calloc<_UniFfiRustBuffer>();
      retBufPtr.ref
        ..capacity = (returnBuf + 0).ref.u64
        ..len = (returnBuf + 1).ref.u64
        ..data = (returnBuf + 2).ref.ptr.cast<ffi.Uint8>();
      rustRetBufferPtrs.add(retBufPtr);
      final Uint8List retBytes = retBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(retBufPtr.ref.data.asTypedList(retBufPtr.ref.len));
      final decodedValue = utf8.decode(retBytes);
      return decodedValue;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _divideFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_divide');

  int divide(int a, int b) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(2);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(5);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      (argBuf + 0).ref.u32 = a;
      (argBuf + 1).ref.u32 = b;
      _divideFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 1).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 2).ref.u64
          ..len = (returnBuf + 3).ref.u64
          ..data = (returnBuf + 4).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        if (statusCode == _uniFfiRustCallStatusError) {
          final Uint8List errBytes = errBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(errBufPtr.ref.data.asTypedList(errBufPtr.ref.len));
          throw _uniffiLiftArithErrorException(errBytes);
        }
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      return (returnBuf + 0).ref.u32;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _divideAsyncFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_divide_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _divideAsyncFfiBufferRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('ffi_uniffi_library_mode_demo_rust_future_poll_u32');
  late final void Function(int handle) _divideAsyncFfiBufferRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('ffi_uniffi_library_mode_demo_rust_future_cancel_u32');
  late final int Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus) _divideAsyncFfiBufferRustFutureComplete = _lib.lookupFunction<ffi.Uint32 Function(ffi.Uint64 handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus), int Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus)>('ffi_uniffi_library_mode_demo_rust_future_complete_u32');
  late final void Function(int handle) _divideAsyncFfiBufferRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('ffi_uniffi_library_mode_demo_rust_future_free_u32');

  Future<int> divideAsync(int a, int b) async {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(2);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(5);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      (argBuf + 0).ref.u32 = a;
      (argBuf + 1).ref.u32 = b;
      _divideAsyncFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 1).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 2).ref.u64
          ..len = (returnBuf + 3).ref.u64
          ..data = (returnBuf + 4).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer async start failed with status $statusCode');
      }
      final int futureHandle = (returnBuf + 0).ref.u64;
      final StreamController<int> pollEvents = StreamController<int>.broadcast();
      final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
        pollEvents.add(pollResult);
      });
      try {
        _divideAsyncFfiBufferRustFuturePoll(futureHandle, callback.nativeFunction, 0);
        while (true) {
          final int pollResult = await pollEvents.stream.first;
          if (pollResult == _rustFuturePollReady) {
            break;
          }
          if (pollResult == _rustFuturePollWake) {
            _divideAsyncFfiBufferRustFuturePoll(futureHandle, callback.nativeFunction, 0);
            continue;
          }
          throw StateError('Rust future poll returned invalid status for divide_async: $pollResult');
        }
        final ffi.Pointer<_UniFfiRustCallStatus> outStatusPtr = calloc<_UniFfiRustCallStatus>();
        outStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        outStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        try {
          final int resultValue = _divideAsyncFfiBufferRustFutureComplete(futureHandle, outStatusPtr);
          final int completeStatusCode = outStatusPtr.ref.code;
          if (completeStatusCode == _uniFfiRustCallStatusSuccess) {
            return resultValue;
          }
          if (completeStatusCode == _uniFfiRustCallStatusCancelled) {
            throw StateError('Rust future was cancelled for divide_async');
          }
          final _UniFfiRustBuffer errorBuf = outStatusPtr.ref.errorBuf;
          if (!(errorBuf.data == ffi.nullptr && errorBuf.len == 0 && errorBuf.capacity == 0)) {
            final ffi.Pointer<_UniFfiRustBuffer> errorBufPtr = calloc<_UniFfiRustBuffer>();
            errorBufPtr.ref
              ..capacity = errorBuf.capacity
              ..len = errorBuf.len
              ..data = errorBuf.data;
            rustRetBufferPtrs.add(errorBufPtr);
            final Uint8List errorBytes = errorBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(errorBufPtr.ref.data.asTypedList(errorBufPtr.ref.len));
            if (completeStatusCode == _uniFfiRustCallStatusError && errorBytes.isNotEmpty) {
              throw _uniffiLiftArithErrorException(errorBytes);
            }
            if (errorBytes.isNotEmpty) {
              throw StateError(utf8.decode(errorBytes, allowMalformed: true));
            }
          }
          throw StateError('Rust future failed for divide_async with status code: $completeStatusCode');
        } finally {
          calloc.free(outStatusPtr);
        }
      } catch (_) {
        _divideAsyncFfiBufferRustFutureCancel(futureHandle);
        rethrow;
      } finally {
        await pollEvents.close();
        callback.close();
        _divideAsyncFfiBufferRustFutureFree(futureHandle);
      }
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _echoLabelFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_echo_label');

  String echoLabel(String label) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(3);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(7);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final Uint8List labelBytes = Uint8List.fromList(utf8.encode(label));
      final ffi.Pointer<ffi.Uint8> labelPtr = labelBytes.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(labelBytes.length);
      if (labelBytes.isNotEmpty) { labelPtr.asTypedList(labelBytes.length).setAll(0, labelBytes); }
      foreignArgPtrs.add(labelPtr);
      final ffi.Pointer<_UniFfiRustCallStatus> labelFromBytesStatusPtr = calloc<_UniFfiRustCallStatus>();
      labelFromBytesStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
      labelFromBytesStatusPtr.ref.errorBuf
        ..capacity = 0
        ..len = 0
        ..data = ffi.nullptr;
      final ffi.Pointer<_UniFfiForeignBytes> labelForeignPtr = calloc<_UniFfiForeignBytes>();
      labelForeignPtr.ref
        ..len = labelBytes.length
        ..data = labelPtr;
      final _UniFfiRustBuffer labelRustBuffer = _uniFfiRustBufferFromBytes(labelForeignPtr.ref, labelFromBytesStatusPtr);
      calloc.free(labelForeignPtr);
      final int labelFromBytesCode = labelFromBytesStatusPtr.ref.code;
      final _UniFfiRustBuffer labelFromBytesErrBuf = labelFromBytesStatusPtr.ref.errorBuf;
      calloc.free(labelFromBytesStatusPtr);
      if (labelFromBytesCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> labelFromBytesErrBufPtr = calloc<_UniFfiRustBuffer>();
        labelFromBytesErrBufPtr.ref
          ..capacity = labelFromBytesErrBuf.capacity
          ..len = labelFromBytesErrBuf.len
          ..data = labelFromBytesErrBuf.data;
        rustRetBufferPtrs.add(labelFromBytesErrBufPtr);
        throw StateError('UniFFI rustbuffer_from_bytes failed with status $labelFromBytesCode');
      }
      (argBuf + 0).ref.u64 = labelRustBuffer.capacity;
      (argBuf + 1).ref.u64 = labelRustBuffer.len;
      (argBuf + 2).ref.ptr = labelRustBuffer.data.cast<ffi.Void>();
      _echoLabelFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 3).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 4).ref.u64
          ..len = (returnBuf + 5).ref.u64
          ..data = (returnBuf + 6).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      final ffi.Pointer<_UniFfiRustBuffer> retBufPtr = calloc<_UniFfiRustBuffer>();
      retBufPtr.ref
        ..capacity = (returnBuf + 0).ref.u64
        ..len = (returnBuf + 1).ref.u64
        ..data = (returnBuf + 2).ref.ptr.cast<ffi.Uint8>();
      rustRetBufferPtrs.add(retBufPtr);
      final Uint8List retBytes = retBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(retBufPtr.ref.data.asTypedList(retBufPtr.ref.len));
      final decodedValue = utf8.decode(retBytes);
      return decodedValue;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _echoMapFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_echo_map');

  Map<String, int> echoMap(Map<String, int> m) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(3);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(7);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final mWriter = _UniFfiBinaryWriter();
      mWriter.writeI32(m.length);
      for (final entry in m.entries) {
        mWriter.writeString(entry.key);
        mWriter.writeI32(entry.value);
      }
      final Uint8List mBytes = mWriter.toBytes();
      final ffi.Pointer<ffi.Uint8> mPtr = mBytes.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(mBytes.length);
      if (mBytes.isNotEmpty) { mPtr.asTypedList(mBytes.length).setAll(0, mBytes); }
      foreignArgPtrs.add(mPtr);
      final ffi.Pointer<_UniFfiRustCallStatus> mFromBytesStatusPtr = calloc<_UniFfiRustCallStatus>();
      mFromBytesStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
      mFromBytesStatusPtr.ref.errorBuf
        ..capacity = 0
        ..len = 0
        ..data = ffi.nullptr;
      final ffi.Pointer<_UniFfiForeignBytes> mForeignPtr = calloc<_UniFfiForeignBytes>();
      mForeignPtr.ref
        ..len = mBytes.length
        ..data = mPtr;
      final _UniFfiRustBuffer mRustBuffer = _uniFfiRustBufferFromBytes(mForeignPtr.ref, mFromBytesStatusPtr);
      calloc.free(mForeignPtr);
      final int mFromBytesCode = mFromBytesStatusPtr.ref.code;
      final _UniFfiRustBuffer mFromBytesErrBuf = mFromBytesStatusPtr.ref.errorBuf;
      calloc.free(mFromBytesStatusPtr);
      if (mFromBytesCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> mFromBytesErrBufPtr = calloc<_UniFfiRustBuffer>();
        mFromBytesErrBufPtr.ref
          ..capacity = mFromBytesErrBuf.capacity
          ..len = mFromBytesErrBuf.len
          ..data = mFromBytesErrBuf.data;
        rustRetBufferPtrs.add(mFromBytesErrBufPtr);
        throw StateError('UniFFI rustbuffer_from_bytes failed with status $mFromBytesCode');
      }
      (argBuf + 0).ref.u64 = mRustBuffer.capacity;
      (argBuf + 1).ref.u64 = mRustBuffer.len;
      (argBuf + 2).ref.ptr = mRustBuffer.data.cast<ffi.Void>();
      _echoMapFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 3).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 4).ref.u64
          ..len = (returnBuf + 5).ref.u64
          ..data = (returnBuf + 6).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      final ffi.Pointer<_UniFfiRustBuffer> retBufPtr = calloc<_UniFfiRustBuffer>();
      retBufPtr.ref
        ..capacity = (returnBuf + 0).ref.u64
        ..len = (returnBuf + 1).ref.u64
        ..data = (returnBuf + 2).ref.ptr.cast<ffi.Uint8>();
      rustRetBufferPtrs.add(retBufPtr);
      final Uint8List retBytes = retBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(retBufPtr.ref.data.asTypedList(retBufPtr.ref.len));
      final _UniFfiBinaryReader retReader = _UniFfiBinaryReader(retBytes);
      final decodedValue = (() { final int __len = retReader.readI32(); final out = <String, int>{}; for (var i = 0; i < __len; i++) { final key = retReader.readString(); final value = retReader.readI32(); out[key] = value; } return out; })();
      if (!retReader.isDone) {
        throw StateError('extra bytes remaining while decoding UniFFI ffibuffer return payload');
      }
      return decodedValue;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _echoStringsFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_echo_strings');

  List<String> echoStrings(List<String> v) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(3);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(7);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final vWriter = _UniFfiBinaryWriter();
      vWriter.writeI32(v.length);
      for (final item in v) {
        vWriter.writeString(item);
      }
      final Uint8List vBytes = vWriter.toBytes();
      final ffi.Pointer<ffi.Uint8> vPtr = vBytes.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(vBytes.length);
      if (vBytes.isNotEmpty) { vPtr.asTypedList(vBytes.length).setAll(0, vBytes); }
      foreignArgPtrs.add(vPtr);
      final ffi.Pointer<_UniFfiRustCallStatus> vFromBytesStatusPtr = calloc<_UniFfiRustCallStatus>();
      vFromBytesStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
      vFromBytesStatusPtr.ref.errorBuf
        ..capacity = 0
        ..len = 0
        ..data = ffi.nullptr;
      final ffi.Pointer<_UniFfiForeignBytes> vForeignPtr = calloc<_UniFfiForeignBytes>();
      vForeignPtr.ref
        ..len = vBytes.length
        ..data = vPtr;
      final _UniFfiRustBuffer vRustBuffer = _uniFfiRustBufferFromBytes(vForeignPtr.ref, vFromBytesStatusPtr);
      calloc.free(vForeignPtr);
      final int vFromBytesCode = vFromBytesStatusPtr.ref.code;
      final _UniFfiRustBuffer vFromBytesErrBuf = vFromBytesStatusPtr.ref.errorBuf;
      calloc.free(vFromBytesStatusPtr);
      if (vFromBytesCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> vFromBytesErrBufPtr = calloc<_UniFfiRustBuffer>();
        vFromBytesErrBufPtr.ref
          ..capacity = vFromBytesErrBuf.capacity
          ..len = vFromBytesErrBuf.len
          ..data = vFromBytesErrBuf.data;
        rustRetBufferPtrs.add(vFromBytesErrBufPtr);
        throw StateError('UniFFI rustbuffer_from_bytes failed with status $vFromBytesCode');
      }
      (argBuf + 0).ref.u64 = vRustBuffer.capacity;
      (argBuf + 1).ref.u64 = vRustBuffer.len;
      (argBuf + 2).ref.ptr = vRustBuffer.data.cast<ffi.Void>();
      _echoStringsFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 3).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 4).ref.u64
          ..len = (returnBuf + 5).ref.u64
          ..data = (returnBuf + 6).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      final ffi.Pointer<_UniFfiRustBuffer> retBufPtr = calloc<_UniFfiRustBuffer>();
      retBufPtr.ref
        ..capacity = (returnBuf + 0).ref.u64
        ..len = (returnBuf + 1).ref.u64
        ..data = (returnBuf + 2).ref.ptr.cast<ffi.Uint8>();
      rustRetBufferPtrs.add(retBufPtr);
      final Uint8List retBytes = retBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(retBufPtr.ref.data.asTypedList(retBufPtr.ref.len));
      final _UniFfiBinaryReader retReader = _UniFfiBinaryReader(retBytes);
      final decodedValue = (() { final int __len = retReader.readI32(); final out = <String>[]; for (var i = 0; i < __len; i++) { out.add(retReader.readString()); } return out; })();
      if (!retReader.isDone) {
        throw StateError('extra bytes remaining while decoding UniFFI ffibuffer return payload');
      }
      return decodedValue;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _greetFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_greet');

  String greet(String name) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(3);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(7);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final Uint8List nameBytes = Uint8List.fromList(utf8.encode(name));
      final ffi.Pointer<ffi.Uint8> namePtr = nameBytes.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(nameBytes.length);
      if (nameBytes.isNotEmpty) { namePtr.asTypedList(nameBytes.length).setAll(0, nameBytes); }
      foreignArgPtrs.add(namePtr);
      final ffi.Pointer<_UniFfiRustCallStatus> nameFromBytesStatusPtr = calloc<_UniFfiRustCallStatus>();
      nameFromBytesStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
      nameFromBytesStatusPtr.ref.errorBuf
        ..capacity = 0
        ..len = 0
        ..data = ffi.nullptr;
      final ffi.Pointer<_UniFfiForeignBytes> nameForeignPtr = calloc<_UniFfiForeignBytes>();
      nameForeignPtr.ref
        ..len = nameBytes.length
        ..data = namePtr;
      final _UniFfiRustBuffer nameRustBuffer = _uniFfiRustBufferFromBytes(nameForeignPtr.ref, nameFromBytesStatusPtr);
      calloc.free(nameForeignPtr);
      final int nameFromBytesCode = nameFromBytesStatusPtr.ref.code;
      final _UniFfiRustBuffer nameFromBytesErrBuf = nameFromBytesStatusPtr.ref.errorBuf;
      calloc.free(nameFromBytesStatusPtr);
      if (nameFromBytesCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> nameFromBytesErrBufPtr = calloc<_UniFfiRustBuffer>();
        nameFromBytesErrBufPtr.ref
          ..capacity = nameFromBytesErrBuf.capacity
          ..len = nameFromBytesErrBuf.len
          ..data = nameFromBytesErrBuf.data;
        rustRetBufferPtrs.add(nameFromBytesErrBufPtr);
        throw StateError('UniFFI rustbuffer_from_bytes failed with status $nameFromBytesCode');
      }
      (argBuf + 0).ref.u64 = nameRustBuffer.capacity;
      (argBuf + 1).ref.u64 = nameRustBuffer.len;
      (argBuf + 2).ref.ptr = nameRustBuffer.data.cast<ffi.Void>();
      _greetFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 3).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 4).ref.u64
          ..len = (returnBuf + 5).ref.u64
          ..data = (returnBuf + 6).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      final ffi.Pointer<_UniFfiRustBuffer> retBufPtr = calloc<_UniFfiRustBuffer>();
      retBufPtr.ref
        ..capacity = (returnBuf + 0).ref.u64
        ..len = (returnBuf + 1).ref.u64
        ..data = (returnBuf + 2).ref.ptr.cast<ffi.Uint8>();
      rustRetBufferPtrs.add(retBufPtr);
      final Uint8List retBytes = retBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(retBufPtr.ref.data.asTypedList(retBufPtr.ref.len));
      final decodedValue = utf8.decode(retBytes);
      return decodedValue;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _greetAsyncFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_greet_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _greetAsyncFfiBufferRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('ffi_uniffi_library_mode_demo_rust_future_poll_rust_buffer');
  late final void Function(int handle) _greetAsyncFfiBufferRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('ffi_uniffi_library_mode_demo_rust_future_cancel_rust_buffer');
  late final _UniFfiRustBuffer Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus) _greetAsyncFfiBufferRustFutureComplete = _lib.lookupFunction<_UniFfiRustBuffer Function(ffi.Uint64 handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus), _UniFfiRustBuffer Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus)>('ffi_uniffi_library_mode_demo_rust_future_complete_rust_buffer');
  late final void Function(int handle) _greetAsyncFfiBufferRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('ffi_uniffi_library_mode_demo_rust_future_free_rust_buffer');

  Future<String> greetAsync(String name) async {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(3);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(5);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final Uint8List nameBytes = Uint8List.fromList(utf8.encode(name));
      final ffi.Pointer<ffi.Uint8> namePtr = nameBytes.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(nameBytes.length);
      if (nameBytes.isNotEmpty) { namePtr.asTypedList(nameBytes.length).setAll(0, nameBytes); }
      foreignArgPtrs.add(namePtr);
      final ffi.Pointer<_UniFfiRustCallStatus> nameFromBytesStatusPtr = calloc<_UniFfiRustCallStatus>();
      nameFromBytesStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
      nameFromBytesStatusPtr.ref.errorBuf
        ..capacity = 0
        ..len = 0
        ..data = ffi.nullptr;
      final ffi.Pointer<_UniFfiForeignBytes> nameForeignPtr = calloc<_UniFfiForeignBytes>();
      nameForeignPtr.ref
        ..len = nameBytes.length
        ..data = namePtr;
      final _UniFfiRustBuffer nameRustBuffer = _uniFfiRustBufferFromBytes(nameForeignPtr.ref, nameFromBytesStatusPtr);
      calloc.free(nameForeignPtr);
      final int nameFromBytesCode = nameFromBytesStatusPtr.ref.code;
      final _UniFfiRustBuffer nameFromBytesErrBuf = nameFromBytesStatusPtr.ref.errorBuf;
      calloc.free(nameFromBytesStatusPtr);
      if (nameFromBytesCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> nameFromBytesErrBufPtr = calloc<_UniFfiRustBuffer>();
        nameFromBytesErrBufPtr.ref
          ..capacity = nameFromBytesErrBuf.capacity
          ..len = nameFromBytesErrBuf.len
          ..data = nameFromBytesErrBuf.data;
        rustRetBufferPtrs.add(nameFromBytesErrBufPtr);
        throw StateError('UniFFI rustbuffer_from_bytes failed with status $nameFromBytesCode');
      }
      (argBuf + 0).ref.u64 = nameRustBuffer.capacity;
      (argBuf + 1).ref.u64 = nameRustBuffer.len;
      (argBuf + 2).ref.ptr = nameRustBuffer.data.cast<ffi.Void>();
      _greetAsyncFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 1).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 2).ref.u64
          ..len = (returnBuf + 3).ref.u64
          ..data = (returnBuf + 4).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer async start failed with status $statusCode');
      }
      final int futureHandle = (returnBuf + 0).ref.u64;
      final StreamController<int> pollEvents = StreamController<int>.broadcast();
      final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
        pollEvents.add(pollResult);
      });
      try {
        _greetAsyncFfiBufferRustFuturePoll(futureHandle, callback.nativeFunction, 0);
        while (true) {
          final int pollResult = await pollEvents.stream.first;
          if (pollResult == _rustFuturePollReady) {
            break;
          }
          if (pollResult == _rustFuturePollWake) {
            _greetAsyncFfiBufferRustFuturePoll(futureHandle, callback.nativeFunction, 0);
            continue;
          }
          throw StateError('Rust future poll returned invalid status for greet_async: $pollResult');
        }
        final ffi.Pointer<_UniFfiRustCallStatus> outStatusPtr = calloc<_UniFfiRustCallStatus>();
        outStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        outStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        try {
          final _UniFfiRustBuffer resultValue = _greetAsyncFfiBufferRustFutureComplete(futureHandle, outStatusPtr);
          final int completeStatusCode = outStatusPtr.ref.code;
          if (completeStatusCode == _uniFfiRustCallStatusSuccess) {
            final ffi.Pointer<_UniFfiRustBuffer> resultBufPtr = calloc<_UniFfiRustBuffer>();
            resultBufPtr.ref
              ..capacity = resultValue.capacity
              ..len = resultValue.len
              ..data = resultValue.data;
            rustRetBufferPtrs.add(resultBufPtr);
            final Uint8List resultBytes = resultBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(resultBufPtr.ref.data.asTypedList(resultBufPtr.ref.len));
            return utf8.decode(resultBytes);
          }
          if (completeStatusCode == _uniFfiRustCallStatusCancelled) {
            throw StateError('Rust future was cancelled for greet_async');
          }
          final _UniFfiRustBuffer errorBuf = outStatusPtr.ref.errorBuf;
          if (!(errorBuf.data == ffi.nullptr && errorBuf.len == 0 && errorBuf.capacity == 0)) {
            final ffi.Pointer<_UniFfiRustBuffer> errorBufPtr = calloc<_UniFfiRustBuffer>();
            errorBufPtr.ref
              ..capacity = errorBuf.capacity
              ..len = errorBuf.len
              ..data = errorBuf.data;
            rustRetBufferPtrs.add(errorBufPtr);
            final Uint8List errorBytes = errorBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(errorBufPtr.ref.data.asTypedList(errorBufPtr.ref.len));
            if (errorBytes.isNotEmpty) {
              throw StateError(utf8.decode(errorBytes, allowMalformed: true));
            }
          }
          throw StateError('Rust future failed for greet_async with status code: $completeStatusCode');
        } finally {
          calloc.free(outStatusPtr);
        }
      } catch (_) {
        _greetAsyncFfiBufferRustFutureCancel(futureHandle);
        rethrow;
      } finally {
        await pollEvents.close();
        callback.close();
        _greetAsyncFfiBufferRustFutureFree(futureHandle);
      }
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _makePointFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_make_point');

  Point makePoint(double x, double y) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(2);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(7);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      (argBuf + 0).ref.float64 = x;
      (argBuf + 1).ref.float64 = y;
      _makePointFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 3).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 4).ref.u64
          ..len = (returnBuf + 5).ref.u64
          ..data = (returnBuf + 6).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      final ffi.Pointer<_UniFfiRustBuffer> retBufPtr = calloc<_UniFfiRustBuffer>();
      retBufPtr.ref
        ..capacity = (returnBuf + 0).ref.u64
        ..len = (returnBuf + 1).ref.u64
        ..data = (returnBuf + 2).ref.ptr.cast<ffi.Uint8>();
      rustRetBufferPtrs.add(retBufPtr);
      final Uint8List retBytes = retBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(retBufPtr.ref.data.asTypedList(retBufPtr.ref.len));
      final decodedValue = _uniffiDecodePoint(retBytes);
      return decodedValue;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _maybeGreetFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_func_maybe_greet');

  String? maybeGreet(String? name) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(3);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(7);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final nameWriter = _UniFfiBinaryWriter();
      if (name == null) {
        nameWriter.writeI8(0);
      } else {
        nameWriter.writeI8(1);
        nameWriter.writeString(name!);
      }
      final Uint8List nameBytes = nameWriter.toBytes();
      final ffi.Pointer<ffi.Uint8> namePtr = nameBytes.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(nameBytes.length);
      if (nameBytes.isNotEmpty) { namePtr.asTypedList(nameBytes.length).setAll(0, nameBytes); }
      foreignArgPtrs.add(namePtr);
      final ffi.Pointer<_UniFfiRustCallStatus> nameFromBytesStatusPtr = calloc<_UniFfiRustCallStatus>();
      nameFromBytesStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
      nameFromBytesStatusPtr.ref.errorBuf
        ..capacity = 0
        ..len = 0
        ..data = ffi.nullptr;
      final ffi.Pointer<_UniFfiForeignBytes> nameForeignPtr = calloc<_UniFfiForeignBytes>();
      nameForeignPtr.ref
        ..len = nameBytes.length
        ..data = namePtr;
      final _UniFfiRustBuffer nameRustBuffer = _uniFfiRustBufferFromBytes(nameForeignPtr.ref, nameFromBytesStatusPtr);
      calloc.free(nameForeignPtr);
      final int nameFromBytesCode = nameFromBytesStatusPtr.ref.code;
      final _UniFfiRustBuffer nameFromBytesErrBuf = nameFromBytesStatusPtr.ref.errorBuf;
      calloc.free(nameFromBytesStatusPtr);
      if (nameFromBytesCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> nameFromBytesErrBufPtr = calloc<_UniFfiRustBuffer>();
        nameFromBytesErrBufPtr.ref
          ..capacity = nameFromBytesErrBuf.capacity
          ..len = nameFromBytesErrBuf.len
          ..data = nameFromBytesErrBuf.data;
        rustRetBufferPtrs.add(nameFromBytesErrBufPtr);
        throw StateError('UniFFI rustbuffer_from_bytes failed with status $nameFromBytesCode');
      }
      (argBuf + 0).ref.u64 = nameRustBuffer.capacity;
      (argBuf + 1).ref.u64 = nameRustBuffer.len;
      (argBuf + 2).ref.ptr = nameRustBuffer.data.cast<ffi.Void>();
      _maybeGreetFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 3).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 4).ref.u64
          ..len = (returnBuf + 5).ref.u64
          ..data = (returnBuf + 6).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      final ffi.Pointer<_UniFfiRustBuffer> retBufPtr = calloc<_UniFfiRustBuffer>();
      retBufPtr.ref
        ..capacity = (returnBuf + 0).ref.u64
        ..len = (returnBuf + 1).ref.u64
        ..data = (returnBuf + 2).ref.ptr.cast<ffi.Uint8>();
      rustRetBufferPtrs.add(retBufPtr);
      final Uint8List retBytes = retBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(retBufPtr.ref.data.asTypedList(retBufPtr.ref.len));
      final _UniFfiBinaryReader retReader = _UniFfiBinaryReader(retBytes);
      final decodedValue = (() { final int __tag = retReader.readI8(); if (__tag == 0) return null; if (__tag != 1) throw StateError('invalid optional tag: $__tag'); return retReader.readString(); })();
      if (!retReader.isDone) {
        throw StateError('extra bytes remaining while decoding UniFFI ffibuffer return payload');
      }
      return decodedValue;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus) _counterFreeRaw = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus), void Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus)>('uniffi_uniffi_library_mode_demo_fn_free_counter');
  late final void Function(int handle) _counterFree = (int handle) {
    final statusPtr = calloc<_UniFfiRustCallStatus>();
    statusPtr.ref.code = _uniFfiRustCallStatusSuccess;
    statusPtr.ref.errorBuf
      ..capacity = 0
      ..len = 0
      ..data = ffi.nullptr;
    _counterFreeRaw(handle, statusPtr);
    calloc.free(statusPtr);
  };

  late final int Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus) _counterClone = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus), int Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus)>('uniffi_uniffi_library_mode_demo_fn_clone_counter');

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _counterCtorNewFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_constructor_counter_new');

  Counter counterCreateNew(int initial) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(1);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(5);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      (argBuf + 0).ref.i32 = initial;
      _counterCtorNewFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 1).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 2).ref.u64
          ..len = (returnBuf + 3).ref.u64
          ..data = (returnBuf + 4).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      final int handle = (returnBuf + 0).ref.u64;
      return Counter._(this, handle);
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _counterAsyncDivideFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_method_counter_async_divide');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _counterAsyncDivideFfiBufferRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('ffi_uniffi_library_mode_demo_rust_future_poll_i32');
  late final void Function(int handle) _counterAsyncDivideFfiBufferRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('ffi_uniffi_library_mode_demo_rust_future_cancel_i32');
  late final int Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus) _counterAsyncDivideFfiBufferRustFutureComplete = _lib.lookupFunction<ffi.Int32 Function(ffi.Uint64 handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus), int Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus)>('ffi_uniffi_library_mode_demo_rust_future_complete_i32');
  late final void Function(int handle) _counterAsyncDivideFfiBufferRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('ffi_uniffi_library_mode_demo_rust_future_free_i32');

  Future<int> counterInvokeAsyncDivide(int handle, int divisor) async {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(2);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(5);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final int clonedHandle;
      {
        final cloneStatusPtr = calloc<_UniFfiRustCallStatus>();
        try {
          cloneStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
          cloneStatusPtr.ref.errorBuf
            ..capacity = 0
            ..len = 0
            ..data = ffi.nullptr;
          clonedHandle = _counterClone(handle, cloneStatusPtr);
          if (cloneStatusPtr.ref.code != _uniFfiRustCallStatusSuccess) {
            throw StateError('UniFFI clone failed with status ${cloneStatusPtr.ref.code}');
          }
        } finally {
          calloc.free(cloneStatusPtr);
        }
      }
      (argBuf + 0).ref.u64 = clonedHandle;
      (argBuf + 1).ref.i32 = divisor;
      _counterAsyncDivideFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 1).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 2).ref.u64
          ..len = (returnBuf + 3).ref.u64
          ..data = (returnBuf + 4).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer async start failed with status $statusCode');
      }
      final int futureHandle = (returnBuf + 0).ref.u64;
      final StreamController<int> pollEvents = StreamController<int>.broadcast();
      final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
        pollEvents.add(pollResult);
      });
      try {
        _counterAsyncDivideFfiBufferRustFuturePoll(futureHandle, callback.nativeFunction, 0);
        while (true) {
          final int pollResult = await pollEvents.stream.first;
          if (pollResult == _rustFuturePollReady) {
            break;
          }
          if (pollResult == _rustFuturePollWake) {
            _counterAsyncDivideFfiBufferRustFuturePoll(futureHandle, callback.nativeFunction, 0);
            continue;
          }
          throw StateError('Rust future poll returned invalid status for async_divide: $pollResult');
        }
        final ffi.Pointer<_UniFfiRustCallStatus> outStatusPtr = calloc<_UniFfiRustCallStatus>();
        outStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        outStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        try {
          final int resultValue = _counterAsyncDivideFfiBufferRustFutureComplete(futureHandle, outStatusPtr);
          final int completeStatusCode = outStatusPtr.ref.code;
          if (completeStatusCode == _uniFfiRustCallStatusSuccess) {
            return resultValue;
          }
          if (completeStatusCode == _uniFfiRustCallStatusCancelled) {
            throw StateError('Rust future was cancelled for async_divide');
          }
          final _UniFfiRustBuffer errorBuf = outStatusPtr.ref.errorBuf;
          if (!(errorBuf.data == ffi.nullptr && errorBuf.len == 0 && errorBuf.capacity == 0)) {
            final ffi.Pointer<_UniFfiRustBuffer> errorBufPtr = calloc<_UniFfiRustBuffer>();
            errorBufPtr.ref
              ..capacity = errorBuf.capacity
              ..len = errorBuf.len
              ..data = errorBuf.data;
            rustRetBufferPtrs.add(errorBufPtr);
            final Uint8List errorBytes = errorBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(errorBufPtr.ref.data.asTypedList(errorBufPtr.ref.len));
            if (completeStatusCode == _uniFfiRustCallStatusError && errorBytes.isNotEmpty) {
              throw _uniffiLiftArithErrorException(errorBytes);
            }
            if (errorBytes.isNotEmpty) {
              throw StateError(utf8.decode(errorBytes, allowMalformed: true));
            }
          }
          throw StateError('Rust future failed for async_divide with status code: $completeStatusCode');
        } finally {
          calloc.free(outStatusPtr);
        }
      } catch (_) {
        _counterAsyncDivideFfiBufferRustFutureCancel(futureHandle);
        rethrow;
      } finally {
        await pollEvents.close();
        callback.close();
        _counterAsyncDivideFfiBufferRustFutureFree(futureHandle);
      }
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _counterAsyncGetFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_method_counter_async_get');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _counterAsyncGetFfiBufferRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('ffi_uniffi_library_mode_demo_rust_future_poll_i32');
  late final void Function(int handle) _counterAsyncGetFfiBufferRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('ffi_uniffi_library_mode_demo_rust_future_cancel_i32');
  late final int Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus) _counterAsyncGetFfiBufferRustFutureComplete = _lib.lookupFunction<ffi.Int32 Function(ffi.Uint64 handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus), int Function(int handle, ffi.Pointer<_UniFfiRustCallStatus> outStatus)>('ffi_uniffi_library_mode_demo_rust_future_complete_i32');
  late final void Function(int handle) _counterAsyncGetFfiBufferRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('ffi_uniffi_library_mode_demo_rust_future_free_i32');

  Future<int> counterInvokeAsyncGet(int handle) async {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(1);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(5);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final int clonedHandle;
      {
        final cloneStatusPtr = calloc<_UniFfiRustCallStatus>();
        try {
          cloneStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
          cloneStatusPtr.ref.errorBuf
            ..capacity = 0
            ..len = 0
            ..data = ffi.nullptr;
          clonedHandle = _counterClone(handle, cloneStatusPtr);
          if (cloneStatusPtr.ref.code != _uniFfiRustCallStatusSuccess) {
            throw StateError('UniFFI clone failed with status ${cloneStatusPtr.ref.code}');
          }
        } finally {
          calloc.free(cloneStatusPtr);
        }
      }
      (argBuf + 0).ref.u64 = clonedHandle;
      _counterAsyncGetFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 1).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 2).ref.u64
          ..len = (returnBuf + 3).ref.u64
          ..data = (returnBuf + 4).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer async start failed with status $statusCode');
      }
      final int futureHandle = (returnBuf + 0).ref.u64;
      final StreamController<int> pollEvents = StreamController<int>.broadcast();
      final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
        pollEvents.add(pollResult);
      });
      try {
        _counterAsyncGetFfiBufferRustFuturePoll(futureHandle, callback.nativeFunction, 0);
        while (true) {
          final int pollResult = await pollEvents.stream.first;
          if (pollResult == _rustFuturePollReady) {
            break;
          }
          if (pollResult == _rustFuturePollWake) {
            _counterAsyncGetFfiBufferRustFuturePoll(futureHandle, callback.nativeFunction, 0);
            continue;
          }
          throw StateError('Rust future poll returned invalid status for async_get: $pollResult');
        }
        final ffi.Pointer<_UniFfiRustCallStatus> outStatusPtr = calloc<_UniFfiRustCallStatus>();
        outStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        outStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        try {
          final int resultValue = _counterAsyncGetFfiBufferRustFutureComplete(futureHandle, outStatusPtr);
          final int completeStatusCode = outStatusPtr.ref.code;
          if (completeStatusCode == _uniFfiRustCallStatusSuccess) {
            return resultValue;
          }
          if (completeStatusCode == _uniFfiRustCallStatusCancelled) {
            throw StateError('Rust future was cancelled for async_get');
          }
          final _UniFfiRustBuffer errorBuf = outStatusPtr.ref.errorBuf;
          if (!(errorBuf.data == ffi.nullptr && errorBuf.len == 0 && errorBuf.capacity == 0)) {
            final ffi.Pointer<_UniFfiRustBuffer> errorBufPtr = calloc<_UniFfiRustBuffer>();
            errorBufPtr.ref
              ..capacity = errorBuf.capacity
              ..len = errorBuf.len
              ..data = errorBuf.data;
            rustRetBufferPtrs.add(errorBufPtr);
            final Uint8List errorBytes = errorBufPtr.ref.len == 0 ? Uint8List(0) : Uint8List.fromList(errorBufPtr.ref.data.asTypedList(errorBufPtr.ref.len));
            if (errorBytes.isNotEmpty) {
              throw StateError(utf8.decode(errorBytes, allowMalformed: true));
            }
          }
          throw StateError('Rust future failed for async_get with status code: $completeStatusCode');
        } finally {
          calloc.free(outStatusPtr);
        }
      } catch (_) {
        _counterAsyncGetFfiBufferRustFutureCancel(futureHandle);
        rethrow;
      } finally {
        await pollEvents.close();
        callback.close();
        _counterAsyncGetFfiBufferRustFutureFree(futureHandle);
      }
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _counterGetFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_method_counter_get');

  int counterInvokeGet(int handle) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(1);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(5);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final int clonedHandle;
      {
        final cloneStatusPtr = calloc<_UniFfiRustCallStatus>();
        try {
          cloneStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
          cloneStatusPtr.ref.errorBuf
            ..capacity = 0
            ..len = 0
            ..data = ffi.nullptr;
          clonedHandle = _counterClone(handle, cloneStatusPtr);
          if (cloneStatusPtr.ref.code != _uniFfiRustCallStatusSuccess) {
            throw StateError('UniFFI clone failed with status ${cloneStatusPtr.ref.code}');
          }
        } finally {
          calloc.free(cloneStatusPtr);
        }
      }
      (argBuf + 0).ref.u64 = clonedHandle;
      _counterGetFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 1).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 2).ref.u64
          ..len = (returnBuf + 3).ref.u64
          ..data = (returnBuf + 4).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      return (returnBuf + 0).ref.i32;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }

  late final void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr) _counterIncrementFfiBuffer = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr), void Function(ffi.Pointer<_UniFfiFfiBufferElement> argPtr, ffi.Pointer<_UniFfiFfiBufferElement> returnPtr)>('uniffi_ffibuffer_uniffi_library_mode_demo_fn_method_counter_increment');

  void counterInvokeIncrement(int handle) {
    final ffi.Pointer<_UniFfiFfiBufferElement> argBuf = calloc<_UniFfiFfiBufferElement>(1);
    final ffi.Pointer<_UniFfiFfiBufferElement> returnBuf = calloc<_UniFfiFfiBufferElement>(4);
    final foreignArgPtrs = <ffi.Pointer<ffi.Uint8>>[];
    final rustRetBufferPtrs = <ffi.Pointer<_UniFfiRustBuffer>>[];
    try {
      final int clonedHandle;
      {
        final cloneStatusPtr = calloc<_UniFfiRustCallStatus>();
        try {
          cloneStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
          cloneStatusPtr.ref.errorBuf
            ..capacity = 0
            ..len = 0
            ..data = ffi.nullptr;
          clonedHandle = _counterClone(handle, cloneStatusPtr);
          if (cloneStatusPtr.ref.code != _uniFfiRustCallStatusSuccess) {
            throw StateError('UniFFI clone failed with status ${cloneStatusPtr.ref.code}');
          }
        } finally {
          calloc.free(cloneStatusPtr);
        }
      }
      (argBuf + 0).ref.u64 = clonedHandle;
      _counterIncrementFfiBuffer(argBuf, returnBuf);
      final int statusCode = (returnBuf + 0).ref.i8;
      if (statusCode != _uniFfiRustCallStatusSuccess) {
        final ffi.Pointer<_UniFfiRustBuffer> errBufPtr = calloc<_UniFfiRustBuffer>();
        errBufPtr.ref
          ..capacity = (returnBuf + 1).ref.u64
          ..len = (returnBuf + 2).ref.u64
          ..data = (returnBuf + 3).ref.ptr.cast<ffi.Uint8>();
        rustRetBufferPtrs.add(errBufPtr);
        throw StateError('UniFFI ffibuffer call failed with status $statusCode');
      }
      return;
    } finally {
      for (final ptr in foreignArgPtrs) {
        if (ptr != ffi.nullptr) {
          calloc.free(ptr);
        }
      }
      for (final bufPtr in rustRetBufferPtrs) {
        if (bufPtr.ref.data == ffi.nullptr && bufPtr.ref.len == 0 && bufPtr.ref.capacity == 0) {
          continue;
        }
        final ffi.Pointer<_UniFfiRustCallStatus> freeStatusPtr = calloc<_UniFfiRustCallStatus>();
        freeStatusPtr.ref.code = _uniFfiRustCallStatusSuccess;
        freeStatusPtr.ref.errorBuf
          ..capacity = 0
          ..len = 0
          ..data = ffi.nullptr;
        _uniFfiRustBufferFree(bufPtr.ref, freeStatusPtr);
        calloc.free(freeStatusPtr);
        calloc.free(bufPtr);
      }
      calloc.free(argBuf);
      calloc.free(returnBuf);
    }
  }
}

final class _CounterFinalizerToken {
  const _CounterFinalizerToken(this.free, this.handle);
  final void Function(int) free;
  final int handle;
}

final class Counter {
  Counter._(this._ffi, this._handle) {
    _finalizer.attach(this, _CounterFinalizerToken(_ffi._counterFree, _handle), detach: this);
  }

  final LibraryModeDemoFfi _ffi;
  int _handle;
  bool _closed = false;

  static final Finalizer<_CounterFinalizerToken> _finalizer = Finalizer((token) {
    token.free(token.handle);
  });

  bool get isClosed => _closed;

  void close() {
    if (_closed) {
      return;
    }
    _closed = true;
    _finalizer.detach(this);
    _ffi._counterFree(_handle);
  }

  void _ensureOpen() {
    if (_closed) {
      throw StateError('Counter is closed');
    }
  }

  static Counter create(int initial) {
    return _bindings().counterCreateNew(initial);
  }

  Future<int> asyncDivide(int divisor) {
    _ensureOpen();
    return _ffi.counterInvokeAsyncDivide(_handle, divisor);
  }

  Future<int> asyncGet() {
    _ensureOpen();
    return _ffi.counterInvokeAsyncGet(_handle);
  }

  int get_() {
    _ensureOpen();
    return _ffi.counterInvokeGet(_handle);
  }

  void increment() {
    _ensureOpen();
    _ffi.counterInvokeIncrement(_handle);
  }

}

final class CounterFfiCodec {
  const CounterFfiCodec._();

  static int lower(Counter value) => value._handle;

  static Counter lift(int handle) => Counter._(_bindings(), handle);
}

LibraryModeDemoFfi? _defaultBindings;

LibraryModeDemoFfi _bindings() => _defaultBindings ??= LibraryModeDemoFfi();

void configureDefaultBindings({ffi.DynamicLibrary? dynamicLibrary, String? libraryPath}) {
  _defaultBindings = LibraryModeDemoFfi(dynamicLibrary: dynamicLibrary, libraryPath: libraryPath);
}

void resetDefaultBindings() {
  _defaultBindings = null;
}

String describeShape(Shape shape) {
  return _bindings().describeShape(shape);
}

int divide(int a, int b) {
  return _bindings().divide(a, b);
}

Future<int> divideAsync(int a, int b) {
  return _bindings().divideAsync(a, b);
}

String echoLabel(String label) {
  return _bindings().echoLabel(label);
}

Map<String, int> echoMap(Map<String, int> m) {
  return _bindings().echoMap(m);
}

List<String> echoStrings(List<String> v) {
  return _bindings().echoStrings(v);
}

String greet(String name) {
  return _bindings().greet(name);
}

Future<String> greetAsync(String name) {
  return _bindings().greetAsync(name);
}

Point makePoint(double x, double y) {
  return _bindings().makePoint(x, y);
}

String? maybeGreet(String? name) {
  return _bindings().maybeGreet(name);
}

