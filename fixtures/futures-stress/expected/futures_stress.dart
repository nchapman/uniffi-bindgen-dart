// Generated by uniffi-bindgen-dart. DO NOT EDIT.
// ignore_for_file: unused_element
library futures_stress;

import 'dart:async';
import 'dart:convert';
import 'dart:ffi' as ffi;
import 'package:ffi/ffi.dart';
import 'dart:typed_data';

final class _RustBuffer extends ffi.Struct {
  external ffi.Pointer<ffi.Uint8> data;

  @ffi.Uint64()
  external int len;
}

final class _RustBufferOpt extends ffi.Struct {
  @ffi.Uint8()
  external int isSome;

  external _RustBuffer value;
}

final class _RustBufferVec extends ffi.Struct {
  external ffi.Pointer<_RustBuffer> data;

  @ffi.Uint64()
  external int len;
}

final class _RustCallStatus extends ffi.Struct {
  @ffi.Int8()
  external int code;

  external ffi.Pointer<Utf8> errorBuf;
}

const int _rustCallStatusSuccess = 0;
const int _rustCallStatusError = 1;
const int _rustCallStatusUnexpectedError = 2;
const int _rustCallStatusCancelled = 3;
const int _rustFuturePollReady = 0;
const int _rustFuturePollWake = 1;

sealed class AsyncError {
  const AsyncError();
}

final class AsyncErrorNotFound extends AsyncError {
  const AsyncErrorNotFound();
}

final class AsyncErrorInvalidInput extends AsyncError {
  const AsyncErrorInvalidInput();
}

sealed class AsyncErrorException implements Exception {
  const AsyncErrorException();
}

final class AsyncErrorExceptionNotFound extends AsyncErrorException {
  const AsyncErrorExceptionNotFound();
}

final class AsyncErrorExceptionInvalidInput extends AsyncErrorException {
  const AsyncErrorExceptionInvalidInput();
}

String _encodeAsyncError(AsyncError value) {
  if (value is AsyncErrorNotFound) {
    return jsonEncode({
      'tag': 'notFound',
    });
  }
  if (value is AsyncErrorInvalidInput) {
    return jsonEncode({
      'tag': 'invalidInput',
    });
  }
  throw StateError('Unknown AsyncError variant instance: $value');
}

AsyncError _decodeAsyncError(String raw) {
  final Map<String, dynamic> map = jsonDecode(raw) as Map<String, dynamic>;
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'notFound':
      return AsyncErrorNotFound(
      );
    case 'invalidInput':
      return AsyncErrorInvalidInput(
      );
    default:
      throw StateError('Unknown AsyncError variant tag: $tag');
  }
}

String _encodeAsyncErrorException(AsyncErrorException value) {
  if (value is AsyncErrorExceptionNotFound) {
    return jsonEncode({
      'tag': 'notFound',
    });
  }
  if (value is AsyncErrorExceptionInvalidInput) {
    return jsonEncode({
      'tag': 'invalidInput',
    });
  }
  throw StateError('Unknown AsyncErrorException exception instance: $value');
}

AsyncErrorException _decodeAsyncErrorException(Object? raw) {
  final Map<String, dynamic> map = raw is String ? (jsonDecode(raw) as Map<String, dynamic>) : (raw as Map<String, dynamic>);
  final String? tag = map['tag'] as String?;
  switch (tag) {
    case 'notFound':
      return const AsyncErrorExceptionNotFound();
    case 'invalidInput':
      return const AsyncErrorExceptionInvalidInput();
    default:
      throw StateError('Unknown AsyncErrorException exception tag: $tag');
  }
}

final class AsyncErrorFfiCodec {
  const AsyncErrorFfiCodec._();

  static String encode(AsyncError value) => _encodeAsyncError(value);

  static AsyncError decode(String raw) => _decodeAsyncError(raw);
}

final class AsyncErrorExceptionFfiCodec {
  const AsyncErrorExceptionFfiCodec._();

  static String encode(AsyncErrorException value) => _encodeAsyncErrorException(value);

  static AsyncErrorException decode(Object? raw) => _decodeAsyncErrorException(raw);
}

class FuturesStressFfi {
  FuturesStressFfi({ffi.DynamicLibrary? dynamicLibrary, String? libraryPath})
      : _dynamicLibrary = dynamicLibrary,
        _libraryPath = libraryPath;

  final ffi.DynamicLibrary? _dynamicLibrary;
  final String? _libraryPath;

  static const String libraryName = 'uniffi_futures_stress';

  ffi.DynamicLibrary open() {
    final provided = _dynamicLibrary;
    if (provided != null) {
      return provided;
    }
    return ffi.DynamicLibrary.open(_libraryPath ?? libraryName);
  }

  late final ffi.DynamicLibrary _lib = open();

  late final void Function(ffi.Pointer<Utf8>) _rustStringFree = _lib.lookupFunction<ffi.Void Function(ffi.Pointer<Utf8>), void Function(ffi.Pointer<Utf8>)>('rust_string_free');

  late final void Function(_RustBuffer) _rustBytesFree = _lib.lookupFunction<ffi.Void Function(_RustBuffer), void Function(_RustBuffer)>('rust_bytes_free');

  late final void Function(_RustBufferVec) _rustBytesVecFree = _lib.lookupFunction<ffi.Void Function(_RustBufferVec), void Function(_RustBufferVec)>('rust_bytes_vec_free');

  late final int Function(int left, int right) _addAsync = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint32 left, ffi.Uint32 right), int Function(int left, int right)>('add_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _addAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_u32');
  late final void Function(int handle) _addAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_u32');
  late final int Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _addAsyncRustFutureComplete = _lib.lookupFunction<ffi.Uint32 Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), int Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_u32');
  late final void Function(int handle) _addAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_u32');

  Future<int> addAsync(int left, int right) async {
    final int futureHandle;
    futureHandle = _addAsync(left, right);
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _addAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _addAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for add_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final int resultValue = _addAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          return resultValue;
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for add_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for add_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _addAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _addAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(ffi.Pointer<Utf8> value) _assertNotEmptyAsync = _lib.lookupFunction<ffi.Uint64 Function(ffi.Pointer<Utf8> value), int Function(ffi.Pointer<Utf8> value)>('assert_not_empty_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _assertNotEmptyAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_void');
  late final void Function(int handle) _assertNotEmptyAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_void');
  late final void Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _assertNotEmptyAsyncRustFutureComplete = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), void Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_void');
  late final void Function(int handle) _assertNotEmptyAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_void');

  Future<void> assertNotEmptyAsync(String value) async {
    final ffi.Pointer<Utf8> valueNative = value.toNativeUtf8();
    final int futureHandle;
    try {
      futureHandle = _assertNotEmptyAsync(valueNative);
    } finally {
    calloc.free(valueNative);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _assertNotEmptyAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _assertNotEmptyAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for assert_not_empty_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        _assertNotEmptyAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          return;
        }
        if (statusCode == _rustCallStatusError) {
          final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
          if (errorPtr != ffi.nullptr) {
            try {
              final String errorPayload = errorPtr.toDartString();
              throw AsyncErrorExceptionFfiCodec.decode(jsonDecode(errorPayload));
            } finally {
              _rustStringFree(errorPtr);
            }
          }
          throw StateError('Rust async error without payload for assert_not_empty_async');
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for assert_not_empty_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for assert_not_empty_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _assertNotEmptyAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _assertNotEmptyAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(int left, int right) _checkedAddAsync = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint32 left, ffi.Uint32 right), int Function(int left, int right)>('checked_add_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _checkedAddAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_u32');
  late final void Function(int handle) _checkedAddAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_u32');
  late final int Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _checkedAddAsyncRustFutureComplete = _lib.lookupFunction<ffi.Uint32 Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), int Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_u32');
  late final void Function(int handle) _checkedAddAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_u32');

  Future<int> checkedAddAsync(int left, int right) async {
    final int futureHandle;
    futureHandle = _checkedAddAsync(left, right);
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _checkedAddAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _checkedAddAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for checked_add_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final int resultValue = _checkedAddAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          return resultValue;
        }
        if (statusCode == _rustCallStatusError) {
          final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
          if (errorPtr != ffi.nullptr) {
            try {
              final String errorPayload = errorPtr.toDartString();
              throw AsyncErrorExceptionFfiCodec.decode(jsonDecode(errorPayload));
            } finally {
              _rustStringFree(errorPtr);
            }
          }
          throw StateError('Rust async error without payload for checked_add_async');
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for checked_add_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for checked_add_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _checkedAddAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _checkedAddAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(_RustBufferVec input) _chunksEchoBytesAsync = _lib.lookupFunction<ffi.Uint64 Function(_RustBufferVec input), int Function(_RustBufferVec input)>('chunks_echo_bytes_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _chunksEchoBytesAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_bytes_vec');
  late final void Function(int handle) _chunksEchoBytesAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_bytes_vec');
  late final _RustBufferVec Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _chunksEchoBytesAsyncRustFutureComplete = _lib.lookupFunction<_RustBufferVec Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), _RustBufferVec Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_bytes_vec');
  late final void Function(int handle) _chunksEchoBytesAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_bytes_vec');

  Future<List<Uint8List>> chunksEchoBytesAsync(List<Uint8List> input) async {
    final ffi.Pointer<_RustBuffer> inputData = input.isEmpty ? ffi.nullptr : calloc<_RustBuffer>(input.length);
    if (inputData != ffi.nullptr) {
      for (var i = 0; i < input.length; i++) {
        final item = input[i];
        final ffi.Pointer<ffi.Uint8> itemData = item.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(item.length);
        if (itemData != ffi.nullptr) {
          itemData.asTypedList(item.length).setAll(0, item);
        }
        (inputData + i).ref
          ..data = itemData
          ..len = item.length;
      }
    }
    final ffi.Pointer<_RustBufferVec> inputVecPtr = calloc<_RustBufferVec>();
    inputVecPtr.ref
      ..data = inputData
      ..len = input.length;
    final _RustBufferVec inputNative = inputVecPtr.ref;
    final int futureHandle;
    try {
      futureHandle = _chunksEchoBytesAsync(inputNative);
    } finally {
    if (inputData != ffi.nullptr) {
      for (var i = 0; i < input.length; i++) {
        final data = (inputData + i).ref.data;
        if (data != ffi.nullptr) calloc.free(data);
      }
      calloc.free(inputData);
    }
    calloc.free(inputVecPtr);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _chunksEchoBytesAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _chunksEchoBytesAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for chunks_echo_bytes_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final _RustBufferVec resultValue = _chunksEchoBytesAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          final _RustBufferVec resultVec = resultValue;
          final ffi.Pointer<_RustBuffer> resultData = resultVec.data;
          final int resultLen = resultVec.len;
          if (resultData == ffi.nullptr) {
            if (resultLen == 0) {
              _rustBytesVecFree(resultVec);
              return <Uint8List>[];
            }
            throw StateError('Rust returned invalid byte vector for chunks_echo_bytes_async');
          }
          try {
            final out = <Uint8List>[];
            for (var i = 0; i < resultLen; i++) {
              final _RustBuffer item = (resultData + i).ref;
              final ffi.Pointer<ffi.Uint8> itemData = item.data;
              final int itemLen = item.len;
              if (itemData == ffi.nullptr) {
                if (itemLen == 0) {
                  out.add(Uint8List(0));
                  continue;
                }
                throw StateError('Rust returned invalid nested buffer for chunks_echo_bytes_async');
              }
              try {
                out.add(Uint8List.fromList(itemData.asTypedList(itemLen)));
              } finally {
                _rustBytesFree(item);
              }
            }
            return out;
          } finally {
            _rustBytesVecFree(resultVec);
          }
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for chunks_echo_bytes_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for chunks_echo_bytes_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _chunksEchoBytesAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _chunksEchoBytesAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(_RustBuffer input) _echoBytesAsync = _lib.lookupFunction<ffi.Uint64 Function(_RustBuffer input), int Function(_RustBuffer input)>('echo_bytes_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _echoBytesAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_bytes');
  late final void Function(int handle) _echoBytesAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_bytes');
  late final _RustBuffer Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _echoBytesAsyncRustFutureComplete = _lib.lookupFunction<_RustBuffer Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), _RustBuffer Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_bytes');
  late final void Function(int handle) _echoBytesAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_bytes');

  Future<Uint8List> echoBytesAsync(Uint8List input) async {
    final ffi.Pointer<ffi.Uint8> inputData = input.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(input.length);
    if (inputData != ffi.nullptr) {
      inputData.asTypedList(input.length).setAll(0, input);
    }
    final ffi.Pointer<_RustBuffer> inputBufferPtr = calloc<_RustBuffer>();
    inputBufferPtr.ref.data = inputData;
    inputBufferPtr.ref.len = input.length;
    final _RustBuffer inputNative = inputBufferPtr.ref;
    final int futureHandle;
    try {
      futureHandle = _echoBytesAsync(inputNative);
    } finally {
    if (inputData != ffi.nullptr) calloc.free(inputData);
    calloc.free(inputBufferPtr);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _echoBytesAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _echoBytesAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for echo_bytes_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final _RustBuffer resultValue = _echoBytesAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          final _RustBuffer resultBuf = resultValue;
          final ffi.Pointer<ffi.Uint8> resultData = resultBuf.data;
          final int resultLen = resultBuf.len;
          if (resultData == ffi.nullptr) {
            if (resultLen == 0) {
              _rustBytesFree(resultBuf);
              return Uint8List(0);
            }
            throw StateError('Rust returned invalid buffer for echo_bytes_async');
          }
          try {
            return Uint8List.fromList(resultData.asTypedList(resultLen));
          } finally {
            _rustBytesFree(resultBuf);
          }
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for echo_bytes_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for echo_bytes_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _echoBytesAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _echoBytesAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(_RustBuffer input) _echoBytesCheckedAsync = _lib.lookupFunction<ffi.Uint64 Function(_RustBuffer input), int Function(_RustBuffer input)>('echo_bytes_checked_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _echoBytesCheckedAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_bytes');
  late final void Function(int handle) _echoBytesCheckedAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_bytes');
  late final _RustBuffer Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _echoBytesCheckedAsyncRustFutureComplete = _lib.lookupFunction<_RustBuffer Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), _RustBuffer Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_bytes');
  late final void Function(int handle) _echoBytesCheckedAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_bytes');

  Future<Uint8List> echoBytesCheckedAsync(Uint8List input) async {
    final ffi.Pointer<ffi.Uint8> inputData = input.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(input.length);
    if (inputData != ffi.nullptr) {
      inputData.asTypedList(input.length).setAll(0, input);
    }
    final ffi.Pointer<_RustBuffer> inputBufferPtr = calloc<_RustBuffer>();
    inputBufferPtr.ref.data = inputData;
    inputBufferPtr.ref.len = input.length;
    final _RustBuffer inputNative = inputBufferPtr.ref;
    final int futureHandle;
    try {
      futureHandle = _echoBytesCheckedAsync(inputNative);
    } finally {
    if (inputData != ffi.nullptr) calloc.free(inputData);
    calloc.free(inputBufferPtr);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _echoBytesCheckedAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _echoBytesCheckedAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for echo_bytes_checked_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final _RustBuffer resultValue = _echoBytesCheckedAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          final _RustBuffer resultBuf = resultValue;
          final ffi.Pointer<ffi.Uint8> resultData = resultBuf.data;
          final int resultLen = resultBuf.len;
          if (resultData == ffi.nullptr) {
            if (resultLen == 0) {
              _rustBytesFree(resultBuf);
              return Uint8List(0);
            }
            throw StateError('Rust returned invalid buffer for echo_bytes_checked_async');
          }
          try {
            return Uint8List.fromList(resultData.asTypedList(resultLen));
          } finally {
            _rustBytesFree(resultBuf);
          }
        }
        {
          final _RustBuffer buf = resultValue;
          if (buf.len > 0 && buf.data != ffi.nullptr) {
            _rustBytesFree(buf);
          }
        }
        if (statusCode == _rustCallStatusError) {
          final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
          if (errorPtr != ffi.nullptr) {
            try {
              final String errorPayload = errorPtr.toDartString();
              throw AsyncErrorExceptionFfiCodec.decode(jsonDecode(errorPayload));
            } finally {
              _rustStringFree(errorPtr);
            }
          }
          throw StateError('Rust async error without payload for echo_bytes_checked_async');
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for echo_bytes_checked_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for echo_bytes_checked_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _echoBytesCheckedAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _echoBytesCheckedAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(ffi.Pointer<Utf8> name) _greetAsync = _lib.lookupFunction<ffi.Uint64 Function(ffi.Pointer<Utf8> name), int Function(ffi.Pointer<Utf8> name)>('greet_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _greetAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_string');
  late final void Function(int handle) _greetAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_string');
  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _greetAsyncRustFutureComplete = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_string');
  late final void Function(int handle) _greetAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_string');

  Future<String> greetAsync(String name) async {
    final ffi.Pointer<Utf8> nameNative = name.toNativeUtf8();
    final int futureHandle;
    try {
      futureHandle = _greetAsync(nameNative);
    } finally {
    calloc.free(nameNative);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _greetAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _greetAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for greet_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final ffi.Pointer<Utf8> resultPtr = _greetAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          if (resultPtr == ffi.nullptr) {
            throw StateError('Rust returned null for greet_async');
          }
          try {
            return resultPtr.toDartString();
          } finally {
            _rustStringFree(resultPtr);
          }
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for greet_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for greet_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _greetAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _greetAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(ffi.Pointer<Utf8> input) _labelEchoAsync = _lib.lookupFunction<ffi.Uint64 Function(ffi.Pointer<Utf8> input), int Function(ffi.Pointer<Utf8> input)>('label_echo_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _labelEchoAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_string');
  late final void Function(int handle) _labelEchoAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_string');
  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _labelEchoAsyncRustFutureComplete = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_string');
  late final void Function(int handle) _labelEchoAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_string');

  Future<String> labelEchoAsync(String input) async {
    final ffi.Pointer<Utf8> inputNative = input.toNativeUtf8();
    final int futureHandle;
    try {
      futureHandle = _labelEchoAsync(inputNative);
    } finally {
    calloc.free(inputNative);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _labelEchoAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _labelEchoAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for label_echo_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final ffi.Pointer<Utf8> resultPtr = _labelEchoAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          if (resultPtr == ffi.nullptr) {
            throw StateError('Rust returned null for label_echo_async');
          }
          try {
            return resultPtr.toDartString();
          } finally {
            _rustStringFree(resultPtr);
          }
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for label_echo_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for label_echo_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _labelEchoAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _labelEchoAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(_RustBufferOpt input) _maybeEchoBytesAsync = _lib.lookupFunction<ffi.Uint64 Function(_RustBufferOpt input), int Function(_RustBufferOpt input)>('maybe_echo_bytes_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _maybeEchoBytesAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_bytes_opt');
  late final void Function(int handle) _maybeEchoBytesAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_bytes_opt');
  late final _RustBufferOpt Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _maybeEchoBytesAsyncRustFutureComplete = _lib.lookupFunction<_RustBufferOpt Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), _RustBufferOpt Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_bytes_opt');
  late final void Function(int handle) _maybeEchoBytesAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_bytes_opt');

  Future<Uint8List?> maybeEchoBytesAsync(Uint8List? input) async {
    final bool inputIsSome = input != null;
    final Uint8List inputValue = input ?? Uint8List(0);
    final ffi.Pointer<ffi.Uint8> inputData = !inputIsSome || inputValue.isEmpty ? ffi.nullptr : calloc<ffi.Uint8>(inputValue.length);
    if (inputData != ffi.nullptr) {
      inputData.asTypedList(inputValue.length).setAll(0, inputValue);
    }
    final ffi.Pointer<_RustBuffer> inputBufferPtr = calloc<_RustBuffer>();
    inputBufferPtr.ref.data = inputData;
    inputBufferPtr.ref.len = inputIsSome ? inputValue.length : 0;
    final ffi.Pointer<_RustBufferOpt> inputOptPtr = calloc<_RustBufferOpt>();
    inputOptPtr.ref.isSome = inputIsSome ? 1 : 0;
    inputOptPtr.ref.value = inputBufferPtr.ref;
    final _RustBufferOpt inputNative = inputOptPtr.ref;
    final int futureHandle;
    try {
      futureHandle = _maybeEchoBytesAsync(inputNative);
    } finally {
    if (inputData != ffi.nullptr) calloc.free(inputData);
    calloc.free(inputBufferPtr);
    calloc.free(inputOptPtr);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _maybeEchoBytesAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _maybeEchoBytesAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for maybe_echo_bytes_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final _RustBufferOpt resultValue = _maybeEchoBytesAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          final _RustBufferOpt resultOpt = resultValue;
          if (resultOpt.isSome == 0) {
            return null;
          }
          final _RustBuffer resultBuf = resultOpt.value;
          final ffi.Pointer<ffi.Uint8> resultData = resultBuf.data;
          final int resultLen = resultBuf.len;
          if (resultData == ffi.nullptr) {
            if (resultLen == 0) {
              _rustBytesFree(resultBuf);
              return Uint8List(0);
            }
            throw StateError('Rust returned invalid optional buffer for maybe_echo_bytes_async');
          }
          try {
            return Uint8List.fromList(resultData.asTypedList(resultLen));
          } finally {
            _rustBytesFree(resultBuf);
          }
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for maybe_echo_bytes_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for maybe_echo_bytes_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _maybeEchoBytesAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _maybeEchoBytesAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(ffi.Pointer<Utf8> values) _summarizeAsync = _lib.lookupFunction<ffi.Uint64 Function(ffi.Pointer<Utf8> values), int Function(ffi.Pointer<Utf8> values)>('summarize_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _summarizeAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_string');
  late final void Function(int handle) _summarizeAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_string');
  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _summarizeAsyncRustFutureComplete = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_string');
  late final void Function(int handle) _summarizeAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_string');

  Future<Map<String, int>> summarizeAsync(Map<String, int> values) async {
    final String valuesNativeJson = jsonEncode(values.map((key, value) => MapEntry(key, value)));
    final ffi.Pointer<Utf8> valuesNative = valuesNativeJson.toNativeUtf8();
    final int futureHandle;
    try {
      futureHandle = _summarizeAsync(valuesNative);
    } finally {
    calloc.free(valuesNative);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _summarizeAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _summarizeAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for summarize_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final ffi.Pointer<Utf8> resultPtr = _summarizeAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          if (resultPtr == ffi.nullptr) {
            throw StateError('Rust returned null for summarize_async');
          }
          try {
            final String payload = resultPtr.toDartString();
            return (jsonDecode(payload) as Map<String, dynamic>).map((key, value) => MapEntry(key, (value as num).toInt()));
          } finally {
            _rustStringFree(resultPtr);
          }
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for summarize_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for summarize_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _summarizeAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _summarizeAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function() _tickAsync = _lib.lookupFunction<ffi.Uint64 Function(), int Function()>('tick_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _tickAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_void');
  late final void Function(int handle) _tickAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_void');
  late final void Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _tickAsyncRustFutureComplete = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), void Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_void');
  late final void Function(int handle) _tickAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_void');

  Future<void> tickAsync() async {
    final int futureHandle;
    futureHandle = _tickAsync();
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _tickAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _tickAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for tick_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        _tickAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          return;
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for tick_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for tick_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _tickAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _tickAsyncRustFutureFree(futureHandle);
    }
  }

  late final int Function(ffi.Pointer<Utf8> input) _validateAsync = _lib.lookupFunction<ffi.Uint64 Function(ffi.Pointer<Utf8> input), int Function(ffi.Pointer<Utf8> input)>('validate_async');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _validateAsyncRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_string');
  late final void Function(int handle) _validateAsyncRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_string');
  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _validateAsyncRustFutureComplete = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_string');
  late final void Function(int handle) _validateAsyncRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_string');

  Future<String> validateAsync(String input) async {
    final ffi.Pointer<Utf8> inputNative = input.toNativeUtf8();
    final int futureHandle;
    try {
      futureHandle = _validateAsync(inputNative);
    } finally {
    calloc.free(inputNative);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _validateAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _validateAsyncRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for validate_async: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final ffi.Pointer<Utf8> resultPtr = _validateAsyncRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          if (resultPtr == ffi.nullptr) {
            throw StateError('Rust returned null for validate_async');
          }
          try {
            return resultPtr.toDartString();
          } finally {
            _rustStringFree(resultPtr);
          }
        }
        if (statusCode == _rustCallStatusError) {
          final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
          if (errorPtr != ffi.nullptr) {
            try {
              final String errorPayload = errorPtr.toDartString();
              throw AsyncErrorExceptionFfiCodec.decode(jsonDecode(errorPayload));
            } finally {
              _rustStringFree(errorPtr);
            }
          }
          throw StateError('Rust async error without payload for validate_async');
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for validate_async');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for validate_async with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _validateAsyncRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _validateAsyncRustFutureFree(futureHandle);
    }
  }

  late final void Function(int handle) _workerFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('worker_free');

  late final int Function() _workerCtorNew = _lib.lookupFunction<ffi.Uint64 Function(), int Function()>('worker_new');

  Worker workerCreateNew() {
    final int handle = _workerCtorNew();
    return Worker._(this, handle);
  }

  late final int Function(int handle, bool fail) _workerAsyncCheckedDescribe = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle, ffi.Bool fail), int Function(int handle, bool fail)>('worker_async_checked_describe');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _workerAsyncCheckedDescribeRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_string');
  late final void Function(int handle) _workerAsyncCheckedDescribeRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_string');
  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _workerAsyncCheckedDescribeRustFutureComplete = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_string');
  late final void Function(int handle) _workerAsyncCheckedDescribeRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_string');

  Future<String> workerInvokeAsyncCheckedDescribe(int handle, bool fail) async {
    final int futureHandle;
    futureHandle = _workerAsyncCheckedDescribe(handle, fail);
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _workerAsyncCheckedDescribeRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _workerAsyncCheckedDescribeRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for worker_async_checked_describe: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final ffi.Pointer<Utf8> resultPtr = _workerAsyncCheckedDescribeRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          if (resultPtr == ffi.nullptr) {
            throw StateError('Rust returned null for worker_async_checked_describe');
          }
          try {
            return resultPtr.toDartString();
          } finally {
            _rustStringFree(resultPtr);
          }
        }
        if (statusCode == _rustCallStatusError) {
          final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
          if (errorPtr != ffi.nullptr) {
            try {
              final String errorPayload = errorPtr.toDartString();
              throw AsyncErrorExceptionFfiCodec.decode(jsonDecode(errorPayload));
            } finally {
              _rustStringFree(errorPtr);
            }
          }
          throw StateError('Rust async error without payload for worker_async_checked_describe');
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for worker_async_checked_describe');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for worker_async_checked_describe with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _workerAsyncCheckedDescribeRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _workerAsyncCheckedDescribeRustFutureFree(futureHandle);
    }
  }

  late final int Function(int handle, ffi.Pointer<Utf8> items) _workerAsyncCounts = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle, ffi.Pointer<Utf8> items), int Function(int handle, ffi.Pointer<Utf8> items)>('worker_async_counts');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _workerAsyncCountsRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_string');
  late final void Function(int handle) _workerAsyncCountsRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_string');
  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _workerAsyncCountsRustFutureComplete = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_string');
  late final void Function(int handle) _workerAsyncCountsRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_string');

  Future<Map<String, int>> workerInvokeAsyncCounts(int handle, Map<String, int> items) async {
    final String itemsNativeJson = jsonEncode(items.map((key, value) => MapEntry(key, value)));
    final ffi.Pointer<Utf8> itemsNative = itemsNativeJson.toNativeUtf8();
    final int futureHandle;
    try {
      futureHandle = _workerAsyncCounts(handle, itemsNative);
    } finally {
    calloc.free(itemsNative);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _workerAsyncCountsRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _workerAsyncCountsRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for worker_async_counts: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final ffi.Pointer<Utf8> resultPtr = _workerAsyncCountsRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          if (resultPtr == ffi.nullptr) {
            throw StateError('Rust returned null for worker_async_counts');
          }
          try {
            final String payload = resultPtr.toDartString();
            return (jsonDecode(payload) as Map<String, dynamic>).map((key, value) => MapEntry(key, (value as num).toInt()));
          } finally {
            _rustStringFree(resultPtr);
          }
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for worker_async_counts');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for worker_async_counts with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _workerAsyncCountsRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _workerAsyncCountsRustFutureFree(futureHandle);
    }
  }

  late final int Function(int handle) _workerAsyncDescribe = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle), int Function(int handle)>('worker_async_describe');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _workerAsyncDescribeRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_string');
  late final void Function(int handle) _workerAsyncDescribeRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_string');
  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _workerAsyncDescribeRustFutureComplete = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_string');
  late final void Function(int handle) _workerAsyncDescribeRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_string');

  Future<String> workerInvokeAsyncDescribe(int handle) async {
    final int futureHandle;
    futureHandle = _workerAsyncDescribe(handle);
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _workerAsyncDescribeRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _workerAsyncDescribeRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for worker_async_describe: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final ffi.Pointer<Utf8> resultPtr = _workerAsyncDescribeRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          if (resultPtr == ffi.nullptr) {
            throw StateError('Rust returned null for worker_async_describe');
          }
          try {
            return resultPtr.toDartString();
          } finally {
            _rustStringFree(resultPtr);
          }
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for worker_async_describe');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for worker_async_describe with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _workerAsyncDescribeRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _workerAsyncDescribeRustFutureFree(futureHandle);
    }
  }

  late final int Function(int handle, ffi.Pointer<Utf8> input) _workerAsyncLabelEcho = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle, ffi.Pointer<Utf8> input), int Function(int handle, ffi.Pointer<Utf8> input)>('worker_async_label_echo');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _workerAsyncLabelEchoRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_string');
  late final void Function(int handle) _workerAsyncLabelEchoRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_string');
  late final ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _workerAsyncLabelEchoRustFutureComplete = _lib.lookupFunction<ffi.Pointer<Utf8> Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), ffi.Pointer<Utf8> Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_string');
  late final void Function(int handle) _workerAsyncLabelEchoRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_string');

  Future<String> workerInvokeAsyncLabelEcho(int handle, String input) async {
    final ffi.Pointer<Utf8> inputNative = input.toNativeUtf8();
    final int futureHandle;
    try {
      futureHandle = _workerAsyncLabelEcho(handle, inputNative);
    } finally {
    calloc.free(inputNative);
    }
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _workerAsyncLabelEchoRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _workerAsyncLabelEchoRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for worker_async_label_echo: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final ffi.Pointer<Utf8> resultPtr = _workerAsyncLabelEchoRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          if (resultPtr == ffi.nullptr) {
            throw StateError('Rust returned null for worker_async_label_echo');
          }
          try {
            return resultPtr.toDartString();
          } finally {
            _rustStringFree(resultPtr);
          }
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for worker_async_label_echo');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for worker_async_label_echo with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _workerAsyncLabelEchoRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _workerAsyncLabelEchoRustFutureFree(futureHandle);
    }
  }

  late final int Function(int handle) _workerAsyncValue = _lib.lookupFunction<ffi.Uint64 Function(ffi.Uint64 handle), int Function(int handle)>('worker_async_value');
  late final void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData) _workerAsyncValueRustFuturePoll = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, ffi.Uint64 callbackData), void Function(int handle, ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Uint64 callbackData, ffi.Int8 pollResult)>> callback, int callbackData)>('rust_future_poll_u32');
  late final void Function(int handle) _workerAsyncValueRustFutureCancel = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_cancel_u32');
  late final int Function(int handle, ffi.Pointer<_RustCallStatus> outStatus) _workerAsyncValueRustFutureComplete = _lib.lookupFunction<ffi.Uint32 Function(ffi.Uint64 handle, ffi.Pointer<_RustCallStatus> outStatus), int Function(int handle, ffi.Pointer<_RustCallStatus> outStatus)>('rust_future_complete_u32');
  late final void Function(int handle) _workerAsyncValueRustFutureFree = _lib.lookupFunction<ffi.Void Function(ffi.Uint64 handle), void Function(int handle)>('rust_future_free_u32');

  Future<int> workerInvokeAsyncValue(int handle) async {
    final int futureHandle;
    futureHandle = _workerAsyncValue(handle);
    final StreamController<int> pollEvents = StreamController<int>.broadcast();
    final callback = ffi.NativeCallable<ffi.Void Function(ffi.Uint64, ffi.Int8)>.listener((int _, int pollResult) {
      pollEvents.add(pollResult);
    });
    try {
      _workerAsyncValueRustFuturePoll(futureHandle, callback.nativeFunction, 0);
      while (true) {
        final int pollResult = await pollEvents.stream.first;
        if (pollResult == _rustFuturePollReady) {
          break;
        }
        if (pollResult == _rustFuturePollWake) {
          _workerAsyncValueRustFuturePoll(futureHandle, callback.nativeFunction, 0);
          continue;
        }
        throw StateError('Rust future poll returned invalid status for worker_async_value: $pollResult');
      }
      final ffi.Pointer<_RustCallStatus> outStatusPtr = calloc<_RustCallStatus>();
      try {
        final int resultValue = _workerAsyncValueRustFutureComplete(futureHandle, outStatusPtr);
        final int statusCode = outStatusPtr.ref.code;
        if (statusCode == _rustCallStatusSuccess) {
          return resultValue;
        }
        if (statusCode == _rustCallStatusCancelled) {
          throw StateError('Rust future was cancelled for worker_async_value');
        }
        final ffi.Pointer<Utf8> errorPtr = outStatusPtr.ref.errorBuf;
        if (errorPtr != ffi.nullptr) {
          try {
            throw StateError(errorPtr.toDartString());
          } finally {
            _rustStringFree(errorPtr);
          }
        }
        throw StateError('Rust future failed for worker_async_value with status code: $statusCode');
      } finally {
        calloc.free(outStatusPtr);
      }
    } catch (_) {
      _workerAsyncValueRustFutureCancel(futureHandle);
      rethrow;
    } finally {
      await pollEvents.close();
      callback.close();
      _workerAsyncValueRustFutureFree(futureHandle);
    }
  }
}

final class _WorkerFinalizerToken {
  const _WorkerFinalizerToken(this.free, this.handle);
  final void Function(int) free;
  final int handle;
}

final class Worker {
  Worker._(this._ffi, this._handle) {
    _finalizer.attach(this, _WorkerFinalizerToken(_ffi._workerFree, _handle), detach: this);
  }

  final FuturesStressFfi _ffi;
  int _handle;
  bool _closed = false;

  static final Finalizer<_WorkerFinalizerToken> _finalizer = Finalizer((token) {
    token.free(token.handle);
  });

  bool get isClosed => _closed;

  void close() {
    if (_closed) {
      return;
    }
    _closed = true;
    _finalizer.detach(this);
    _ffi._workerFree(_handle);
  }

  void _ensureOpen() {
    if (_closed) {
      throw StateError('Worker is closed');
    }
  }

  static Worker create() {
    return _bindings().workerCreateNew();
  }

  Future<String> asyncCheckedDescribe(bool fail) {
    _ensureOpen();
    return _ffi.workerInvokeAsyncCheckedDescribe(_handle, fail);
  }

  Future<Map<String, int>> asyncCounts(Map<String, int> items) {
    _ensureOpen();
    return _ffi.workerInvokeAsyncCounts(_handle, items);
  }

  Future<String> asyncDescribe() {
    _ensureOpen();
    return _ffi.workerInvokeAsyncDescribe(_handle);
  }

  Future<String> asyncLabelEcho(String input) {
    _ensureOpen();
    return _ffi.workerInvokeAsyncLabelEcho(_handle, input);
  }

  Future<int> asyncValue() {
    _ensureOpen();
    return _ffi.workerInvokeAsyncValue(_handle);
  }

}

final class WorkerFfiCodec {
  const WorkerFfiCodec._();

  static int lower(Worker value) => value._handle;

  static Worker lift(int handle) => Worker._(_bindings(), handle);
}

FuturesStressFfi? _defaultBindings;

FuturesStressFfi _bindings() => _defaultBindings ??= FuturesStressFfi();

void configureDefaultBindings({ffi.DynamicLibrary? dynamicLibrary, String? libraryPath}) {
  _defaultBindings = FuturesStressFfi(dynamicLibrary: dynamicLibrary, libraryPath: libraryPath);
}

void resetDefaultBindings() {
  _defaultBindings = null;
}

Future<int> addAsync(int left, int right) {
  return _bindings().addAsync(left, right);
}

Future<void> assertNotEmptyAsync(String value) {
  return _bindings().assertNotEmptyAsync(value);
}

Future<int> checkedAddAsync(int left, int right) {
  return _bindings().checkedAddAsync(left, right);
}

Future<List<Uint8List>> chunksEchoBytesAsync(List<Uint8List> input) {
  return _bindings().chunksEchoBytesAsync(input);
}

Future<Uint8List> echoBytesAsync(Uint8List input) {
  return _bindings().echoBytesAsync(input);
}

Future<Uint8List> echoBytesCheckedAsync(Uint8List input) {
  return _bindings().echoBytesCheckedAsync(input);
}

Future<String> greetAsync(String name) {
  return _bindings().greetAsync(name);
}

Future<String> labelEchoAsync(String input) {
  return _bindings().labelEchoAsync(input);
}

Future<Uint8List?> maybeEchoBytesAsync(Uint8List? input) {
  return _bindings().maybeEchoBytesAsync(input);
}

Future<Map<String, int>> summarizeAsync(Map<String, int> values) {
  return _bindings().summarizeAsync(values);
}

Future<void> tickAsync() {
  return _bindings().tickAsync();
}

Future<String> validateAsync(String input) {
  return _bindings().validateAsync(input);
}

